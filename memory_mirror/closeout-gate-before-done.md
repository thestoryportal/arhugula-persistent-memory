---
name: closeout-gate-before-done
description: "Every experiment/session close ends with `python3 tools/closeout_check.py <D-ID>` = ✅ ALL GREEN — the canonical-tracker close-out is a MECHANICAL gate, not a prose norm; never hand the operator the job of checking it. ⚠️ BUT the gate checks PRESENCE not CURRENCY — a refined result after the first close-out won't re-propagate automatically; re-propagate to the FULL set on every refinement"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: f4d82a89-8dda-448d-8fc0-cf79fc2d6af9
---

The write-side of the read↔write context loop (propagating a result to ALL canonical trackers) **silently lapsed** this loop (2026-06-21): I updated the salient docs (the `CORPUS/NN` writeup, runbook §0.3, `SESSION_CHECKPOINT`, memories) but missed the scattered trackers (`CORPUS/00-03`, `EVIDENCE_INDEX`, `EXPERIMENT_REGISTRY`, `PROGRESS`, `HYPOTHESIS_REGISTER`). The operator had to catch it.

**Root cause:** the close-out set was **prose-only** (DISCIPLINE §1.1 / runbook §0.4) with **no enforcement gate**, and the documented list was itself **incomplete** (omitted PROGRESS, CORPUS/01, CORPUS/02, HYPOTHESIS_REGISTER). Under a long session the soft norm got partially applied.

**The fix (durable, in-repo):** `tools/closeout_check.py <D-ID>` greps every canonical tracker (+ runbook §0.3/§12/§13 section-level + the CORPUS writeup + memory-mirror sync) for the Decision-ID and exits non-zero on any gap. It is wired as a **BINDING gate** in DISCIPLINE §1.1 + §3, runbook §0.4, CLAUDE.md, AGENTS.md, AUTONOMY.md.

**How to apply:**
- An experiment/session is **NOT done** until `python3 tools/closeout_check.py <D-ID>` is **✅ ALL GREEN**. Run it as the LAST step before declaring done or writing the checkpoint — and BEFORE telling the operator it's complete.
- Checking coverage is the **script's** job, never the operator's. If the operator is asking "did you update X?", the gate failed to run.
- When a new canonical tracker is added to the program, extend `REQUIRED` in `tools/closeout_check.py` (the gate is only as complete as that list).
- Write canonical docs via shell/python (Edit-tool reverts on this network FS — [[verify-canonical-state-edits-persist]]); the gate re-reads from disk so it doubles as a persistence check.

**⚠️ CURRENCY BLIND SPOT (caught by the operator 2026-06-21, D-D1-2):** the gate is a **string-presence** check — it verifies the D-ID *appears* in each tracker, NOT that each tracker's *content is current*. When a result **REFINES after the first close-out** (D-D1-2: the `k≤2` guardrail was later revised to `k≤1` + a mixed-load addendum + a seed-2 across-held-out finding), the gate stayed ✅ GREEN while **7 ledgers (CORPUS/00-03, EVIDENCE_INDEX, EXPERIMENT_REGISTRY, PROGRESS) silently kept the STALE `k≤2` one-liner** — because the refinements were hand-folded only into the *salient* docs (amendment, checkpoint, §0.3, CORPUS/22). There is **no auto-sync**; a green gate gave **false comfort about currency**.
- **How to apply:** every time a result CHANGES (not just first-closes), **re-propagate the new content to the FULL canonical set**, not only the docs you happened to hand-edit. Treat each refinement as its own mini close-out.

**✅ FIXED 2026-06-21 — the gate is now CONTENT/CURRENCY-aware (operator-requested):** `tools/closeout_check.py` has two layers — PRESENCE (D-ID in each tracker) + **CURRENCY**: for D-IDs registered in `tools/closeout_fingerprints.json` (D-ID → canonical SOURCE span), the gate hashes that span and requires the token `<D-ID>@<8hex>` in every tracker. When the source content changes, the hash changes → every tracker carrying the OLD token reads **STALE** (a distinct state from MISSING) → the gate goes RED until re-propagated. Verified: editing the result re-stales all trackers.
- **How to use it:** (1) register the D-ID's canonical source span in `closeout_fingerprints.json`; (2) `python3 tools/closeout_check.py --fp <D-ID>` prints the current token; (3) when you write/refine a result, embed that token in EVERY tracker alongside the prose; (4) on any later refinement of the source, re-run `--fp`, update each tracker's prose + token, re-run the gate to ✅. Bare-D-ID D-IDs (no registry entry) fall back to presence-only (back-compatible).
- **Discipline still required:** the fingerprint forces you to *re-touch* every tracker on a change (it can't verify the prose is *correct*, only that the tracker was updated to the current version-token) — so embed the token in the SAME edit that fixes the prose; don't stamp tokens without updating content.

**✅ EXTENDED to a 3-LAYER ANTI-DRIFT SYSTEM (2026-06-21, reconciled Opus + gpt-5.5 — full how-to in `tools/ANTI_DRIFT.md`):** the fingerprint gate is only layer 3. (1) **DE-DUP** — full detail in ONE source; other docs carry D-ID+verdict+pointer, NOT restated copies (copying is what caused k≤2→k≤1). (2) **GENERATE** structured status from `docs/program_state.json` via `tools/render_state.py --write` into `<!-- BEGIN GENERATED:* -->` blocks (PROGRESS ⑤ / runbook §0.3 / checkpoint / bootstrap / README) = the real *auto-update*. (3) **FINGERPRINT** narrative refs (this gate). A **pre-commit hook** (`tools/git_hooks/`, `core.hooksPath`) BLOCKS commits with stale generated blocks or stale fingerprints (`git commit --no-verify` to bypass). **Honest scope: auto-ENFORCES currency + auto-UPDATES generated content; CANNOT auto-write prose; only guards REGISTERED edges → de-dup is the primary defense.** Do NOT fingerprint process docs/spec or chase repo-wide fingerprinting (false-green). Right-sized — stop there; don't hold the F1 science hostage to doc integrity.

**Point-in-time, not a permanent invariant (2026-06-25):** the gate's GENERATED 'latest' trackers (SESSION_CHECKPOINT/PROGRESS/§0.3, rendered from program_state) and its §13 check (reads only after the LAST `### §13` header) hold only the **most recent** D-ID. So closing out a NEW experiment renders the generated blocks + appends a new §13 entry → the PREVIOUS D-ID's gate goes RED. **This is expected, not a regression — do NOT chase it.** Each experiment is green AT ITS CLOSE; the permanent multi-experiment record lives in the MANUAL trackers (CORPUS/00–03, EVIDENCE_INDEX, EXPERIMENT_REGISTRY, HYPOTHESIS_REGISTER — all carry every D-ID; §13 keeps every entry physically). Verify older findings persist there, not via re-running `closeout_check <old-D-ID>`.