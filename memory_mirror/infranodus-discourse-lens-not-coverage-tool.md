---
name: infranodus-discourse-lens-not-coverage-tool
description: "InfraNodus surfaces discourse structure/clusters/gaps over the TEXT you give it; it CANNOT read code, verify an implementation, or assess coverage of a prior-art corpus. For \"are there gaps in repo/corpus X relevant to our science,\" enumerate→triage→READ; don't InfraNodus."
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 7398eed9-f57d-4c0c-9015-e7c0c33b2ff7
---

InfraNodus is a **text-network-analysis lens**: word co-occurrence → topical clusters, betweenness gateways, structural gaps, discourse-diversity state (biased/focused/diversified/dispersed) — **over whatever TEXT you feed it.** The operator repeatedly asks whether to use it for an analysis task; match the tool to the claim.

**RIGHT tool for:** synthesizing a body of text you ALREADY have. Demonstrated 2026-06-23 — fed the tooling-audit corpus → it returned a "dispersed" discourse state + 3 content gaps (Test/Debug↔Stats, Experiment-Refine↔Bias-Audit, Power↔Bias-Audit) that genuinely sharpened the adoption order into "build ONE pre-register→verify-runs-stats→bias-audit gate, don't scatter." A discourse-structure / ideation lens.

**WRONG tool for:** (a) reading CODE or verifying what an implementation actually does; (b) anything needing semantic understanding of files you haven't read. It only sees the text you give it.

**REFINED 2026-06-23 (operator corrected an over-claim):** I had said it's wrong for "assessing COVERAGE / gaps in our repo." Too strong. It CAN do **workflow/repo gap analysis** — the mechanism is: *I* enumerate the repo into text corpora (it has no filesystem; the corpus is mine to build), then feed them to `difference_between_texts` / `merged_graph_from_texts` and read `contentGaps`. **Proven:** workflow-doc corpus vs full-repo corpus (heading-level) → it ranked "**governance/contract-prototype layer is a structural island**" as the #1 gap (3/3 merged gaps), which grep then CONFIRMED (governance=3 hits vs experiments=18–23; no prototype→canonical promotion path) — plus a verified **no spec-level threat-model/security-review gate** gap (workflow "security" = secret-scanning only). The enumerate→read discipline still holds (the corpus + the verification are mine); InfraNodus is the LENS that RANKS the gaps, and its output stays LEADS until grep/read-verified. Caveat: small/heading corpora return `modularity:0` (degenerate clusters) — lean on `contentGaps`, not cluster stats; feed fuller body text for sharper signal. Plumbing: text must be authored inline (no file-path/repo input), so filter+cap the corpus to ~authorable size.

**Why:** [[match-metric-to-the-claim]] generalized to *tool selection* — the goal "coverage / relevance-mapping" has metric "enumerate→triage→read," not "discourse-diversity." **Proven the same session:** the chrishayuk/LARQL gap-map (`docs/CHRISHAYUK_CORPUS_GAP_MAP.md`) — LQL has no "violates" grammar, ffn_down is a Q6_K/Q4_K mix, a hidden `REBALANCE` solver — was ALL code-level facts InfraNodus could never surface; **a Tier-1 deep-read (fan-out subagents) was required.**

**How to apply:** for "are there gaps in this corpus relevant to our science," **enumerate the repos → triage by relevance → deep-read the on-domain ones (fan-out subagents) → map to our open questions** (runbook §0.3 / hypothesis register). Use InfraNodus only as an optional *end-stage* thematic lens over text you've already extracted. Fence unchanged: InfraNodus outputs = LEADS, never CORPUS evidence ([[research-first-and-verify-tool-availability]]).

**Can it replace the token-heavy markdown read/write in our workflows? (asked + TESTED 2026-06-23 — corrected by operator + docs.)** The split is by FIDELITY, not storage. **Durability claim RETRACTED:** I first argued "off-git, non-durable" — WRONG. Per `docs/INFRANODUS_OFFICIAL_DOCS_REVIEW.md:100` InfraNodus exports **JSON/CSV/GEXF**, tagged statements **MD (Obsidian)**, API returns **Graphology JSON + DOT**; the MCP returns graph structure into context (`includeGraph:true`); the **llm-wiki** skill is purpose-built for an append-only git-committed `[[wikilinks]]` base. Durable git-committable graph artifacts ARE producible (wrote one: `/workspace/MEMORY_INDEX.graph.poc.md`). Off-git was only true of the *hosted* `memory_add_relations` feature, not the export/skill path. **The real boundary (demonstrated):** generate_knowledge_graph over MEMORY.md gave a faithful, compact, committable **MAP** (9 clusters, gateways by betweenness, structural gaps, DOT edges) — but DROPPED the load-bearing facts: `edited 100% vs native 97.4%` lost both numbers (`100`/`97.4` never became nodes), `PASS→CONFOUNDED` survived only as co-occurring tokens not a directional claim, and `statements:[]` came back empty. So: **YES** replace the token-heavy *navigational/synthesis* reads with a regenerable committed graph INDEX (the map layer); **NO** for the *evidence/canonical-fact* layer — and the repo's own BINDING fence already says exactly this (InfraNodus = LEADS, never `CORPUS/` evidence — `SESSION_BOOTSTRAP §5` + all 3 INFRANODUS_* docs). Viable architecture = two git-committed layers: **graph index (regenerable map) + canonical markdown statements (source of truth)** = the llm-wiki pattern. Cheapest token win is still git-native de-dup to one-source+pointers ([[anti-drift-enforce-not-autowrite]]); the graph index is complementary, best where large files get re-read just to find *where* things are.
