---
name: notebooklm-install-and-notebook-ids
description: "NotebookLM programmatic access (notebooklm-py) — DURABLE across pod restarts as of 2026-06-22 (wipe-tested): auth on /workspace/.notebooklm + idempotent reinstall script. After a restart run `bash /workspace/tools/setup_notebooklm.sh` once at session start, then `notebooklm use <id>; notebooklm ask`. Two relevant notebook IDs below."
metadata: 
  node_type: memory
  type: reference
  originSessionId: 1a56870e-a5ec-406a-9964-827f761992a9
---

**Programmatic NotebookLM access** (`teng-lin/notebooklm-py`), installed 2026-06-21 per the repo's recommended path (operator-requested). Full detail + reactivation recipe: `tools/codex-skills/notebooklm-mcp-codex-audit.md` (top banner).

- **Install:** `pip install uv` → `uv tool install "notebooklm-py[browser]"` → **`notebooklm-py==0.7.2`**, CLI at **`/root/.local/bin/notebooklm`**. Put `~/.local/bin` on PATH: `export PATH="$HOME/.local/bin:$PATH"`.
- **✅ AUTH LIVE + WORKING (2026-06-21):** the operator logged in on their laptop and supplied `storage_state.json`; it's installed **OUTSIDE the repo** at `/root/.notebooklm/profiles/default/storage_state.json`. `notebooklm auth check --test --json` → `status: ok` (token_fetch true). Chromium is present — `notebooklm ask` runs **headless from the pod** end-to-end (test query returned a real grounded answer). So I (or the operator) can query NotebookLM via Bash: `export PATH="$HOME/.local/bin:$PATH"; notebooklm use <id>; notebooklm ask "<q>"`.
- **⚠ NEVER put `storage_state.json` in the repo tree** (it holds live Google cookies; repo is PUBLIC). It briefly leaked once (committed via `git add -A`, force-push-purged) — see [[git-add-all-leaks-secrets-on-public-repo]]. Keep it under `~/.notebooklm/` only; the repo's `tools/notebooklm-verify-tmp/` is gitignored.
- **⭐ DURABLE SETUP (2026-06-22, wipe-tested):** `/workspace/tools/setup_notebooklm.sh` makes the CLI survive pod restarts. The pattern mirrors `setup_codex.sh`: **auth lives on the persistent volume** at `/workspace/.notebooklm/` (gitignored by `**/storage_state.json` + `/.notebooklm/`), and `~/.notebooklm` is a **symlink** to it — so cookie *rotations write through to /workspace* and are never lost. The CLI binary itself is reinstalled on each boot (`uv tool install notebooklm-py==0.7.2`, ~10s; plain pkg, **no `[browser]`/Playwright** — headless `ask`/`list` only need the cookie→token path). **After a restart: `bash /workspace/tools/setup_notebooklm.sh` once at session start** (semi-manual — no real pod-init hook; "restart == new session"; documented in `SESSION_BOOTSTRAP.md §5`). Verified by simulating the wipe (removed install + `~/.notebooklm`) then rebuilding from durable state → live grounded `ask` succeeded. ⚠ MFS forces 0666 perms so `chmod 600` won't stick on the cookie file — fine (single-tenant root pod; the real protection is the gitignore vs the PUBLIC repo).
- **Re-auth (only if cookies expire / are absent):** operator re-logs-in on laptop → copies a fresh `storage_state.json` to **`/workspace/.notebooklm/profiles/default/`** (the durable path, NOT into the repo tree), then re-run the setup script. (Cookies can't be minted on a headless pod.)
- **(historical)** Initial login path: `notebooklm login` opens a browser for Google sign-in — **can't complete on a headless pod** (no display); hence the laptop-login + copy-cookie-file approach above.
- **⭐ Relevant notebook IDs (operator-provided 2026-06-21 — use these, NOT the old `Agent Harness Engineering` pin):**
  - `f667f1f2-7624-4039-b521-6ca37b437b6b`
  - `23ba5f2d-8317-4412-bb8a-f7c30af4c017`
  - select with `notebooklm use <id>`; `notebooklm ask "<q>"`; `notebooklm list` to confirm titles.
- **⚠ Pod restart wipes `~/.local` + `~/.notebooklm/`** ([[pod-restart-wipes-system-python-ml-stack]]) — but this is now **handled by the durable setup above**: just `bash /workspace/tools/setup_notebooklm.sh` (no laptop `login` needed unless the persisted cookies have expired).
- **⭐ UNIFIED RESTORE (2026-06-22, supersedes per-tool steps):** `bash /workspace/tools/restore_pod_tools.sh` rebuilds NotebookLM **+ InfraNodus + Perplexity** in one shot from the gitignored bundle `/workspace/.pod_restore/` (snapshot via `tools/backup_pod_tools.sh`). For NotebookLM it restores the 20M uv venv **by copy** (no reinstall) then calls `setup_notebooklm.sh` for cookies; for the two MCP servers it atomically merges their config blocks + API keys back into `~/.claude.json` (MCP tools register only after a SESSION RELOAD). Wipe-tested end-to-end. Detail: `SESSION_BOOTSTRAP.md §5`.
- **Discipline:** NotebookLM = corpus-grounded **advisory synthesis**, NOT authoritative for load-bearing claims; check our own `research_and_specs/` corpus FIRST ([[research-first-and-verify-tool-availability]]); outputs are leads/context, evidence still comes from `CORPUS/` + primary source.
