---
name: c10-status-and-session-b-residual-test
description: Deployment target FIXED (local Intel CPU + batch writes); C10 multi-token-value falsifier OPEN/CONDITIONAL/bounded; session-B = option B residual-test with project-coined multi-word SEMANTIC values
metadata: 
  node_type: memory
  type: project
  originSessionId: 77383f65-76b8-42d1-a7de-d3da62645250
---

**Deployment target FIXED 2026-06-26 (operator): `local Intel CPU + batch writes`** (re-Genesis model). F1 re-scoped (`docs/F1_DETERMINATION.md` header block): C4/§8.7/D1-incremental DROP from critical path; **C1 REFRAMES** to batch-genesis-at-scale (gated on TARGET FACT-COUNT — still unset, worth pinning); **C10 multi-token values + C3 firing + C2 read-layer + real-Intel-CPU-serving** = the on-target frontier.

**C10 (D-C10-1, CORPUS/35) = FALSIFIER FIRED, OPEN/CONDITIONAL, severity-BOUNDED-not-dissolved.** At the N≤100 batch path: single-token 97% & prior-coherent multi-token 97% paraphrase-robust, but **arbitrary (non-prior-coherent) multi-token values 36%** (the edit FITS the trained prompt but doesn't GENERALIZE). §7.1/D1 removes the SYNTACTIC subset (file paths/identifiers/exact strings → Git, not in-weight), BUT the residual = **project-coined multi-word SEMANTIC named entities (§7.2 domain_concept) with no prior** = the fragile class, plausibly ENRICHED (a project KB's value-prop IS the base-model-unknown facts). c10c: naive diverse-training is NOT a fix (interference); the wall = generalization at the continuation position, localized → mechanistic case FOR the AnyEdit fix.

**⭐ OPTION B EXECUTED 2026-06-26 → RESIDUAL FRAGILE (D-C10b-residual, CORPUS/36).** 7-arm novel-insert (same 24 fictional subjects): single 98.6% & coherent-bigram 98.6% robust, but **realistic project-coined multi-word SEMANTIC values express at only 19–31% held-out-paraphrase full-seq** (RANGE [A7 coined-coined 19.4%, A6 coined+real-noun 30.6%]) — ≪85% gate, BELOW even the 2-tok floor (45.8%) and the original C10 floor (36%) because realistic coined values tokenize LONG (4–5.5 tok). **→ the severity-bound prediction is empirically CONFIRMED: the real in-weight KB value class IS fragile, not a corner case** (answers Perplexity caveat #4 — realistic terms are NOT compositional enough to rescue full-sequence usability). Two precise claims (advisor+cross-family calibrated): (1) C10 CONDITION confirmed-and-WORSENED; (2) dominant MECHANISM = a DISTINCT multi-token **length-realization wall**, NOT C10's fit-then-fail-to-generalize parrot — realistic arms fail to even FIT the canonical prompt (canon 37–46% vs floor 95.8%). **z-probe (advisor cheap-check):** compute_z reaches 0.99 target-prob for ALL value classes @25≈100 steps → NOT a *compute_z* knob (the z-target is achievable at 0.99); the bottleneck RELOCATES to W-realization (the linear W-edit can't install the long continuation) — but the W-realization knobs (band width / layer count / edit strength) are UNTESTED → option A (AnyEdit) is mechanistically CONSISTENT with the failure, NOT proven NECESSARY; the cheap resolver to run BEFORE a multi-day port = re-run one realistic arm with a wider/later band or more layers and see if canon_full climbs. Option A over C remains operator-gated. Sub-finding: continuation prior-coherence mildly PROTECTIVE per-token (teacher-forced 60–72% vs floor 46%) but length-overwhelmed (cite tf_pertok, NOT the length-confounded para_full decomposition — [[name-the-manipulated-variable-not-the-arm-intent]]). Design hardened by the pre-build advisor gate: added A7 coined-head+coined-modifier as the BINDING no-prior-continuation worst case (A6 real-noun continuation gets prior help by construction = flattering artifact); demoted A5 single-coined-WORD out of R (§7.1 identifier-adjacent); deliverable = a RANGE not a bit. Runners `experiments/track_c/{c10d_residual_coined,c10d_zprobe}.py`; pre-reg `docs/C10_RESIDUAL_TEST_PREREG.md`.

**Remaining open moves (operator-gated): (A)** AnyEdit port (`jianghoucheng/AnyEdit`, Qwen2.5-compat; multi-day; PORT real solve, don't reimplement — BetaEdit lesson; mechanistically CONSISTENT-not-proven-necessary — the z-probe relocates the bottleneck to W-realization, whose knobs (band width/layer count/edit strength) are UNTESTED → try a wider/later band FIRST before the multi-day port; payoff unproven) vs **(C)** accept C10 as the bounded limitation (viable only if CORE in-weight values are single-token / prior-coherent). **(B) the residual-test is COMPLETE.**

Fresh-session entry: `SESSION_CHECKPOINT.md` top (2026-06-26 continuation) + `docs/F1_DETERMINATION.md` (C10 row + severity bound).
