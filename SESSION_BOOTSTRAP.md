# SESSION BOOTSTRAP — read this FIRST in any fresh context
_Updated 2026-06-18. This is the single entry point for re-grounding a new session on the "LLM-as-Database" program. It orients; the detail lives in the files it points to. If you are a fresh agent context: read this top-to-bottom, then `CORPUS/README.md`, then skim the spec. Do not start work until grounded._

<!-- BEGIN GENERATED:program-state -->
**📍 PROGRAM STATE (updated 2026-06-26 (AnyEdit advisor FIX-FIRST resolved by token-alignment gate; prereg next))** — _auto-generated from `docs/program_state.json` — DO NOT edit between the markers; run `python3 tools/render_state.py --write`._

- **North star:** F1 — prove/falsify the 'LLM-as-Database' spec is implementable BEFORE it is built; deliver a ready / not-ready-with-conditions determination. Falsification-first.
- **Latest:** **DEPLOYMENT TARGET FIXED (operator): `local Intel CPU + batch writes` → F1 re-scoped + first on-target falsifier fired.** Re-scope (advisor-checked for over-rotation): C4/§8.7/D1-incremental DROP from the critical path (incremental-only); **C1 REFRAMES** to batch-genesis-cleanliness-at-scale (gated on target fact-count, NOT deleted); **C10 multi-token values ELEVATED**. **C10 (D-C10-1, CORPUS/35) = FALSIFIER FIRED:** 3-arm novel-insert at the N≤100 batch path (binding = held-out-paraphrase full-sequence) — single-token 97.2% & prior-COHERENT multi-token 97.2% robust, but arbitrary INCOHERENT multi-token values 36.1% (first-token 70.8%; conditional P(full|first)=0.51 vs coherent 1.00). The edit FITS the trained prompt (95.8%) but does NOT GENERALIZE to paraphrases → the multi-token instance of the R13/R5 trained-prompt-parrot axis. **C10 = the FIRST condition both on the critical path for the fixed target AND failing → OPEN MUST-FIX; named fix = AnyEdit per-token editing.** 3 advisor gates + Perplexity cross-family; scoped 3B/band[4-8]/AlphaEdit/N≤100/1-seed/HF-fp16 (single-token + prior-coherent values already robust). F1 net readiness UNCHANGED (NOT-READY-WITH-CONDITIONS) but the target now has a concrete blocker with a known fix to try. ⮕ FOLLOW-UP (c10c, D-C10b-diverse): tested whether paraphrase-diverse training rescues the incoherent failure — it does NOT cleanly (CANON+PTRAIN degraded everything via 3×-request interference → uninformative); replicates C10 (38.9≈36.1). The wall is GENERALIZATION at the continuation position, cleanly localized (single-token 97% → first-of-multi 82% → continuation collapses) — a reasonable mechanistic case FOR the AnyEdit fix (canon=100% is a teacher-forcing artifact, not 'already solved'). AnyEdit port = OPERATOR-GATED (multi-day; mechanistic case but payoff empirically unproven; PORT jianghoucheng/AnyEdit, don't reimplement). Alternative: accept C10 as a documented value-class limitation (single-token + prior-coherent values work today). ⮕ SEVERITY BOUND (spec-grounded, advisor-corrected — NOT a downgrade): §7.1/D1 (LLM=semantics, Git=syntax) removes the SYNTACTIC subset (paths/identifiers/exact strings → Git) from the in-weight fragility surface, BUT the fragility axis is prior-coherence (orthogonal to syntax) → the residual = project-coined multi-word SEMANTIC named entities (§7.2 domain_concept) with no prior = the non-prior-coherent fragile class, plausibly ENRICHED (the value-prop of an in-weight project KB is exactly the base-model-unknown facts). C10 stays OPEN/CONDITIONAL: bites iff CORE in-weight facts include project-novel multi-word semantic values (a project KB likely has). Both the AnyEdit port AND an empirical residual-test are OPERATOR-GATED.

**⮕ RESIDUAL-TEST DONE 2026-06-26 (D-C10b-residual, CORPUS/36 — operator's Option B): RESIDUAL FRAGILE.** 7-arm novel-insert (same 24 fictional subjects): single 98.6% & coherent-bigram 98.6% robust, but **realistic project-coined multi-word SEMANTIC values express at only 19–31% held-out-paraphrase full-seq** (RANGE [A7 coined-coined 19.4%, A6 coined+real-noun 30.6%]) — ≪ the 85% gate, below the 2-tok floor (45.8%) AND the original C10 floor (36%) because they tokenize LONG (4–5.5 tok). **→ the severity-bound prediction is empirically confirmed: the in-weight value class a real project KB holds IS fragile, not a corner case.** Two precise claims: (1) C10 CONDITION CONFIRMED-and-WORSENED; (2) dominant MECHANISM = a DISTINCT multi-token length-realization wall, NOT C10's fit-then-fail parrot — realistic arms fail to even FIT the canonical prompt (canon 37–46% vs floor 95.8%). z-probe (advisor cheap-check): compute_z reaches 0.99 target-prob for ALL classes @25≈100 steps → NOT a *compute_z* knob (z-target achievable at 0.99); bottleneck RELOCATES to W-realization, whose knobs (band/layer-count/edit-strength) are UNTESTED → option A (AnyEdit) CONSISTENT-not-proven-necessary; cheap resolver = wider/later band BEFORE a port. Continuation prior-coherence mildly protective per-tok (60–72% vs 46%) but length-overwhelmed. Advisor before build+verdict; Perplexity cross-family on interpretation. F1 net readiness UNCHANGED. Scope 3B/band[4-8]/N≤24/1-seed; NOT promotable.

**⮕ BAND-KNOB DONE 2026-06-26 (D-C10e-bandknob ⟨D-C10e-bandknob@82b491dc⟩, CORPUS/37): NO MATERIAL KNOB RESCUE.** C10e tested the cheapest W-realization knob before AnyEdit: baseline `[4,8]` vs later `[8,12]` on the hard A7 coined-coined arm, with A1/A2 controls. Later band **does NOT rescue held-out behavioral readout**: A7 `para_full` worsens **13.9 to 5.6** (-8.3pp), `para_first` worsens **75.0 to 33.3**, and teacher-forced continuation worsens **60.2 to 34.0**. The canonical trained-prompt diagnostic improves only **29.2 to 45.8** (+16.6pp), still below the pre-registered rescue threshold and below 60; `para_full` remains binding. A1/A2 controls remain high (97.2 / 91.7 later), so the failure is within-tested-arms specific to long no-prior coined-coined values. Scope 3B/N=24/1-seed/HF-fp16; NOT promotable. This eliminates the pure later-band explanation only; wider `[4,12]`, layer-count/edit-strength, and AnyEdit remain untested/consistent-not-proven-necessary. F1 readiness unchanged.

**⮕ WIDENED-BAND DONE 2026-06-26 (D-C10f-band412 ⟨D-C10f-band412@d93d6a19⟩, CORPUS/38): NO MATERIAL KNOB RESCUE.** C10f tested the next cheap W-realization knob before AnyEdit: baseline `[4,8]` vs widened `[4,12]` on the hard A7 coined-coined arm, with A1/A2 controls. The widened recipe **does NOT rescue held-out behavioral readout**: A7 `para_full` worsens **11.1 to 6.9** (-4.2pp), `para_first` worsens **72.2 to 37.5**, and teacher-forced continuation worsens **56.2 to 40.1**. Canonical trained-prompt fit improves only **29.2 to 41.7** (+12.5pp), below the W-realization-only threshold and far below usability. A1/A2 controls remain above floor but degrade **100.0 to 90.3**. Scope 3B/N=24/1-seed/HF-fp16; NOT promotable. This eliminates this `[4,12]` band-widening recipe only; edit-strength/layer-count and AnyEdit remain untested/consistent-not-proven-necessary. F1 readiness unchanged.

**⮕ STRENGTH/LAYER SWEEP DONE 2026-06-26 (D-C10g-strengthlayers ⟨D-C10g-strengthlayers@d691acab⟩, CORPUS/39): NO BEHAVIORAL RESCUE WITH ONE NON-VIABLE TRADEOFF.** C10g tested the final bounded pre-AnyEdit MEMIT knob pass: strength150 `[4,12]`, lowcov2500 `[4,12]`, and deep `[4..12,18..22]` on hard A7 with A1/A2 controls. No candidate reached behavioral lead: baseline A7 `para_full` 20.8 (15/72); strength150 13.9 (10/72) with clean controls; lowcov2500 5.6 (4/72) with clean controls despite canon 45.8; deep canon 58.3 but A7 `para_full` 0/72 and controls collapse A1 37.5/A2 22.2. LAW#5 all pass. This closes cheap MEMIT band/strength/layer-count route for this scoped A7/Qwen2.5-3B run only; does not prove AnyEdit necessary or MEMIT generally exhausted. F1 unchanged.

**⮕ ANYEDIT PRE-COMMIT TRIAGE DONE 2026-06-26 (D-C10h-anyedit-triage): PROCEED-TO-VIABILITY-GATED PILOT DESIGN; NOT EVIDENCE.** Independent advisor-review returned **PROCEED** with a hard first gate: code-level viability audit before harness authoring. Implementation triage of the official repos found AnyEdit is the smaller first port target: `jianghoucheng/AnyEdit` ships `AlphaEdit_ARE/` + `MEMIT_ARE/`, Qwen2.5-7B hparams, and an autoregressive per-token/window loop, but assumes old deps (`transformers==4.23.1`) + A100-class headroom and uses Qwen hparams (`lm_head`) that do not directly match the local Qwen2.5-3B/transformers==4.51.0 harness (`model.embed_tokens`, local clamp/cov settings). Therefore the next scientific move is NOT running upstream as-is; it is a transplant preserving local MEMIT primitives and LAW#5 inertness, then a minimal A7 + A1/A2 held-out full-sequence pilot. FABLE is real and relevant but heavier/more invasive (EasyEdit/UnKE-style layer-parameter optimization with preservation examples), so it is a fallback if AnyEdit is infeasible or fails cleanly. EV-6 broad AnyEdit-neighborhood review confirms this path but upgrades the gate: `DCDmllm/AnyEdit` and RefEdit are image-editing name collisions; LLM AnyEdit remains `jianghoucheng/AnyEdit`; μKE/AnyEdit++ make fixed-window dependency logging mandatory; COIN makes context-reliance mandatory; SUIT is a cleaner over-edit fallback; NeuralDB/SCR increase pressure on the side-store/gated-overlay alternative. This review strengthens AnyEdit as the best first C10 code target, but does not prove it necessary or sufficient; accept-bounded remains the alternative.

**⮕ ANYEDIT VIABILITY AUDIT DONE 2026-06-26 (D-C10h-anyedit-viability; docs/C10_ANYEDIT_VIABILITY_AUDIT.md): PROCEED-TO-PREREG/ADVISOR, BUT DEFAULT WINDOW IS INVALID AS PRIMARY C10 TEST.** Code-level audit confirms the official LLM AnyEdit repo is the correct target and a harness-side transplant is plausible without editing the local MEMIT engine or downgrading dependencies. Critical correction: upstream `window_size=50` creates only one window for C10 A7 values because they tokenize to 4-6 Qwen2.5 tokens (mean 5.5); a C10 per-token/window rescue must preregister a small-window primary condition (likely `window_size=1`) plus any default-window run only as diagnostic. External advisor-review returned FIX-FIRST: prove standalone `answer` tokenization matches the `question + answer` continuation suffix before prereg/harness. The no-GPU token-alignment gate now passes for current A7 stimuli (24/24 suffix alignment with leading-space answers; `window_size=50` still one window), so prereg may proceed only with this gate embedded as an abort condition.
- **F1 status:** DETERMINATION DRAFTED 2026-06-24 (`docs/F1_DETERMINATION.md`) = **NOT-READY-WITH-CONDITIONS** (draft-with-open-conditions, NOT a clean close; shippable as the honest current state). Deployment data-path spine PROVEN-FOR-SCOPE (recipe→A1 batch-clean→B3 Q4_K_M→E1·A CPU-serve; 3B/N≤100/band[4-8]/single-batch/1-seed/fictional). Architecture = scope-keyed COMPACTION-BOUNDED HYBRID (D-B3N-1), in-weight a serving copy per the verified §11.2/D42 structural finding (no class weights-authoritative). Determination carries a 10-row CONDITIONS REGISTER of first-class falsifiers: C1 compaction-at-scale (sharpest; D20 directionally negative) · C2 read-contract (biggest gap) · C3 R5-at-scale · C4 7B numeric transfer (OQ-W1) · C5 governance+orchestration (prototyped-not-empirical) · C6 security (un-red-teamed) · C7 Pruning/GC (untouched) · C8 R15-adversarial · C9 R9-native-deletion · C10 multi-token/G7. C1/C2 closing negative would move the verdict toward not-implementable-as-specified. Closure-path lead = UNGATED `tools/power.py` pool-sizing.
- **Next actions (priority):**
  1. DEPLOYMENT TARGET = local Intel CPU + batch writes (re-Genesis model). F1 CONCLUDED but the target has a concrete on-critical-path blocker (C10). Operator-steer, in priority:
  2. (1) C10 — RESIDUAL FRAGILE plus THREE MEMIT KNOB FAILURES plus ANYEDIT PRE-COMMIT TRIAGE/GRAPH CONTEXT (D-C10b-residual/CORPUS/36; D-C10e-bandknob ⟨D-C10e-bandknob@82b491dc⟩/CORPUS/37; D-C10f-band412 ⟨D-C10f-band412@d93d6a19⟩/CORPUS/38; D-C10g-strengthlayers ⟨D-C10g-strengthlayers@d691acab⟩/CORPUS/39; D-C10h-anyedit-triage): realistic project-coined multi-word SEMANTIC values remain the on-target blocker. Later `[8,12]`, widened `[4,12]`, and the bounded strength/layer-count sweep all fail to rescue A7 held-out behavior. Advisor-review + code triage now say PROCEED with AnyEdit only through a code-level viability gate: transplant the official AnyEdit autoregressive per-token/window loop into the local Qwen2.5-3B/transformers==4.51.0 harness, preserve MEMIT primitives + LAW#5, then pilot A7 with A1/A2 controls. EV-5/EV-6 add prereg diagnostics, not new evidence: context-reliance probe (canonical/context-prefix vs held-out paraphrase), chunk/window dependency logging, per-token/window continuation, locality/bystander deltas, and name-collision filters (`DCDmllm/AnyEdit`/RefEdit are image-editing). μKE is the first targeted fallback if fixed-window AnyEdit fails by dependency; SUIT is the cleaner over-edit fallback; NeuralDB/SCR keep the side-store/gated-overlay architecture pressure live. Step-1 viability audit is now done: proceed only to prereg/advisor/harness design, with a required correction that upstream `window_size=50` is one window for 4-6 token A7 values and cannot be the primary per-token C10 test. Next gate: draft prereg with the token-alignment gate embedded, small-window primary (likely `window_size=1`), default-window diagnostic, context-prefix/window/locality guardrails, vector-count/delta-norm/locality interpretation, and LAW#5 before any edit evidence. Remaining fork after a valid pilot: (A) AnyEdit/small-window rescue, or (B) accept-as-bounded with project-coined multi-word facts routed through index/side-store.
  3. (2) Q4_K_M survival of multi-token values (B3 covered single-token only) — the CPU-serve quantization check for the fixed target.
  4. (3) batch-genesis-at-scale cleanliness (C1 reframed) — gated on TARGET FACT-COUNT (worth pinning: single solve under the 2000 cap vs sub-batched = the D20 cliff).
  5. (4) real-Intel-CPU serving validation (E1 was a pod-CPU proxy; the operator's actual hardware unvalidated).
  6. STANDING: re-auth codex/gpt-5.5 (Perplexity-Sonar = working cross-family fallback). Guardrails: pre-register + advisor before build AND before verdict · name-the-covering-clause / test-the-confound before stamping a result · cross-family on decisive falsifiers.
<!-- END GENERATED:program-state -->

> **⚠️ CURRENT STATUS (2026-06-21) — §3 "immediate next work" below is STALE (it predates the empirical arc).** Since then: **G6.1** (cross-entity read corruption falsifier), **A1** (batch eliminates it), **B1/B3** (size + Q4_K_M survival), **C2/C2-band**, **D1** (capacity law → drift is per-relation-concentration, not global edge-count), **B1 size-term** (law model-general on 7B), and **D-D1-2 ⟨D-D1-2@e023d8d2⟩** (§8.7 numeric guardrail `k≤2`, dual-reviewed; mixed-load shows it needs a global-volume bound too) are all DONE. The §8.7 structural amendment is **operator-approved** (`docs/SPEC_8_7_AMENDMENT_DRIFT_CONCENTRATION.md`). **NEXT = 7B size-term via determinism (OQ-W1 transfer) → B3 (in-weight-necessity vs routed side-store, the field-convergence signal from the 2026-06-21 ConnectedPapers review, register §J) → CP2 schema build-items → F1 readiness.** Canonical "what's next" = `EXPERIMENT_RUNBOOK.md` §0.3 + `SESSION_CHECKPOINT.md` top block (NOT §3 below).

═══════════════════════════════════════════════════
## 0. WHAT THIS PROGRAM IS (one screen)
We are pre-development-validating a **specification**: "LLM as Database — Agent Harness Architecture" (`research_and_specs/llm-as-database-v1_2-integrated-spec.md`). The thesis: treat a transformer's FFN as a **physically queryable + editable graph database** — knowledge lives **in-weight** (semantic "map"), edited via **MEMIT**, stored as **`.vindex`** overlays on a **frozen base**, queried via **LQL** (LARQL), with Git holding the **code** (syntactic "territory"). The spec was authored/debated by a **7-skill Framework Council** (`/workspace/skills/`). The goal of ALL the experimentation (this session + many prior months) is to **prove the spec is implementable before building it** — falsification-first, evidence over opinion.

**Operator context:** the user is the spec owner, **learning ML/LLM as they go** — make every decision *legible*, never push an expertise-gated call onto them, and defer hard calls to *evidence*, not authority. Deployment target is **TBD** — may be a remote GPU, not necessarily a local Intel CPU (so the pod is a valid proxy).

═══════════════════════════════════════════════════
## 1. RE-GROUND IN THIS ORDER
0. **`EXPERIMENT_RUNBOOK.md`** (NEW 2026-06-18) — the LIVING operating roadmap from the current blocker to spec implementation-readiness: role/system-prompt, tools, disciplines, hygiene, knowledge-base index, decisions ledger, dependency/sourcing catalog, the Track A–F experiment decision tree (with pre-written code), readiness checklist, risk register, live status dashboard. **Its §0.3 is the single source of "what's next." Start here for any experimental work.**
1. **This file** (arc + norms + environment).
2. **`CORPUS/README.md`** → then `CORPUS/00_MASTER_EVIDENCE.md` (every experiment + exact numbers + status), `03_STATUS_LEDGER.md` (proven/corrected/open + the CP1-3 checkpoints), `06_SPEC_WRITE_PATH_AND_BRIDGE_FRAMING.md` (how our work maps to the spec — the most important framing).
3. **The spec** `research_and_specs/llm-as-database-v1_2-integrated-spec.md` (§7-§12 are the six layers; read them — do NOT work from grep excerpts, that caused a real misframing this session).
4. **Auto-memory** (`MEMORY.md` index, loaded each session) — durable lessons + traps.
5. `LARQL_INTEGRATION_ASSESSMENT.md` (the LARQL narrative + the bridge).

═══════════════════════════════════════════════════
## 2. THE ARC — what the experiments were meant to evidence, and what they surfaced
**Arc:** spec authored (council) → **write-engine viability determination** (Phase 0 metric calibration → Phase 1 model/size dependence; `write_engine_viability_determination_report.md`) → **pre-dev validation** (Tiers 1-2: durability, novel-insert, quantization, CRUD, compaction, scale, diversity; `spec_predev_validation_plan.md`) → **recipe development** (in-solve AlphaEdit + preserve-sampling + batched compile) → **Qwen3 re-validation** (deployment-servable model, A1-A5) → **LARQL integration + the decoupled bridge**. (Prior months live in the artifacts: `framework_finding_v1_10` = 10 iterations; `s229_*` etc. = many prior sessions; git/memory hold the rest.)

**What is PROVEN (mechanics)** — full detail in `CORPUS/00`:
- In-weight multi-field editing is viable, **model/size-dependent** (GPT-J fails; Qwen-7B clean; Qwen-3B & Qwen3-0.6B work with the recipe).
- **Recipe:** in-solve AlphaEdit (null-space/orthogonal projection, `nullspace_threshold=0.005`) + preserve-sampling (cross-entity) + batched-per-record compile (novel inserts). Edits survive 4-bit. Full CRUD + compaction hold. Transfers to Qwen3 with no recalibration.
- LARQL serves edited weights **in-weight on CPU**; the **bridge** (our clean ΔW → `.vlp` overlay → `APPLY` on frozen base → `COMPILE`) works end-to-end, no LARQL code (`build_vindex_overlay.py`).
- Our AlphaEdit null-space step **aligns with the spec's mandatory D20 orthogonal-projection safeguard.**

**What is NOT proven (the next work)** — `CORPUS/03` + `06`:
- **CP1** governed *in-pipeline* MEMIT write (we only did offline compile + the bridge bypasses the Gate — a C16 violation as a production path). **CP2** LARQL query-schema capability (`SELECT` read-back = spec's mandatory L1 probe, `DELETE FROM EDGES`, triple/relation-families/`violates`). **CP3** confirm AlphaEdit counts as "MEMIT" (D12). Then governance gaps **G1** 2PC/State-Ledger, **G2** authz/audit, **G3** validation pipeline; capability/scale **G6** (real GGUF-Q4_K, scale), **G7** multi-token.

**The key reframe (don't relitigate):** there is **no "bridge-vs-spec-architecture" decision** to agonize over. The spec IS the architecture (Commit-Executor→Gate→2PC→Ledger governed write path); our recipe is the **engine internals**; LARQL is the **`.vindex`/serve** layer; the bridge was **viability scaffolding**. Architecture-*ownership* is settled; the open items are empirical viability tests (CP1-3, G*) — **pod-runnable now**.

═══════════════════════════════════════════════════
## 3. IMMEDIATE NEXT WORK — UPDATED 2026-06-18 (CP1-3 + G1/G2/G3 DONE; G6/G7 next)
**DONE this session (all PROVEN-FOR-SCOPE; see `CORPUS/07`–`12` + the consolidated handoff at the top of `SESSION_CHECKPOINT.md`):** CP1 governed in-pipeline parametric write · CP2 LARQL query-schema (verbs run; L1-triple/5-families/violates are OUR layer) · CP3 D12 MEMIT-compliance confirmed (C15 band open) · G1 dual-medium 2PC+ledger+TC+circuit-breaker · G2 real-Ed25519 security (verify-cannot-forge) · G3 deterministic validation pipeline (delivers the CP2-surfaced contracts).

**NEXT — G6/G7 (the categorically different, empirical run):** real GGUF-Q4_K (not the crude sim quantizer), larger Qwen3, overlay size at scale + many-overlay behavior, multi-token value robustness, and the **CP3 C15 band recalibration** (our band [4-8] diverges from C15's L15-25/32-layer). ⚠️ Unlike CP1-G3 (self-authored contract prototypes that pass because we built them to — [[prototype-tautology-trap]]), **G6/G7 can ACTUALLY FAIL** → it is the binding falsification evidence. **Set falsifiable pass criteria BEFORE the runs; let numbers fall where they land.** Then D1/G5 (deployment hardware) contingent.
**Scope reminder:** all the above are *viability prototypes*, NOT the production harness (later dev project; orchestrator Path A/B/C TBD). Each = bounded experiment + durable result + honest CORPUS update.

═══════════════════════════════════════════════════
## 4. HOW TO WORK HERE (norms — these are load-bearing)
- **Engine + LARQL are UNMODIFIED.** Workarounds are config/our-own-code only. Honor the CLAUDE.md LAWs: fingerprint gate, determinism, known-baseline reproduction, **read source before authoring**, science-path-patch isolation (prove inertness). One-fix-then-halt discipline.
- **Evidence discipline:** set falsifiable pass criteria BEFORE the result; cite artifact path + exact number or flag `UNVERIFIABLE`; distinguish `EVIDENCE-SHOWS` vs `I-INFER`; **log every correction/reversal honestly** (see `CORPUS/03` — this session had several). Do NOT glaze over failures; do NOT overclaim ("high-certainty/proven" got recalibrated twice by the advisor — keep language precise: *mechanics proven; these contracts untested*).
- **Advisor:** call `advisor()` (no args; it sees the full transcript) BEFORE substantive work, when stuck, when changing approach, and before declaring done. Weight it heavily; it caught real over-corrections this session. It's enabled via `/advisor` (model: Opus 4.8).
- **Council caveat:** the 7-skill council is a **spec-authoring** tool. Same-model subagents reading our own corpus = **weak independence / confirmation-amplification** — do NOT use them to "confirm" self-authored conclusions. A *different* model cold-reading the corpus is better, but **review ≠ evidence**; the binding signal is empirical.
- **HIL gates:** stop and surface decisions that are the operator's to make; make them legible; offer a recommendation, not a survey; don't infer approval from navigation.
- **Durable-save discipline:** save deliverables to `/workspace`; update the CORPUS + `SESSION_CHECKPOINT.md`; re-mirror memory (`cp /root/.claude/projects/-root/memory/*.md /workspace/memory_mirror/`). `/dev/shm/` is RAM (ephemeral) — reproducible from scripts, never the source of truth.

═══════════════════════════════════════════════════
## 5. ENVIRONMENT & PERSISTENCE (traps that cost time this session)
- Pod: RunPod, RTX 4090 (24GB) + ~500GB RAM + `/dev/shm` 29GB tmpfs (RAM-speed). `/workspace` = persistent network volume (slow I/O — run heavy I/O on `/dev/shm`, not the network FS). `/` overlay = ephemeral on pod restart.
- **transformers pin = 4.51.0** (Qwen3 support + memit engine work; 4.45 lacks Qwen3, 5.x breaks the engine nethook). tf 4.51 uses `torch_dtype=`. torch 2.4.1+cu124.
- **LARQL build deps** (apt): rust(rustup), libssl-dev, pkg-config, cmake, protobuf-compiler, libopenblas-dev. Cap BLAS threads (`OPENBLAS_NUM_THREADS=8` etc.) or `convert` thrashes. LARQL is Metal-first; CPU path works (positive control).
- **Reusable assets:** `build_vindex_overlay.py` (the bridge packager, model-agnostic); `qwen3_06b_memit_hparams.json` / `qwen25_3b_memit_hparams.json`; `covariance_caches/` (Qwen3 band cached — ~70 min to recompute fresh); editing scripts `s24*.py` (engine at `memit_dry_run/memit`); LARQL at `external_prior_art/larql` (`cargo build --release`; binary `target/release/larql`).
- **On a fresh POD (not just context):** restore memory: `mkdir -p /root/.claude/projects/-root/memory && cp /workspace/memory_mirror/*.md /root/.claude/projects/-root/memory/`. Rebuild `/dev/shm` artifacts from scripts. Re-`cargo build` LARQL if `/` was wiped.
- **⭐ UNIFIED POD-TOOL RESTORE (2026-06-22, wipe-tested) — `bash /workspace/tools/restore_pod_tools.sh`:** ONE script rebuilds all three research tools (NotebookLM CLI + InfraNodus MCP + Perplexity MCP) from the durable, gitignored bundle `/workspace/.pod_restore/` (snapshot it with `tools/backup_pod_tools.sh` whenever a key/version changes). It restores the NotebookLM venv by copy (falls back to `setup_notebooklm.sh`'s uv reinstall) + wires cookies; unpacks the npx package cache; and **atomically, non-destructively merges** the InfraNodus(global) + Perplexity(project `/root`) `mcpServers` blocks **with their API keys** back into `~/.claude.json` (creates missing keys; never clobbers other state). It then boot-checks each MCP server standalone (confirmed InfraNodus 1.1.0 + Perplexity 0.9.0 handshake with the restored keys), and **re-installs the InfraNodus Claude skills** into `~/.claude/skills` (from the durable master `tools/infranodus-skills/`, gitignored; via `tools/install_infranodus_skills.sh` — **13 active**; the 2 T3 skills `embodied-navigation`/`perspective-reversal` are held back by the installer's `PRUNED` exclude-list). **⚠ MCP tools AND skills only register on a SESSION RELOAD** — run the script, then reload. Perplexity is project-scoped to cwd `/root` (the default); for cwd-independence move it under root `mcpServers`. The detailed per-tool bullets below remain the reference for what's under the hood.
- **InfraNodus MCP (verified working 2026-06-22; tools appear after a session reload):** Node v20 lives at **`/workspace/node-v20/`** (persistent — survives restarts; no system node/npx otherwise). But `/root/.claude.json` is on the ephemeral `/` overlay → a **pod restart wipes the MCP config** (now auto-restored by `restore_pod_tools.sh` above; this is the manual fallback). To restore by hand, re-add the `mcpServers.infranodus` block (command `/workspace/node-v20/bin/npx`, args `["-y","infranodus-mcp-server"]`, env `INFRANODUS_API_KEY=25530:8b14…` + `PATH` including `/workspace/node-v20/bin`) then reload the session. The server (v1.1.0, 32 tools) + API key are confirmed functional via a live `analyze_text` round-trip. ⚠ The earlier malformed config (`command:"env", args:["25530:…","npx",…]`) could NOT launch — key must be `INFRANODUS_API_KEY=…` in `env`, and npx needs node on PATH. Fence unchanged: InfraNodus outputs = LEADS → hypothesis register, never CORPUS/ evidence ([[research-first-and-verify-tool-availability]]).
- **NotebookLM CLI (durable across restarts as of 2026-06-22 — wipe-tested):** the `notebooklm` CLI (teng-lin/notebooklm-py) is a uv-installed Python tool on the ephemeral overlay, so a restart wipes the binary **and** `~/.notebooklm` auth. Durability = **auth on `/workspace/.notebooklm`** (the live Google cookies + future rotations; gitignored) + an idempotent reinstall script. **After a restart, run once at session start:** `bash /workspace/tools/setup_notebooklm.sh` — it symlinks `~/.notebooklm → /workspace/.notebooklm`, reinstalls the CLI via uv (~10s, plain `notebooklm-py==0.7.2`, no Playwright needed for headless queries), and reports auth. Then `export PATH="$HOME/.local/bin:$PATH"; notebooklm use <id>; notebooklm ask "<q>"`. This is **semi-manual** (no real pod-init hook here; "restart == new session"). Cookies can't be minted headless — if `auth check` fails, the operator re-exports `storage_state.json` from a laptop login into `/workspace/.notebooklm/profiles/default/`. Notebook IDs + fence: NotebookLM = advisory synthesis, never CORPUS/ evidence ([[notebooklm-install-and-notebook-ids]]).

═══════════════════════════════════════════════════
## 6. TASK LEDGER (carry forward — recreate as TaskCreate items in the fresh session)
DONE: A1-A5 (Qwen3 re-validation), B1-B4 (overlay characterization), D2 (LARQL native write immature); **CP1, CP2, CP3, G1, G2, G3 (all 2026-06-18, PROVEN-FOR-SCOPE — `CORPUS/07`–`12`)**. OPEN: **G6/G7** (scale/efficiency + multi-token + C15 band — the empirical falsification run, gated next); D1/G5 (deployment-hardware) contingent. (Detail in `CORPUS/03` + `00 §Known Gaps`; per-prototype caveats in `CORPUS/07`–`12`.)

> **Always-in-context discipline:** load `DISCIPLINE.md` — north-star goal (F1), context read-triggers, deep-thinking-on-failure protocol, and tool/loop thresholds (binds Claude + Codex).
