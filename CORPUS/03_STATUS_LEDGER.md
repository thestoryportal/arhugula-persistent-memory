# 03 — STATUS LEDGER (proven / partial / untested / corrected)
_The brutally honest disposition of every load-bearing claim, INCLUDING reversals. Council: do not re-litigate PROVEN; do not cite CORRECTED-FROM (superseded) conclusions; the OPEN/UNTESTED items are your targets._

## PROVEN (mechanics)
- In-weight same-entity editing viable, model/size-dependent (GPT-J fails; Qwen-7B/3B, Qwen3-0.6B work). [C1, P0/P1]
- In-solve AlphaEdit (null-space P, thresh 0.005) fixes sequential clobber: 33%→100% retention. [C2, R-*]
- Preserve-sampling fixes cross-entity-at-depth (held-out 22%→78%); coverage knob. [C3, T1.1b]
- Batched/joint compile fixes novel-record INSERT (0/6→6/6). [C4, T1.2c]
- Edits survive 4-bit (edit-survival 100%). [C5, T1.3]  (caveat: real GGUF-Q4_K untested)
- Full CRUD + compaction-regression hold, Qwen2.5 + Qwen3. [C6]
- Recipe transfers to Qwen3-0.6B with no recalibration. [C7]
- LARQL CPU inference correct (positive control). [L-CPU]
- Decoupled bridge: clean+in-weight+governed-overlay, no LARQL code, on CPU. [C8, L-BRIDGE]
- ROUTE VERIFY fixes the KNN retrieval cross-entity bleed (no code). [L-VERIFY]
- COMPILE bakes overlays→weights. [L-COMPILE]

## PARTIAL / clarifying
- T1.1: edited-fact retention + no-forgetting PROVEN; un-edited cross-entity-at-depth FALSIFIED-clean (needs preserve-sampling). 
- T2.6: layer-band tiering does NOT isolate collisions (it hurts); protection lives in cache_c+preserve in a SHARED band.
- A4/T2.2: scale retention holds; control-locality degrades with store size unless preserve-anchor coverage scales (Qwen3-0.6B faster, model-size effect).
- T2.3b (people domain): suggestive but UNDERPOWERED (2 entities).
- Governance: file-level frozen-base+overlay+rollback PROVEN; spec's 2PC/State-Ledger NOT (see G1).

## NEGATIVE (findings, not failures of the concept)
- GPT-J unusable for multi-field same-entity. [P1-GPTJ]
- LARQL NATIVE in-weight write (COMPOSE install) immature: corrupts at default, bleeds without preserve-sampling. [L-COMPOSE]
- LARQL COMPACT/MEMIT CLI flow blocked: "no edge metadata". [L-COMPACT]
  → These do NOT block the spec: the bridge bypasses them (our pipeline writes; APPLY+COMPILE serve).

## CORRECTED-FROM (reversals — DO NOT cite the superseded version)
- ❌ "LARQL is Gemma-only" → ✅ CORRECTED: `qwen.rs` supports Qwen2/2.5/3; Qwen2.5 garbage = extraction-bias bug. [L-BIAS]
- ❌ "Qwen2.5 garbage = CPU backend broken" → ✅ CORRECTED: CPU works (Qwen3 positive control); it was the missing bias. [L-CPU]
- ❌ "Option B needs LARQL development / cross-entity bleed unfixable without code" → ✅ CORRECTED: ROUTE VERIFY fixes the bleed; the bridge gives clean+governed+in-weight, no code. [L-VERIFY, L-BRIDGE]
- ❌ "rollback fails" → ✅ CORRECTED: file-level rollback works by construction (serve base alone); the REMOVE-command failure was unnamed-patch usage. [L-ROLL]
- ⚠️ "high-certainty viable path demonstrated" (one fact) → RECALIBRATED per advisor: mechanics proven; full spec contracts NOT. Use precise language (see 00 gaps).
- ⚠️ "DIFF INTO PATCH = governance overlay bridge" → CORRECTED: DIFF captures down-METADATA only (no vectors); the working bridge writes real down-vectors via our Python .vlp generator. [L-BRIDGE]
- ⚠️ T1.3 verdict field says "FAIL/DEGRADED" → that label = a too-strict ppl gate on a CRUDE quantizer; edit-survival (the real question) PASSED.

## OPEN / UNTESTED (the council's audit + local-testing targets)
- G1 Consistency: ✅ **PROVEN-FOR-SCOPE (2026-06-18, `g1_two_phase_commit.py`, `10_G1_*`).** Dual-medium 2PC with D46 ordering (Weights-ahead unreachable by construction), State Ledger Git↔overlay pairing + divergence detection (sanctioned-park-aware), Transaction Controller C-TC2 asymmetry (Structural auto-revert / Layer4 park+HIL, sole compensator), circuit breaker trip→READ_ONLY→precondition-gated reset, C-TPC4 bypass DETECTION. Remaining: content-level vindex integrity (vindex-side divergence is pointer-metadata, not artifact re-hash → G2/G6); full signed reset ceremony (→G2); PREPARED-timeout + cross-task 5-in-10min trip (deferred). 10/10 tests pass.
- G2 Security: ✅ **PROVEN-FOR-SCOPE (2026-06-18, `g2_security_layer.py`, `11_G2_*`).** REAL Ed25519. verify-cannot-forge STRUCTURAL (T-NO-PRIVKEY); content-level overlay integrity (defeats the metadata-rewrite that fooled G1); CAK CeremonyToken verifier (§20.3, structural exclusion refuses valid Orchestrator self-auth); edge-cap blast dimension; agent suspension; naive ledger-tamper detection. 9/9. Caveats: key CUSTODY not addressed (in-process keygen tests the VERIFIER; offline/HSM=v2); ledger = tamper-DETECTION not immutability (needs anchored head/Genesis Seal); blast-radius = per-patch edge-cap ONE dimension; audit_category = tagged not enforced; boot = simulated.
- G3 Validation: ✅ **PROVEN-FOR-SCOPE (2026-06-18, `g3_validation_pipeline.py`, `12_G3_*`).** Deterministic schema core delivers the CP2-surfaced contracts (violates/undeclared rejection vs real §7.3 families before MEMIT; storage-probe SPLIT = triple-SELECT expressibility + storage-pass/behavior-fail reconciliation) + §9 invariants (identity-collision independence, fail-fast cascade, code_pass_patch_fail disposition, injection no-retry, bounded retry control-plane, signed-PASS handoff G3→CP1). 8/8. Caveats: storage-truth needs behavioral signal (index=intent only); Reflexion=control-plane only; independence=identity-collision (D33 sequencing construction-asserted); only STATIC level real; determinism=documented property.
- G4 Query schema: ⏩ ADDRESSED across CP2 + G3. LARQL `DELETE FROM EDGES` works (CP2); the L1 SELECT-readback + 5-family + violates-rejection that LARQL lacked are now delivered at OUR schema layer (G3, `12_G3_*`: triple-SELECT expressibility + deterministic violates/undeclared rejection vs real §7.3 families). Larger-model relation-label quality still UNTESTED (→ G6).
- G5 Operator hardware: actual Intel CPU — UNTESTED (D1).
- G6 Efficiency/scale: **G6.1 many-overlay scale-of-N DONE (2026-06-18, `13_G6_1_*`) — SPLIT (first empirical falsifier).** Write-side PASS (98% retention @ N=100, apply-expression 100% through record 100, within-entity 95.6%, global 98.4%); **cross-entity consistency FAIL — held-out top-1 correctness on the edited relation collapses 100→91.7→58.3→41.7% with N (genuine relation-specific read corruption, scale-amplifying).** Falsifies "cross-entity-clean at scale" for subject-keyed AlphaEdit; **scopes the prior same-entity "VALIDATED."** Next falsifier = entity-aware in-solve projection at scale. STILL UNTESTED: overlay size-at-scale (characterization), larger Qwen3, real GGUF-Q4_K (volume-space gated).
- G7 Multi-token value robustness — UNTESTED (partial expression observed).

## VIABILITY CHECKPOINTS (gated FIRST; pod-runnable). ✅ ALL THREE DONE 2026-06-18 (CP1/CP2/CP3).
_These were briefly over-collapsed into "engineering details" in an earlier doc-06 draft; corrected. CP1 = PROVEN-FOR-SCOPE (`07_CP1_*`); CP2 = RESOLVED, mixed (`08_CP2_*`); CP3 = D12 CONFIRMED + C15 open divergence (`09_CP3_*`). The three gating checkpoints are cleared; next = governance gaps G1-G3 + capability/scale G6/G7._
- **CP1 — governed in-pipeline MEMIT write.** ✅ **PROVEN-FOR-SCOPE (2026-06-18; `cp1_governed_write.py`, `07_CP1_*`).** A deterministic non-reasoning executor hosts the GPU MEMIT compile in-process as a first-class step (answers the n8n/D70 execution-model question), Gate enforces 5×§10.2 checks as hard rejections, clean-fail atomicity holds (PREPARED-no-COMMITTED, frozen base untouched), hash-chained ledger intact. SCOPE = **parametric-only**; carried forward: dual-medium 2PC→G1, L1 SELECT probe→CP2 (behavioral stand-in used), asymmetric signing→G2, batch/multi-minute compile duration→G6. Cross-process overlay bit-repro does NOT hold (GPU FP) and is a non-requirement (§8.10 compaction re-runs MEMIT, verified behaviorally not by hash).
- **CP2 — LARQL query-schema capability.** ✅ **RESOLVED (2026-06-18; `cp2_query_schema_probe.sh`, `08_CP2_*`).** LQL verbs EXIST + execute (not INFER-only): `DELETE FROM EDGES` works (feature-level); `SELECT/INSERT INTO EDGES` parse+run over an entity/relation/target vocabulary. BUT load-bearing semantics diverge: **L1 `SELECT` read-back is NOT provided** — positive-control-confirmed it can't read back even a NATIVE fact (France→Paris) as a triple (`SELECT FROM EDGES` = weight-derived feature-introspection); the **5 relation families** are unmodeled (24,469 emergent decompiler labels); **`violates` is accepted, no C6/C9**. These three are SCHEMA/VALIDATION-LAYER contracts the spec assigns elsewhere — now concretely-scoped build items (→ schema layer / G3), NOT LARQL mods. **Back-fills CP1**: the deferred L1 probe needs OUR triple index, so CP1's behavioral stand-in was the correct call.
- **CP3 — MEMIT-compliance of our recipe.** ✅ **CONFIRMED (D12) + 1 open divergence (C15) (2026-06-18; `09_CP3_*`).** Method-class CONFIRMED: engine = kmeng01/memit reference; our code = closed-form down_proj solve with null-space `Pi` (not gradient FT). **D20 is the spine** — stock 2022-MEMIT has NO orthogonal projection, so literal-MEMIT *fails* D20's mandatory orthogonal-projection safeguard; the spec's "MEMIT" therefore *means* the family-with-safeguards, and the null-space step is spec-MANDATED, not a deviation. Not ROME/GRACE/FT. preserve-sampling + batched = standard MEMIT usage. **OPEN:** the edit band [4-8] (both 0.6B/28L and 3B/36L) is *early* under any charitable scaling of C15's L15-25/32L middle-to-late prescription (C15 stated declaratively, NOT provisional); passed-locality is supporting not vindicating → small-model band-vs-C15 is an unresolved divergence (→ OQ-W2/G5/G6; spec may need small-model recalibration).

## ⭐ 2026-06-18 (post-B3) — E1 / B1 / C2
- **E1 (deployment serving): SPLIT — Claim A PASS / Claim B FALSIFIED (`18_E1_*`, D-E1-1 ⟨D-E1-1@55708623⟩).** CPU loop closes via llama.cpp+Q4_K_M (edited 100%/native 97.4%, ~8-13 tok/s pod-CPU). LARQL `gguf-to-vindex` cannot serve Qwen2.5-3B: vocab-config mismatch (151643 vs tensor 151936) + 0/108 attention biases extracted → garbage. **A7 ablation = causal proof** (zeroing q/k/v bias in HF Qwen2.5-3B alone → garbage). Whole-story open (token-mismatch with LARQL garbage, non-diagnostic). LARQL CPU-ONLY here (no CUDA; Metal only). **Model-family split** logged. CORRECTED-FROM: the premature "conclusive" before de-confounding (operator-challenged → H1-H6 tested + A7 added). Did NOT debug LARQL Rust; did NOT run the airtight non-quant 3B safetensors de-confound.
- **B1 (size-density): PARTIAL (`19_B1_*`, D-B1-1 ⟨D-B1-1@2ebae54e⟩).** A1 batch-clean does NOT fully replicate at Qwen2.5-7B (held-out edited-rel top-1 100%→91.7%, one of 12 probes). Scopes A1 to 3B/N≤100. SECONDARY (7B less clean than 3B) directional-only — band relative-depth + pool + mom2_uw confounds; NOT a size law (D1's job). Single seed.
- **C2 (keying root-cause): PRUNED + mechanism (`20_C2_*`, D-C2-1 ⟨D-C2-1@e2eff6af⟩).** Relation-inclusive keying eliminated (raises same-relation collinearity to 0.93-0.99). Depth map: separability U-shaped, min L8-12, max late. C15 late-band = worst isolation zone (new tension). Collinearity→bleed link is plausible-not-causal (matters for the incremental path). New lead: band [8-12].

## ⭐ 2026-06-20 — D1 capacity law (concentration-vs-dilution drift variable)
- **D1: DONE = PARTIAL (dual-reviewed); ROBUST directional claim PROMOTABLE (`22_D1_CAPACITY_LAW`, D-D1-1 ⟨D-D1-1@0db8d819⟩).** Pre-registered (`docs/D1_CAPACITY_LAW_PREREG.md`, frozen). Phase 1 no-edit predictor map: covariate same-rel cross-ent collinearity @[4-8] capital 0.436>language 0.412>continent 0.333; D7 basis-rotation weak-moderate (dissociation 1.71→2.38, relation-clustering falls not entity rising). Phase 2 concentration-vs-dilution @ **fixed total-N=50** (Qwen2.5-3B band[4-8] seq; LAW#5 inertness gate ✓ |Δ|=0.0007; fixed disjoint 12-entity held-out): **CONCENTRATED (50 capital) more corrupted than DILUTED (17 cap+33 other) at equal total-N in 4/4 seeds** (CONC mean 52.1% vs DIL 83.3%; gaps 50/16.7/41.7pp; seed3 INVALID=continent cardinality-4 dilutant under-expressed 81.2%, directionally consistent). → **global `edge_count_since_anchor` (§8.7) is INSUFFICIENT as a relation-agnostic drift predictor; drift must be relation-concentration-aware** (central OQ-W1 / §7.2 'wrong variable' reconciliation, DIRECTIONAL). **NOT settled:** valid labels 2 CONFIRM + 1 PARTIAL; seed1's 8.3pp equal-capital-count gap → a **two-variable law** (relation-concentration + smaller cross-relation term), not pure single-variable; thresholds/dominance/model-size term UNSET (N≤50, 3B, band[4-8] only). **Dual-reviewed:** Opus `advisor()` (narrow + replicate-before-CORPUS) + **gpt-5.5 cross-family via Codex** (`FIX-FIRST`, applied: record PARTIAL not CONFIRM; 'falsified'→'insufficient'; 'dominant'→two-variable; don't over-lean single-seed p≈0.005; seed2@100%-dilutant rebuts weak-dilutant confound). **Independence CLOSED (directional).** **Phase 3 DONE (clean replication):** dose-response with language dilutant (100% expr all 9 arms — continent confound eliminated; currency's confident pool also collapses to ~5 euro-values, so capital+language are the only clean high-cardinality relations). At FIXED total-N=48 concentration dominates (R_pure k24 51%→k36 24%); cross-relation term = a small NON-NEGATIVE tendency (within-arm paired, 6 positive/3 zero/0 negative — a LOW-POWER directional sign signal, NOT a precise effect size); two-variable framing holds, magnitude UNRESOLVED. Triple-reviewed (Opus advisor caught a near-over-correction to 'single-variable'; gpt-5.5 cross-family review: don't headline the sign-test p as an effect size). **NEXT:** B1/Qwen3-4B model-size term (the larger F1 unknown) → quantitative law + §8.7 amendment for F1.

## ⭐ 2026-06-20 — C2-band falsifier (supervised fold-in)
- **C2-band: pre-registered LABEL=PASS, NOT PROMOTED to PROVEN (real-but-underpowered redistribution) (`21_C2BAND_*`, D-C2band-1 ⟨D-C2band-1@c6fb6103⟩).** Editing in the low-collinearity band [8-12] vs default [4-8] (sequential, N=100, Qwen2.5-3B, single seed): cross-entity JS locality 67.68→86.41 (+18.73pp, guard cleared ret 98/96 expr 100) → mechanical PASS on the frozen rule. **But not a PROVEN node:** (1) the top-1 leg (the metric matching the claim) is n.s. — 7/12→10/12, Fisher p≈0.37; (2) within-entity JS locality collapses 95.48→77.77 (−17.71pp, monotone; global fine 97.34→98.40); (3) band812 retention worse (96<98, 2× edits lost) = a REAL direction-specific REDISTRIBUTION (the within-loc FALL refutes a uniformly-weaker edit, which would RAISE both; expression 100% both arms excludes under-expression) traded against a within-entity perturbation + durability cost; (4) the deciding edited-entity within-attr top-1 was never logged (continent stable=100% is stability of already-wrong 33% reads); (5) no depth-matched sham control, bands not magnitude-matched, single seed/ordering. **Disposition:** C2-band lead is **REAL-BUT-UNDERPOWERED, not promoted** (effect established; cost magnitude + mechanism unmeasured) — NOT a deployment recipe change (batch path already clean at [4-8]); value is mechanistic (feeds D1 + C15 tension) IF de-confounded. De-confounders queued (a)+(b) gate: (a) same-entity cross-relation collinearity-vs-depth [no GPU edit], (b) edited-entity within-attr top-1 re-run, (c) per-band Δ-norm + sham control, (d) ≥3 seeds/≥2 orderings. **Cross-family review DONE (gpt-5.5 via Codex, `FIX-FIRST`):** concurs PASS-not-promoted; narrows the claim — only a *uniformly*-weaker edit is excluded, NOT Δ-norm/depth-metric/non-uniform under-editing; names the norm-matched/sham control (de-confounder c) as THE cheapest overturning test (now primary). Independence obligation CLOSED. **Process:** autonomy staged LABEL=ERROR (wall-clock) but the run completed — mislabeled completed run, corrected.

## ⭐ 2026-06-21 — B1 model-size term (D-B1-2 ⟨D-B1-2@0db8d819⟩)
- **B1 model-size term: DONE — concentration law MODEL-GENERAL; size threshold UNRESOLVED (`22` B1-§, D-B1-2).** Ported D1 dose-response to Qwen2.5-7B (advisor redirect from 4B: cached/free, clean same-generation axis). **REPLICATE:** corruption rises with per-relation edit-count on 7B (monotone on means, expr 100%) → the §8.7 per-relation-concentration amendment is **not 3B-specific**. **Size threshold UNRESOLVED:** 7B leans less-corrupted 7/8 paired cells (+11.5pp) but ≪ **~50pp run-to-run nondeterminism** (7B seed3 same-config 20.8→70.8 on re-run; the 4–21% 'collapse' was a non-reproducible noise draw, NOT a tail — advisor-mandated reproducibility re-run). NOT size-invariance, NOT size-worsens. **§8.7 structural amendment WRITTEN** (`docs/SPEC_8_7_AMENDMENT_DRIFT_CONCENTRATION.md`); numeric threshold blocks on a lower-variance instrument (NEXT-ARC). LAW#5 gates PASSED (|Δ|=0.0000–0.0003); VRAM fixes proven inert. Dual advisor pass.


### D-D1-2 ⟨D-D1-2@e023d8d2⟩ — §8.7 numeric-threshold instrument (2026-06-21)
**D-D1-2** (2026-06-21): §8.7 numeric-threshold instrument → **operational guardrail `k≤1`** (max unanchored per-relation concentration; anchor by k=2; WARNING k=2-3, HARD k=8-10 — REVISED down from k≤2 after the seed-2 across-held-out check). Dual-reviewed (Opus advisor + gpt-5.5 cross-family). k=3-4/k=10-12 = scoped order-dominated observations, NOT portable thresholds; per-relation count = fail-closed SENTINEL not the causal var (edit-set/key-collinearity geometry is). 3B-only (size transfer OPEN), pure-capital anti-conservative, incremental-path-only (deploy=batch/Genesis A1-clean). Instrument: 3B within-process SD=0; ~50pp noise is 7B/across-process; binding 3B uncertainty = edit-ORDER. Artifacts: results/d1_threshold_lowk_3b_s3{,_lowextra}.json, results/d1_instrument_variance_diagnostic_3b_*.json; reviews logs/codex_review_threshold_*OUT.log. **MIXED-LOAD:** pure-capital fails under +12 other-relation load (mixed clean ceiling k=0; driver=other-relation volume) → vindicates the worse-of(global,per-relation) amendment design; pair k≤1 with a global-volume bound + compaction. **SEED-2 (more-toxic held-out):** corrupts at k=1-2 where seed-3 was clean → ceiling k≤2→k≤1; no per-relation count is universally clean (held-out-dependent SENTINEL, not the causal var). +results/d1_mixedload_smoke_3b_s3.json, results/d1_threshold_lowk_3b_s2.json.

- **R15: DONE = not-ready-with-conditions (NOT promoted) (`24_R15_CONSTRAINT_PROBE`, D-R15-1).** **D-R15-1** (R15 L2 Constraint-probe firing; `CORPUS/24`, 2026-06-24): the sharpest **WEIGHTS-owned** read-contract falsifier (§21.2, **no delegation route**). v1 subject-property hazard edits express perfectly + fire under cooperative paraphrase (Tier-0 24/24, Tier-1 24/24) but **adversarial firing collapses to ~½** (hand-adj ~11–14/24 flag; ~7/24 silent compliance-leaks; 2/24 explicit refusals). Controls clean (global-shift 0/4; neutral property-specificity color/price 1/24; `Holding` 15/24 = hazard-adjacent, a v1 control-design lesson, pre-reg HALT reinterpreted post-hoc). **Not-ready-with-conditions** for the Constraint sub-type; **bounded** (easiest single-entity case; relational archetype expected worse). **Spec-gap flag (Finding 2):** §21.2 disjunctive pass admits warn-and-comply (Wexolite + ~3 borderline; suggestive, entangled with hazard-vs-prohibition operationalization). band[4–8]/3B/single-batch/1-seed/fictional-hazard. Pre-reg `docs/R15_CONSTRAINT_PROBE_PREREG.md`; runner `experiments/track_c/r15_constraint_probe.py`; result `results/r15_constraint_probe.json`. NOT a promoted node.

- **R9: DONE = CHARACTERIZATION (NOT promoted, NOT a falsification) (`25_R9_DELETION_RESIDUE`, D-R9-1).** **D-R9-1** (R9 in-weight deletion residue; `CORPUS/25`, 2026-06-24): **CHARACTERIZATION, NOT falsification** (label pre-committed — spec read: §11.2/D42 makes Git/`.vindex` authoritative for every class, **NO class is weights-authoritative**; no delete-time must-not-fire clause → resurfacing CONFIRMS the overlay architecture, R2-class). Corrective canonical-only delete (toward pre-write top-1, NOT snapshot-revert) **suppresses a freshly-written localized edit even on untouched held-out paraphrases — RESIDUE 0/7, code rank 0→10³–10⁴** (write generalized 19/24; delete-took 12/12). **But this is the EASIEST deletion case** (inverting a localized self-made edit): (a) FLOOR result — native/distributed-knowledge redaction UNTESTED + expected hard; (b) mechanism = **overwrite-toward-generic** (top-5 filler), not clean removal; (c) **2/12 undeleted bystanders lost paraphrase-retrievability = the G6.1 corruption signature**. Composes with R15: localized FFN edits are easy to REMOVE but hard to make adversarially ROBUST. **Delete-time-L2 spec-gap stands. STRUCTURAL: no class is weights-authoritative (§11.2).** band[4–8]/3B/1-seed/n_informative=7. Pre-reg `docs/R9_DELETION_RESIDUE_PREREG.md`; runner `experiments/track_c/r9_deletion_residue.py`; result `results/r9_deletion_residue.json`. NOT promoted.

## ⭐ 2026-06-25 — C1-(a) compaction K-vs-C interaction (D-C1KVC-1)
- **D-C1KVC-1** (C1-(a) compaction K-vs-C; `CORPUS/26`, 2026-06-25): compaction corruption = **non-additive SCALE×SUB-BATCHING interaction** — concentration-alone (N100 single solve 98.2%) & sub-batching-alone (N50/5ch 98.0%) BOTH clean; only the interaction corrupts (N100/10ch 73.2%, −25pp, p≤0.001). Single-factor-ONLY models refuted by EQUAL-LEVEL contrasts; diff-in-diff (N-eff −3.8pp@C50 vs −24.8pp@C10). Unifies D1+D20; control-dissociation=G6.1 (only cross-entity bystanders corrupt). ⚠ does NOT break literal size-vs-count (count=N/C reciprocal) nor pin functional form; **pressures B3N condition 3, not falsified**. Advisor+Perplexity-Sonar cross-family CONVERGED (codex auth-expired). band[4-8]/3B/N≤100/country/8-ord/1-held-out. Pre-reg `docs/C1_KVC_PREREG.md`; runner `experiments/track_d/c1_kvc_grid.py`; results `results/c1_kvc_grid_result.json` + `results/c1_kvc_stats.json`. NOT promoted; true-scale stimulus-gated (tier b).

## ⭐ 2026-06-25 — C1 true-scale substrate diagnostic (D-C1TS-1)
- **D-C1TS-1** (C1 true-scale substrate-feasibility DIAGNOSTIC; `docs/C1_TRUESCALE_SUBSTRATE_DIAGNOSTIC.md`, 2026-06-25): **DIAGNOSTIC — NOT a corruption datapoint, NOT promoted.** Operator-greenlit true-scale C1 attempt (B3N cond-3). Screen found 2,631 native-known `city→country` single-token facts (looked unblocked), but pilot + cheap diagnostics found the substrate **non-viable on two editability walls**: (1) **multi-token city subjects** (2,516/2,631) → key-collinear → ill-conditioned AlphaEdit solve → **ΔW blow-up (8900 vs 207), model emits garbage 'all-!'** (the N=2,000 pilot collapse is **weight-destruction, NOT compaction corruption** — single-token-subject N=50/100 stay clean: held-out 100%/86.7%, ΔW 207/294); (2) **strong-prior counterfactual under-expression**: even clean single-token subjects express only **27–42%** (R5/R13 counterfactual-over-prior fragility) < the ≥85% gate. → an edit-corruption substrate needs single-token-value + clean(≤2-tok)-subject + EXPRESSIBLE-counterfactual + cardinality≥2,000 + native SIMULTANEOUSLY; clean-subject single-token relations cap ~78–118. **True-2,000-scale C1 with real native knowledge is NOT runnable with this recipe/substrate → the 78-entity country pool (C1-(a), N≤100) stays the clean ceiling; C1 is now characterized AND bounded (substrate-ceiling'd).** Cost anchor: N=2,000 single-solve = 62 min, 8.4 GB VRAM. **Pilot-first + cheap-diagnostic discipline (advisor-steered) prevented a 4–10-day wasted grid.** Reinforces D-B3N-1 (unstable counterfactual-at-scale = the incremental path, already side-store-routed). Folded into `docs/F1_DETERMINATION.md` C1. Artifacts: `results/c1_scale_city_country_screen.json`, `results/c1_truescale_substrate_diagnostic.json`.

## ⭐ 2026-06-25 — C5 compaction-verify audit (D-C5-1)
- **D-C5-1** (C5 compaction-verify audit; `docs/C5_COMPACTION_VERIFY_AUDIT.md`, 2026-06-25): **ANALYSIS (spec-read + CPU sampling-power) — corrects a pushed over-claim; NOT an experiment.** The C5 mount-verify governance fork. **CORRECTION:** post-compaction verification is **spec-MANDATED**, not the gap the R1-bit/R10 annotations claimed — §11.14/D66/C-OC3: a `CompactionProbeReport` to the TC **before Phase-2 commit**, **100% CORE / CORE=1.0-or-abort**, atomic 2PC; a CORE-diverged recompile never goes live (retracted across all artifacts). **Findings:** (A) C-OC3 by design tolerates non-CORE `behavior_fail` (≤5% SUPPORTING / ≤20% INCIDENTAL — a named §8.9/D19/D30 mode) that **nothing surfaces at read** (R1-bit's bit is tier-blind → the R13 L1/L2 split reappears post-compaction, un-flagged; D43 is transactional-isolation not fidelity, weights lossy per D42). (B) the non-CORE sampling (20%/floor-5, 10%/floor-3) has a **quantified detection-power deficit** near thresholds (exact hypergeometric; S=100/true-10% = 36% false-negative; floor-dominated; reliable only at large strata / large breach); **CORE fully protected**. **Prediction (NOT concluded → C1-true-scale / B3N cond-3):** CORE=1.0 vs sub-batched-compaction at the 8000-edge HARD-drift livelock (multiply untested; engine single-solve-vs-sub-batch open; committed→CORE + N100→8000 double-extrapolation). Recs (operator-gated): surface the C-OC3 tolerance at read + a tier/freshness-marked bit + lift small-stratum floors. F1 net readiness UNCHANGED; C5 stays prototyped-not-empirical. Artifacts: `results/c5_compaction_probe_power.json`.

## ⭐ 2026-06-26 — C10 RESIDUAL-TEST: project-coined values — RESIDUAL FRAGILE (D-C10b-residual)
- **D-C10b-residual** (C10 RESIDUAL-TEST, Option B; `CORPUS/36`, 2026-06-26): operator-chosen sizing of the real in-weight C10 exposure. 7-arm novel-insert (same 24 fictional subjects, request-count-matched; binding = held-out-paraphrase **FULL-SEQUENCE**): single **98.6%** & coherent-bigram **98.6%** robust; **realistic project-coined multi-word SEMANTIC values = 19–31% held-out-paraphrase full-seq** (RANGE [A7 coined-coined 19.4%, A6 coined+real-noun 30.6%]) — **≪ 85% gate, below the 2-tok floor (45.8%) AND the original C10 floor (36%)** because they tokenize LONG (4–5.5 tok). **C10 CONDITION CONFIRMED-and-WORSENED for the real value class.** Two precise claims (advisor + cross-family calibrated): (1) condition confirmed/extended; (2) dominant MECHANISM = a DISTINCT multi-token **length-realization wall**, NOT C10's fit-then-fail-to-generalize parrot — realistic arms fail to even FIT the canonical prompt (canon_full 37–46% vs floor 95.8%). **z-probe (advisor cheap-check) localizes it:** `compute_z` reaches **0.99 target-prob for ALL value classes, converged by step 25 (25≈100)** → NOT a *compute_z* knob (the z-target is achievable at 0.99); the bottleneck RELOCATES to W-realization (the linear W-edit can't install the long continuation) — but the W-realization knobs (band width / layer count / edit strength) are UNTESTED → option A (AnyEdit) is mechanistically CONSISTENT with the failure, NOT proven NECESSARY; the cheap resolver to run BEFORE a multi-day port = re-run one realistic arm with a wider/later band or more layers and see if canon_full climbs. Option A (AnyEdit) over C remains operator-gated. Sub-finding (the pre-registered question's clean answer, length-isolated via teacher-forced per-tok): continuation prior-coherence is mildly PROTECTIVE (realistic 60–72% vs incoherent floor 46%) but overwhelmed by length. Cross-family (Perplexity) on interpretation → toned down 'storage wall'→'W-realization limit', 'nothing edits continuation'→'only indirectly, no direct objective'; full-seq EM = strict DB-style read bar, reported alongside first-token/teacher-forced/P(full|first). **F1 net readiness UNCHANGED** (sizes an existing open condition). Scope 3B/band[4-8]/AlphaEdit/N≤24/1-seed/HF-fp16; NOT promotable. Pre-reg `docs/C10_RESIDUAL_TEST_PREREG.md`; runners `experiments/track_c/{c10d_residual_coined,c10d_zprobe}.py`; results `results/{c10d_residual_coined,c10d_zprobe}.json`.

## ⭐ 2026-06-25 — C10 multi-token VALUE expression: FALSIFIER FIRED (D-C10-1)
- **D-C10-1** (C10 multi-token VALUE expression; `CORPUS/35`, 2026-06-25): **FALSIFIER FIRED** (scoped, NOT a universal law, NOT promotable). First on-target leg (target=`local Intel CPU + batch writes`). 3-arm novel-insert at N≤100 batch path (binding = held-out-paraphrase full-seq): single **97.2%** & prior-coherent multi **97.2%** robust, arbitrary **incoherent** multi **36.1%** (first 70.8%; **P(full|first)=0.51 vs coherent 1.00**). Edit **FITS** trained prompt (95.8%) but doesn't **GENERALIZE** → multi-token instance of R13/R5 trained-prompt-parrot. Coherent positive control + fuller LAW#5 gate + base 0/24 close confounds; Run1 counterfactual floored (unrepresentative). **→ C10 = OPEN MUST-FIX for the target (first on-critical-path AND failing); fix=AnyEdit per-token.** 3 advisor gates + Perplexity cross-family. Scope 3B/band[4-8]/AlphaEdit/N≤100/1-seed/HF-fp16; cities=proxy-not-code. NOT promoted. Pre-reg `docs/C10_MULTI_TOKEN_VALUE_PREREG.md`; runners `experiments/track_c/{c10_multitoken_value,c10b_novel_multitoken}.py`; results `results/{c10_multitoken_value,c10b_novel_multitoken}.json`.

## ⭐ 2026-06-25 — C6 ledger-immutability red-team: property demo + spec finding (D-C6L-1)
- **D-C6L-1** (C6 ledger-immutability red-team; `CORPUS/34`, 2026-06-25): **PROPERTY DEMO + SPEC-LEVEL SECURITY FINDING — NOT an empirical red-team, NOT a spec falsification, NOT promotable.** First C6 move. Demo vs the real G2 `verify_chain`: K1 rewrite-recompute (suppresses the W3 emergency entry) + K2 truncation **UNDETECTED**; K4 naive-edit **DETECTED** (control); K5 CT-style **Signed Tree Head** fix (offline-signed `(seq,tip)`, custody-compatible §20.2) detects both. **Finding (2 claims):** (A, threat-model-independent) spec mandates NO operational-window crypto tamper-evidence (keyless chain §16.1; external anchors only root §13.2 + close §16.7/§27) → spec-level property (elevates CORPUS/11 caveat); (B, threat-model-scoped) §16.5 (Orchestrator fs-write) unreconciled with §16.2 (Orchestrator compromise in-scope; §5 physical-only) → W3 entry rewritable = **unreconciled threat-model seam**. **⚠ Advisor corrected a first-pass "self-undermining gap" over-claim → name-the-covering-clause framing** ([[spec-coherence-audit-is-circular]] #4). Vectors cross-checked vs Perplexity/CT; A3-fork/timestamp/2nd-preimage surveyed-excluded (single-machine v1.2). Moves **C6 not-red-teamed → red-teamed (1 mechanism)**; C6 stays open. F1 readiness UNCHANGED. NOT promoted. Pre-reg `docs/C6_LEDGER_IMMUTABILITY_REDTEAM_PREREG.md`; runner `experiments/governance/c6_ledger_immutability_redteam.py`; result `results/c6_ledger_immutability_redteam.json`.

## ⭐ 2026-06-25 — R11 medium/severity ON READ: spec-coherence audit (D-R11-1)
- **D-R11-1** (R11 content-scoped authoritative-medium & severity ON READ; `CORPUS/33`, 2026-06-25): **SPEC-COHERENCE AUDIT — NOT a falsifier** (matrix ③, medium-delegated per §11.2/D42), CPU-only, NOT promotable. First BUILD-path leg after the FRONTIER INFLECTION (C2 read-contract). **Verdict: COHERENT via derivation + prevention** — (a) "wrong medium wins on read" foreclosed by §11.3/D43 strong-consistency prevention + §11.8 system-wide trip; (b) content-class = f(entity_type) (§11.2) and severity = the SAME §11.7 class-function → both **DERIVABLE-IF-TYPE-RETURNED** (§7.2/C4 mandates typed entities). **⚠ Advisor corrected a first-pass 3-gap over-claim** (the runner silently assumed "reads don't return type"): the read-RETURN shape is itself unspecified (no formal query-language section) → R11 **restates the ONE already-recorded root gap**, NOT new F1 conditions → don't double-count. Honesty: output = documentation echo of the spec read, not an independent check. Spec rec: pin the query-result schema to return `entity_type`; optionally extend §26.3's read-quality-flag beyond L4-external. F1 readiness UNCHANGED. NOT promoted. Pre-reg `docs/R11_MEDIUM_ON_READ_PREREG.md`; runner `experiments/track_e/r11_medium_on_read.py`; result `results/r11_medium_on_read.json`.

## ⭐ 2026-06-25 — R1-bit SELECT read-back DELIVERED-FOR-SCOPE commit-time (D-R1-2)
- **D-R1-2** (R1-bit commit-bit SELECT; `CORPUS/32`, 2026-06-25): **CP-class CONSTRUCTIVE DELIVERY — DELIVERED-FOR-SCOPE (commit-time)**, NOT a falsifier. Fixes R1's bleed-unsound reconciliation (D-R1-1): replaces the in-weight logprob storage-signature with a hash-chained G1 State Ledger whose per-(entity,relation,target) **commit-status bit is set only on a successful 2PC commit** — SELECT reads the bit, never the weights → **bleed-immune by construction**. D1 LANDED read-back 8/8 · D2 LEAK NULL 6/6 (model fires 5/6) · D3 GATE-REJECTED NULL 4/4 + **DROPPED NULL 2/2 via 2PC-abort (`VINDEX_FAILED`)** · D4 **bleed-immunity: Velloria** — the logprob proxy would return a phantom TRIPLE (sig 2.06≥2.0) but the **commit-bit correctly returns NULL** (no COMMITTED entry) · D5 ledger hash-chain intact, no PREPARED-bypass. **Scope = commit-TIME consistency ONLY**; post-commit divergence (compaction/quant/corruption drift, §11.3/D43/R10) **DEFERRED to C1 (operator-scope-gated), NOT solved** → R1 status = 'L1 read-back delivered for commit-time; post-commit divergence open' (NOT 'R1 delivered'). Free scope evidence: committed-triple expression 94–100% across all D20/C1 sub-batched-compaction cells (held-out is what corrupts). Re-assembles proven G1/CP1 parts; labeled CP-class like CP1/G3 (not falsification). band[4-8]/3B/N=8/1-seed/capital/fictional/CPU-governance. NOT promoted. Pre-reg `docs/R1_COMMIT_BIT_SELECT_PREREG.md`; runner `experiments/track_e/r1_commit_bit_select.py`; result `results/r1_commit_bit_select.json`.

## ⭐ 2026-06-25 — R1 SELECT read-back NOT delivered (D-R1-1)
- **D-R1-1** (R1 SELECT triple-readback; `CORPUS/31`, 2026-06-25): **CHARACTERIZATION / NOT-DELIVERED** (frozen pre-reg `hard_fail=True`; mechanical label `FAIL_TAUTOLOGY` corrected — anti-firing held, so it is NOT intent/firing-echo). First build of the operator-chosen #1 frontier (INDEX/QUERY LAYER, load-bearing per §11.2/R2/R6/R9). Ledger-backed SELECT reconciled vs the deployed store: **abstains correctly on never-stored facts** — LEAK 6/6 NULL while the model *fires* the value 5/6 (≠greedy top-1), GATE-REJECTED 4/4 NULL, LANDED read-back 8/8. **But the NOVEL divergence test FAILED: DROPPED (ledger-claimed, store-not-persisted) flagged 1/2** — a phantom read `SELECT→(Velloria,capital,Tokyo)` (never written, doesn't fire) because cross-entity bleed from the 8 batch edits lifted common token 'Tokyo' to **+2.06 nats**, 0.06 over the frozen STORE_THRESH=2.0 (LANDED sigs 7.9–10.75 vs DROPPED 0.18/2.06). The **in-weight logprob storage-signature is UNSOUND — bleed-noise-limited not mistuned** (~50pp run-to-run swing [[sequential-edit-run-nondeterminism]] forecloses threshold-tuning). → R1 read-back **delegated-but-NOT-delivered**; **confirms B0/§11.2** (commit-status can't be inferred from weights → the ledger/`.vindex` medium must carry it). **Post-commit note (CORRECTED 2026-06-25, D-C5-1):** in-weight proxy bleed-unsound; a 2PC commit-bit (G1) is bleed-immune but covers **commit-time only** (no post-compaction freshness). The spec's post-compaction verify is **MANDATED, not unspecified** (§11.14/C-OC3, CORE=1.0 abort) — see `docs/C5_COMPACTION_VERIFY_AUDIT.md`. R13 split half-shown (fired-not-stored only; stored-not-fired 0 obs). GATE-REJECTED 4/4 only re-tests the proven G3 validator, not a new read result. band[4-8]/3B/N=8/1-seed/capital/fictional. Advisor done-gate done (corrected the mislabel + the 'peripheral proxy' reframe). NOT promoted. Pre-reg `docs/R1_SELECT_READBACK_PREREG.md`; runner `experiments/track_e/r1_select_readback.py`; result `results/r1_select_readback.json`.

## ⭐ 2026-06-25 — R2 reverse-lookup write-only (D-R2-1)
- **D-R2-1** (R2 reverse-lookup; `CORPUS/27`, 2026-06-25): **CHARACTERIZATION** (pre-committed; D16 auto-reverse + §11.2 → cannot falsify). A forward in-weight MEMIT edit (C→X, subject-keyed) creates **ZERO reverse-readable edge** — forward-took 24/24, native-reverse positive control 8/10 (template valid), reverse base 0/24 → post 0/24, **max |ΔP(country)|=0.0003 across all 24** (nothing moved). Edits are **write-only/forward-only** (reversal curse, complete). → D4 'no write-only edges' unsatisfiable weight-native; reverse-lookup index/`.vindex`-delegated (D16), §11.2 confirmed. **Bounds read contract for B3N: weights carry FORWARD native-knowing (100%) but ZERO reverse → reverse/bidirectional leg entirely side-store-delegated.** Composes with R9/R6 (storage/retrieval read legs index-owned; only behavioral firing R5/R15 weights-owned). band[4-8]/3B/N=24/capital↔country/1-seed. Advisor done-gate PASS; cross-family WAIVED (confirmatory, codex auth-expired). NOT promoted. Pre-reg `docs/R2_REVERSE_LOOKUP_PREREG.md`; runner `experiments/track_c/r2_reverse_lookup.py`; result `results/r2_reverse_lookup.json`.

## ⭐ 2026-06-25 — R13 paraphrase-generalization (D-R13-1)
- **D-R13-1** (R13 paraphrase-generalization / editing-overfit; `CORPUS/28`, 2026-06-25): **CHARACTERIZATION** — relabeled (NOT the spec storage-vs-behavior split; both probes behavioral, true L1-SELECT index-delegated per CP2). Counterfactual capital edits fire **100% on the trained prompt but mean ~22% across 3 natural paraphrases** (P1 8%/P2 17%/P3 42%; all-3-fail 54%; any 46%) → trained-prompt firing **over-reports usable knowledge**. BOUND (consistent-with, NOT isolated; domain/protocol confounds): editing AGAINST dense native priors generalizes far worse than fictional (R9 79% vs R13 22%); mechanism = narrow trained-template attractor FAILS to override native attractor (revert-to-native diagnostic UNLOGGED). CONSEQUENCE (narrowed): counterfactual overwrites of strong priors fragile to paraphrase at small N → qualifies B3N 'weights carry forward native-knowing' (true on trained phrasing; degrades vs priors); NOT generalized to native-knowing broadly (neutral/novel + recipes UNTESTED). Legitimate §8.9 point: L2 probe MUST use held-out paraphrases. band[4-8]/3B/N=24/capital/1-seed. Advisor + Perplexity-Sonar cross-family CONVERGED. NOT promoted. Pre-reg `docs/R13_STORAGE_BEHAVIOR_SPLIT_PREREG.md`; runner `experiments/track_c/r13_storage_behavior_split.py`; result `results/r13_storage_behavior_split.json`.

## ⭐ 2026-06-25 — R5 native-knowing paraphrase-robustness (D-R5-1)
- **D-R5-1** (R5 native-knowing paraphrase-robustness; `CORPUS/29`, 2026-06-25): **CHARACTERIZATION** — in-weight editing CAN produce USABLE paraphrase-robust knowledge, COMPETITOR-SPECIFICALLY. **NOVEL facts robust (16/16 all-3-hit, 100% mean); counterfactual-over-prior fragile (0/16 all-hit, 25% mean), 75% of failures revert to TRUE fact** (R13 mechanism confirmed + isolated within-protocol; competitor Fisher p≈0). Paraphrase-DIVERSE recipe partially rescues (25→65% mean, 13/0 sign p≈0.0002; full-robust only 3/16) — DIVERSITY not intensity (intensity-control 31%; isolated steps underpowered N=16, directional). **F1: INSERT path (novel+thin project facts) materially IMPROVED/robust; UPDATE-vs-pretraining fragile-but-partially-rescuable; update-over-prior-EDIT a SEPARATE UNMEASURED cell** (may localize=easier OR entangle=harder; not assumed between). B3N two-sided. Recipe amendment=CANDIDATE pending powered run. Advisor+Perplexity cross-family CONVERGED. band[4-8]/3B/N=16/capital/1-seed. NOT promoted. Pre-reg `docs/R5_PARAPHRASE_ROBUSTNESS_PREREG.md`; runner `experiments/track_c/r5_paraphrase_robustness.py`; results `results/r5_paraphrase_robustness.json`+`r5_stats.json`.

## ⭐ 2026-06-25 — R5b overwrite-prior-edit (D-R5b-1)
- **D-R5b-1** (R5b overwrite-prior-edit; `CORPUS/30`, 2026-06-25): **CHARACTERIZATION** — discriminating axis is **PRETRAINED-prior presence, NOT prior-EDIT presence** (advisor-corrected). Re-editing a NO-pretrained-prior (fictional) fact stays paraphrase-robust: OVERWRITE-EDIT 93.8% (15/16 all-hit) ≈ NOVEL 97.9% (p=1.0) ≫ PRIOR 14.6% (0/16, p<1e-6); prior edit v1 does NOT resurface (0/3). 2×2: no-prior×{1,2}edits robust; pretrained-prior×1edit fragile; **pretrained-prior×2edit (real-subject re-edit) UNMEASURED, predicted ≈PRIOR**. F1 CONDITION (not assumption): in-weight native-knowing robust IFF subjects lack an entrenched pretrained competitor (code/structural→yes; common-knowledge domain_concepts→no). Advisor done-gate PASS; cross-family WAIVED (decisive ordinal). band[4-8]/3B/N=16/1-seed. NOT promoted. Pre-reg `docs/R5B_OVERWRITE_PRIOR_EDIT_PREREG.md`; runner `experiments/track_c/r5b_overwrite_prior_edit.py`; results `results/r5b_overwrite_prior_edit.json`+`r5b_stats.json`.
