# Read / Query Contract Matrix (spec-first extraction — Database-Behavioral-Contract: READ slice)

**Date:** 2026-06-22. **Method:** spec-first, sections read end-to-end: §7.1–7.8 (schema/query surface), §8.5 (`.larql` query ops), §8.9 (post-write probes), §11.2–11.3 (read consistency / authoritative medium), §21 (L2 probe family + Semantic Assertion Criterion), §26 (provenance). Per COUNCIL_PROTOCOL: clauses quoted/cited, not paraphrased from memory. **Columns:** Evidence-type (§1 of `F1_COMPREHENSIVE_RUNBOOK.md`) · Medium-of-Obligation (anti-scope-laundering) · "what makes this FAIL" · Coverage. This is the first slice of the comprehensive matrix (read/query first, per the next-step decision); other layers follow.

**Medium tags:** `WEIGHTS` = weights must carry · `VINDEX/GIT` = side-medium may carry · `GOV` = governance/ledger may enforce · `HYBRID` = weights + external · `OOS` = out of F1 scope.

| # | Read requirement (spec clause) | Evidence-type | Medium | What makes it FAIL | Coverage |
|---|---|---|---|---|---|
| **R1** | **Exact SELECT read-back of a committed edge** (§8.9 L1: "`SELECT` read-back confirms the edge was written"; §8.5 SELECT op) | constructive + empirical-falsifier | **HYBRID** (a `SELECT` may read an external index over `.larql`, not the weights) | no `SELECT` primitive returns a written triple as a structured value | ⚠ **DELIVERY ATTEMPTED → NOT-DELIVERED (R1, CORPUS/31, D-R1-1, 2026-06-25).** Built a ledger-backed SELECT reconciled vs the deployed store. **Abstain side SOLID** (anti-firing: LEAK 6/6 NULL while the model *fires* 5/6; GATE-REJECTED 4/4 NULL; LANDED read-back 8/8). **But the confirm-only-what-persisted side FAILED** (DROPPED divergence 1/2 — phantom read `Velloria,capital,Tokyo` from +2.06-nat cross-entity bleed over the frozen STORE_THRESH=2.0). The in-weight logprob storage-signature is **bleed-UNSOUND** (~50pp run-to-run swing — not threshold-tunable). → R1 **delegated-but-not-delivered**; confirms B0/§11.2 (ledger medium must carry commit-status). **NEW gap: post-commit divergence-detection unspecified; a 2PC commit-bit covers commit-time ONLY.** <br>✅ **COMMIT-TIME DELIVERED (R1-bit, CORPUS/32, D-R1-2, 2026-06-25):** a G1 2PC **commit-status bit** (per-(e,r,o), SELECT reads the bit not the weights) delivers the §8.9 L1 read-back **bleed-immune** — D1 8/8, D2 LEAK NULL 6/6, D3 REJECTED 4/4 + **DROPPED NULL 2/2 via 2PC-abort**, D4 Velloria proxy-TRIPLE(2.06)→**bit-NULL** (the R1 phantom read fixed), D5 chain intact. **Status: L1 read-back delivered for COMMIT-TIME; post-commit divergence (R10/§11.3/D43) OPEN → C1.** |
| **R2** | **Reverse lookup / bidirectional traversal — every edge reverse-lookable, no write-only edges** (D4 §7.6); `defined_in` auto-reverse (D16) | empirical-falsifier (the reversal-curse can break it) | **HYBRID** (auto-reverse may be an index, not weight-native) | a forward-declared edge is not retrievable in reverse (target→entity) | ❌ **UNTESTED** — sharp falsifier (LLM reversal-curse) |
| **R3** | **Relation-family queries — all 5 families representable + queryable** (D6 §7.3: Structural/Knowledge/Constraint/Taxonomy/Namespace) | constructive | **WEIGHTS / HYBRID** | a family cannot be expressed or queried | ⏩ **RESOLVED-BY-SCREENING (2026-06-25, `results/r3_family_screen.json`).** 4/5 families have clean single-token confident (s,r,o) factual-completion bases on Qwen2.5-3B (Knowledge/capital, Taxonomy/is_a, Namespace/belongs_to, Structural/defined_in all 4/4; Constraint/requires 3/4 noisier) → they **reduce to the SAME MEMIT edit op as Knowledge** → expression predicted; full run NOT done (constructive tautology). Cross-family corruption study skipped: family is 1-to-1 with cardinality (the C1 `count=N/C` confound) AND cardinality→corruption is already **D1** within-family + **Constraint firing is R15**. Cell COVERED by screening + D1 + R15. |
| **R4** | **Knowledge-namespace scoped reads** (domain vs project, `scoped_to`; §7.4) | constructive | **GOV / VINDEX** (scoping is metadata, likely tier/index) | a query cannot isolate domain vs project namespace | ❌ UNTESTED |
| **R5** | **Native-knowing / reason-over-fact — the fact FIRES in inference** (§8.9 L2; the paradigm §1: agents "natively know" without prompt reconstruction) | empirical-falsifier (reliability) | **WEIGHTS** (the paradigm payoff) | committed CORE facts don't fire on read; or don't survive scale/quantization (margin dilution) | ⚠ **PARTIAL** — INFER read-back works at clean small-N (A1/B0 committed 8/8); **reliability at scale + Q4_K_M is OPEN → routed to the compaction-at-scale run as a seam metric** |
| **R6** | **Negative reads / closed-world — absent key returns null, not confabulation** (implied by "database"; not a stated weight obligation) | empirical-falsifier | **GOV** (commit-status is ledger-delegated, NOT weight-native) | n/a as a *weight* falsifier — see disposition | ✅ **DISPOSITIONED (B0, 2026-06-22):** weights carry NO commit-status bit (leak: uncommitted-known facts read correct 6/6@0.43–0.82) — but the spec **delegates** commit-status/provenance to the State Ledger + `.vindex` + §26, so this **confirms** the architecture. **Spec GAP: read-time provenance/closed-world surface is under-specified.** `results/B0_RESULT_ANALYSIS.md` |
| **R7** | **Aggregation / negation reads** ("how many X", "which lack R") | constructive | **HYBRID** | the read surface cannot aggregate/negate over the store | ❌ UNTESTED — **spec does not explicitly mandate these → under-specified (flag for F1)** |
| **R8** | **Multi-hop / compositionality** (A→B, B→C ⇒ A→C) | empirical-falsifier (mis-answer risk) | **n/a** | a *confabulated* transitive edge scored as a PASS | ❌ UNTESTED — ⚠ **spec mandates NO transitive closure (no clause); a "correct" transitive read may be confabulation → tautology trap if mis-scored.** Nice-to-have at most |
| **R9** | **DELETE FROM EDGES — true deletion / tombstone; deletion non-resurfacing** (§8.5; §7.8 deletion authority) | ~~empirical-falsifier~~ → **characterization** (medium-delegated) | ~~WEIGHTS + GOV~~ → **GOV/overlay-authoritative, weights-derived** | a deleted fact resurfaces under paraphrase/composition | ⚠ **RE-TAGGED 2026-06-24 (spec read): NOT a weights-owned falsifier.** §11.2/D42 makes **Git/`.vindex` the authoritative medium for every content class — NO class is weights-authoritative** (grep: zero "weights authoritative"); and there is **no delete-time "must-not-fire"/closed-world/erasure clause** (zero hits). So a secret resurfacing in raw weights *confirms* deletion lives in the overlay/tombstone layer (R2-class), it does not falsify. Run as **characterization** (pre-committed label): does in-weight corrective-delete leave residue → empirically grounds §11.2 (weights non-authoritative for deletion) + flags the **delete-time-L2 spec-gap**. Program showed only T2.1 snapshot-revert (bit-identical un-write), NOT corrective-delete residue. |
| **R10** | **Read consistency — strong; reads block during `.vindex` mount; no stale-read fallback** (D43 §11.3) | fault-injection | **GOV / State** (enforced at the mount boundary, not in weights) | a stale read is served during the mount window | ❌ UNTESTED — Phase-2 protocol; **capture-now seam: mount-window read-block duration**. <br>⚠ **SHARPENED 2026-06-25 (R1-bit/D-R1-2 + C1 cross-ref, no new run):** a commit-TIME bit (R1-bit) **false-certifies the committed triples that compaction diverges** — C1 measured committed-triple expression **94.8% at N100×10ch** (≈5% served-vs-ledger divergence) while the bit, set at commit and never updated, still returns those triples. → **strong read-consistency CANNOT rest on the commit-bit; compaction output must be re-verified against the ledger at mount.** The spec already has the mount-time hook (reads block + re-compile verified, D43) — so the real open question is **whether compaction's output re-passes the Gate/verify before becoming the active store = a GOVERNANCE question (C5), not a track-e read probe.** |
| **R11** | **Content-scoped authoritative medium on read** (Git auth for structural; `.vindex` auth for L4 `domain_concept`+`constraint_rule`; D42 §11.2) | constructive + fault-injection | **GOV** | wrong medium wins on a read divergence; failure-severity not class-differentiated | ❌ UNTESTED — **dual-medium content-class severity seam** (read-error on structural = Git-recoverable; on L4 = data loss) |
| **R12** | **Schema-hygiene rejection at the read/write boundary** — `violates` hard-rejected (C6/C9); undeclared relation rejected pre-MEMIT (C5); untyped rejected (C4); `target` reserved (C3); 5-type cap (C1) | fault-injection (submit-bad → assert-reject) | **GOV** (Validator/Gate, pre-MEMIT) | a malformed/undeclared patch is accepted | ⚠ partial — G3 tested C5/C6 schema-rejection (constructive); the rest UNTESTED. Cheap, run early |
| **R13** | **storage-pass / behavior-fail are DISTINCT probes** (§8.9: L1 SELECT-readable vs L2 fires-in-inference; never collapsed) | empirical-falsifier (measure the base rate) | mixed (L1=HYBRID, L2=WEIGHTS) | the read suite conflates "SELECT-readable" with "fires" → reports verified when only one holds | ⚠ **HALF-SHOWN (R1, CORPUS/31, 2026-06-25).** L1(SELECT)×L2(firing) contingency reported with both columns separate; the **fired-not-stored** cell is demonstrated (LEAK 5 facts: model fires, SELECT NULL) but the **stored-not-fired** cell has **0 observations** (clean fictional inserts fire robustly per R5). The split is real in one direction; the other awaits a margin-diluted/counterfactual stimulus. |
| **R14** | **Evaluation oracle — method-typed grading** (§21.4: `exact_substring` / `structured_field_match` / `judge_model_classification`, judge **frozen at template commit**) | (discipline, not an experiment) | **GOV** | reads graded by a free/unfrozen judge → "pass" silently drifts (tautology vector) | ❌ not operationalized — **bind per-probe-class before any read run; ε calibration §22 untouched** |
| **R15** | **L2 Constraint probe — adversarial "must not Y" → response refuses/flags** (§21.2) | empirical-falsifier (adversarial) | **WEIGHTS** | an edited prohibition does not fire as a refusal under an adversarial prompt | ⚠ **TESTED → not-ready-with-conditions (CORPUS/24, D-R15-1, 2026-06-24).** v1 subject-property hazard edits: perfect cooperative firing (Tier-0 24/24, paraphrase 24/24) but **adversarial firing ~½** (hand-adj ~11–14/24, ~7/24 silent compliance-leaks); 2/24 explicit refusals. Controls clean (global-shift 0/4; neutral property-specificity color/price 1/24). **No delegation route** — WEIGHTS-owned. Bounded: easiest single-entity case; relational archetype expected worse. Spec-gap flag: §21.2 disjunctive pass admits warn-and-comply (Finding 2, suggestive). band[4–8]/3B/single-batch/1-seed |
| **R16** | **Provenance flag visible in query results** for external L4 facts (§26: `provenance_flag ∈ {STALE,UNAVAILABLE}` in query results) | constructive | **GOV / VINDEX** | query results don't surface the provenance/staleness flag | ❌ UNTESTED — Phase-2 (ties to §26 machinery) |

## Headline reading of the read slice
- **What the model/weights actually owe (WEIGHTS):** R5 native-knowing + R15 constraint-firing + the write-half of R9 delete — i.e. *does committed knowledge fire reliably, and can it be removed*. The reliability-at-scale question (R5) is the live falsifier → **folded into compaction-CORE-at-scale** (council #1), not a standalone B1.
- **What the spec DELEGATES off the weights (GOV/HYBRID):** R1 SELECT, R6 closed-world/commit-status, R10 consistency, R11 medium-authority, R14 oracle, R16 provenance. B0 confirmed R6 is governance-delegated — *not a weight falsification.*
- **Sharp UNTESTED falsifiers worth GPU later:** ~~R2 reverse-lookup~~ (moot — medium-agnostic; not WEIGHTS-owned), ~~R9 deletion-non-resurfacing~~ (**RE-TAGGED 2026-06-24: medium-delegated, not WEIGHTS-owned — characterization not falsification; see R9 row**), ~~R15 constraint-probe~~ → **R15 TESTED 2026-06-24 (CORPUS/24, D-R15-1): not-ready-with-conditions.**
- **⭐ STRUCTURAL FINDING (2026-06-24 spec read, §11.2/D42):** **NO content class is weights-authoritative** — Git authoritative for `structural_entity`, `.vindex` authoritative for Layer-4 `domain_concept`/`constraint_rule`; weights carry zero authoritative content. → in-weight storage is **always a derived/serving copy**, never the system-of-record. This **re-scopes the matrix's WEIGHTS tags GLOBALLY** (the original WEIGHTS/WEIGHTS+GOV tags were optimistic — §11.2 resolves every row at once; the per-row tag is DERIVED, not authoritative). **One-pass 3-way re-classification (apply BEFORE choosing the next falsifier):**
  - **① weights-owned BEHAVIORAL + spec-MANDATED → genuine FALSIFIER:** **R5** (fact must fire, §8.9 L2/§1), **R15** (prohibition must fire, §21.2). *(R13's L2-fires half is the same behavioral surface.)*
  - **② weights-owned BEHAVIORAL + spec-UNMANDATED → CHARACTERIZATION + spec-gap** (still worth probing — generates spec-gaps): **R9 resurfacing half** (deleted fact must-not-fire — no delete-time L2 clause), **R6** closed-world (no weight obligation), **R8** transitive (spec mandates none).
  - **③ STORAGE / AUTHORITY → medium-delegated per §11.2, in-weight failure CONFIRMS the overlay (NOT a falsifier):** **R1** SELECT, **R2** reverse, **R3** family-queryability, **R9 authority half**, **R10** consistency, **R11** medium-authority, **R16** provenance, **R14** oracle (GOV).
  Net: the only category-① (genuine weights-owned falsifier) cells are **R5 + R15**; R15 done (CORPUS/24). **Strengthens the scope-keyed-hybrid** ([[in-weight-necessity-is-scope-keyed-hybrid]], D-B3N-1). Remaining category-① open = **R5 reliability-at-scale** (pool-gated) → otherwise the read-contract slice points to **F1 reconciliation**. See [[in-weight-falsifier-must-be-weights-owned]].
- **Spec GAPS flagged for F1 (under-specified read surface):** R6 read-time provenance/closed-world; R7 aggregation/negation; R8 transitive semantics; R13 L1-vs-L2 split. The spec specifies read *requirements* (exact/reverse/traverse) but ships no formal query-language section — the least-specified production surface (confirms the e2e §G note).

## Status
Read/query slice COMPLETE (first pass). B0 folded (R6). Next matrix slices (other layers) follow the same schema. The WEIGHTS-owed reliability falsifier (R5) is queued for the compaction-at-scale GPU cycle (operator-greenlit, pool-gated).
