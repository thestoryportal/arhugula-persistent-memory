# Consolidated Hypothesis Register — 3 deep-thinking passes (2026-06-18)
Synthesis of: Pass 1 (E1-attribution de-confounding H1–H6) + Pass 2 (novel approaches) + Pass 3 (foundational).
Status: COLLAPSED=tested&ruled-out · OPEN=untested · PARTIAL=corroborated-not-proven. Cost/Value are rough.

## A. E1 — LARQL-serving attribution
| # | Hypothesis | Status | Cost | Value |
|---|---|---|---|---|
| A1 | Garbage = Q4_K_M dequant bug, not bias (H1) | COLLAPSED (0.5B non-quant also 0-bias) | — | — |
| A2 | Bias-drop co-incidental; real cause = decompile (H2) | PARTIAL (corr. strong; not causal) | — | — |
| A3 | My vocab edit (→151936) corrupts output (H3) | COLLAPSED (HF config IS 151936) | — | — |
| A4 | --level inference omits vs --level all (H4) | COLLAPSED (level-all 0.5B also 0-bias) | — | — |
| A5 | LARQL globally broken now / wrong path (H5) | COLLAPSED (Qwen3 serves clean now) | — | — |
| A6 | n=1 observation (H6) | COLLAPSED (4 prompts systematic garbage) | — | — |
| A7 | **Bias ablation in HF Qwen2.5-3B** — zero q/k/v bias, probe recall. Garbage→bias causal; "Paris"→attribution WRONG | **OPEN — DECISIVE** | ~5min | **HIGH (can overturn)** |
| A8 | Bias tensor norm vs activation scale — is bias even large enough to be catastrophic? | OPEN | trivial | med |
| A9 | Airtight: safetensors-to-vindex on edited 3B (non-quant, same model) | OPEN (near-redundant) | ~1hr | low |

## B. Serving / deployment architecture
| # | Hypothesis | Status | Cost | Value |
|---|---|---|---|---|
| B1 | E1 should re-decide B1's model: Qwen3 (LARQL-servable) not Qwen2.5-7B (un-servable) | OPEN (decision) | — | HIGH |
| B2 | DB query-layer ≠ LARQL vindex: external triple-index (G3 prototyped) over llama.cpp serve → keeps Qwen2.5 + recovers DB reads + sidesteps bias | OPEN | med | HIGH |
| B3 | **L2 in-weight may be unnecessary** — L1 retrieval (KNN+ROUTE VERIFY, no-code, works) may suffice for exact-lookup DB. Questions the core thesis | **RESOLVED 2026-06-21 (D-B3N-1):** in-weight NOT contractually required (native-knowing = paradigm *preference*, not a tested requirement; side-store meets the enforced read contract given routing). VERDICT = scope-keyed HYBRID — in-weight viable for batch core at scope, side-store for incremental high-churn. `docs/B3_IN_WEIGHT_NECESSITY_DECISION.md` | low (analysis) | HIGH |
| B4 | Governance↔science miscalibration: does drift-detection fire on real G6.1 corruption? (built before corruption characterized; keyed to wrong variable) | OPEN | med | med-HIGH |
| B5 | Hybrid read: in-weight store + L1 ROUTE-VERIFY fallback for corrupted reads — end-to-end accuracy untested | OPEN | med | med |

## C. Editing-method space (beyond MEMIT family)
| # | Hypothesis | Status | Cost | Value |
|---|---|---|---|---|
| C1 | **GRACE/WISE non-interference methods structurally avoid cross-entity corruption** (parked prematurely; CPU-write-friendly; OK for exact-lookup) | OPEN (grace_dry_run exists) | med | HIGH |
| C2 | **Cross-entity corruption is a KEYING artifact** — relation-inclusive subject reformulation (subject_last, NO engine patch) cuts key cosine 0.92-0.93 at source. Corpus-identified, UNRUN | OPEN — cheap root-cause | low | HIGH |
| C3 | **DB-specific write: explicit same-relation neighbor RE-ASSERTION in the solve** (we know the facts; distinct from A2 key-sentinels) — fixes incremental path | OPEN — novel | low-med | HIGH |
| C4 | A3/BetaEdit port (λ1·Σ + τ-P-refresh) — parked on incremental-requirement gate | PARKED | med | cond. |

## D. Capacity / mechanism (the D1 cluster = F1 deliverable)
| # | Hypothesis | Status | Cost | Value |
|---|---|---|---|---|
| D1 | Capacity law: interference = f(relation fan-out × N × model size × band × key-cosine). The core spec-readiness deliverable | **STRUCTURAL DONE+model-general+operator-APPROVED; NUMERIC guardrail SET (D-D1-2 ⟨D-D1-2@e023d8d2⟩: k≤2, dual-reviewed; mixed-load→needs global-volume bound too)**; cross-model transfer OPEN (OQ-W1, 7B via determinism) | high | HIGH |
| D-NOISE | Sequential-edit held-out corruption is ~50pp run-to-run nondeterministic on the IDENTICAL config (7B seed3 20.8→70.8 on re-run; GPU nondeterminism, unverified mechanism) → single-run absolutes unreliable; BLOCKS numeric §8.7 thresholds on this instrument | **RESOLVED (D-D1-2):** 3B within-process SD=0; noise is 7B/across-process + edit-ORDER (intrinsic); lower-variance instrument BUILT (order-clustered bootstrap) → numeric guardrail set | — | HIGH (gates the numeric law) |
| D2 | Mechanistic: WHY batch eliminates corruption — SVD edit-deltas, project on shared-relation direction, null-space occupancy. Convert inference→evidence | OPEN | med | HIGH |
| D3 | Ground capacity in null-space DIMENSION (compute_P reports it) — principled bound, not fitted | OPEN | low-med | med-HIGH |
| D4 | Corruption law is model-SPECIFIC (Phase-1: locality is) — bounds D1 generality | OPEN | high | med |
| D5 | Quantization × cross-entity-corruption INTERACTION — does A1 batch-clean survive Q4_K? (B3 tested edits, not isolation) | OPEN | med | med-HIGH |
| D6 | B1 size-density (Qwen2.5-7B): does A1 batch-clean replicate upward? | DONE-PARTIAL (D-B1-1 ⟨D-B1-1@2ebae54e⟩, CORPUS/19). NB distinct from D-B1-2 ⟨D-B1-2@0db8d819⟩ (D1 concentration law's model-size term, DONE: REPLICATE + threshold unresolved) | — | med |

## E. Evaluation validity
| # | Hypothesis | Status | Cost | Value |
|---|---|---|---|---|
| E1 | Margin confound is program-WIDE (compute_z inflation distorts A1/A2/G6.1 cross-entity + locality metrics, not just B3) | OPEN | med | HIGH |
| E2 | Robustness/calibration: edited fact under sampling/temp/paraphrase; "stored vs barely-expressed" | OPEN | low-med | med-HIGH |
| E3 | Read-contract surface: reverse lookup, aggregation, negation, 5 relation families on READ — the real DB query semantics | OPEN | med | HIGH |
| E4 | Ripple/multi-hop (RippleEdits): France→Oslo, does "Eiffel Tower in ___" stay consistent? | OPEN | med | med-HIGH |

## F. Frontier
| # | Hypothesis | Status | Cost | Value |
|---|---|---|---|---|
| F1 | SAE/superposition decomposition (A6): edit in sparse feature basis to avoid shared-direction leakage no λ_s arrests | OPEN | high | high (if linear fixes plateau) |
| F2 | Temporal durability: drift across SUCCESSIVE batch rebuilds (deployment = periodic rebuild; only 1 batch tested) | OPEN | med | med-HIGH |

## TOP PICKS (cheap × decisive × foundational)
1. **A7** bias ablation — can overturn the E1 attribution in 5 min.
2. **C2** relation-inclusive keying — cheap, unrun, attacks corruption root cause.
3. **B3 + B2** — question whether L2 is needed; if it is, decouple the DB layer from LARQL. Reframes the whole deployment story.
4. **C1/C3** — GRACE and DB-specific neighbor re-assertion: methods that *structurally* avoid the corruption we keep fighting.
5. **D1/D2** — the capacity law + its mechanism: the actual F1 deliverable.

## G. Advisor additions (Pass-4 input — one outside voice, eliminates nothing)
| # | Hypothesis | Status | Cost | Value |
|---|---|---|---|---|
| NEW-1 | Sharpen A7: compare LARQL garbage TOKEN-FOR-TOKEN vs HF-Qwen2.5 bias-zeroed. Match→bias is the WHOLE story; different→second bug (decompile) layered on bias | OPEN — do as part of A7 | ~5min | HIGH |
| NEW-2 | "Wikidata output matching: 0/512 clusters labeled" in convert log = Qwen2.5-specific decompile-degradation signal, independent of bias. Check Qwen3 labeled nonzero | OPEN — cheap | trivial | med-HIGH |
| NEW-3 | correct() prefix-match labeling confound (a==b or startswith) underneath G6.1/A1/B1 cross-entity %. Distinct from margin confound (probability vs labeling rule). Audit exact-match flips | OPEN | low | med-HIGH |
| NEW-4 | Write-type asymmetry: all results use counterfactual REASSIGNMENT (overwrite). DB also INSERTs (no prior competitor) + DELETEs — different interference profiles. DB use-case is insertion-heavy | OPEN | med | med-HIGH |

### Advisor review notes (input, not authority):
- TOP-5 ranking endorsed. After A7, highest-leverage = B3/B2 + C2.
- C1 (GRACE): structurally avoids corruption but MOVES it to serving (GRACE adapters don't serve via llama.cpp/LARQL free) — trades write-problem for serve-integration problem.
- B3 (drop L2) and E3 (read-contract) are the SAME decision twice: L1 retrieval can't support multi-hop "reason OVER the stored fact". Decide B3 against E3.
- SEQUENCING (binding): run A7 + read B1 BEFORE more theorizing — evidence should prune the register first.

## H. External-evidence references (catalog triage, 2026-06-19)
External repos found by reading the content (not stars) of all 130 in [`research_and_specs/llm_research_tools_high_value.md`](../research_and_specs/llm_research_tools_high_value.md). **None is a knowledge-editing implementation** — these are *adjacent* corroboration/tools, keyed to the hypotheses they bear on. **Caveat (`[[verify-external-artifacts-before-effort]]`):** all are external, unvetted, mostly single-author repos — treat as **leads/corroboration**, verify before relying on any number or method.

| Hypothesis | External evidence | Relevance |
|---|---|---|
| **D2** (method only) ~~C2~~ | [skgallagher/ELLMTrees-paper](https://github.com/skgallagher/ELLMTrees-paper) | ⚠️ **VERIFIED — DOWNGRADE** (`research_and_specs/external_evidence_notes.md` EV-1): its "key matrices" = attention **W_k** carrying *fine-tuning lineage* (RF-distance phylogeny), **NOT** our down_proj activation keys. Same word, different object — **not** C2 corroboration. Residual: its RF / permutation-null method may be reusable for D2 (edit-delta structure vs a random-relabel null). |
| **F1** (SAE / superposition) | [Melodiz/llm-research-projects](https://github.com/Melodiz/llm-research-projects) · [ADEL9st/LLM-Mind-Visualizer](https://github.com/ADEL9st/LLM-Mind-Visualizer) | SAE feature-recovery + superposition geometry with **known ground truth**; logit-lens / residual-stream / direction-ablation tooling (GGUF adapters). Method references if we test the sparse-feature-basis fix. |
| **D2** (interference geometry, mechanism) | [sasakimc/semantic-resilience-project](https://github.com/sasakimc/semantic-resilience-project) | Representation-collapse experiments (stance-drift on Qwen2.5, embedding/stance metrics, run-schema) — low-dim "semantic mode" framing. |
| **D5, E1** (quant × corruption; margin confound) | [tanueihorng/llm-ethics-benchmark](https://github.com/tanueihorng/llm-ethics-benchmark) | Matched **fp16→INT8→NF4** pairs + a **refusal_margin** module + repro kit — an independent quantization-survival harness measuring behavior by margins. |
| **E2 (+E1)** (calibration / stored-vs-expressed) | [cscheffler/elicit-model-beliefs](https://github.com/cscheffler/elicit-model-beliefs) · [Pankick/llm-research](https://github.com/Pankick/llm-research) | Belief **stability across paraphrases** vs model size on Qwen2.5; confidence-vs-correctness (**AUROC/ECE**, next-token distribution) on Qwen2.5. Direct methods for the read-robustness/margin question. |
| **F2, D1** (temporal durability / capacity / forgetting) | [vivsn289/Adaptive-Self-rehearsal](https://github.com/vivsn289/Adaptive-Self-rehearsal) · [luisroberto0/project-hebb](https://github.com/luisroberto0/project-hebb) | Catastrophic-forgetting + adaptive rehearsal on **Qwen2.5-3B** (3 seeds, lm-eval, paired SE); continual-learning/plasticity with CI95 + honest negatives + **CPU-latency deployment** analysis. |
| **C1** (non-interference methods) | [luisroberto0/project-hebb](https://github.com/luisroberto0/project-hebb) | Bio-inspired local-plasticity learning = a non-interference paradigm in the GRACE/WISE family of "structurally avoid corruption." |
| **B2, B3, E3** (external query-index vs in-weight) | [Pra0809/xai-knowledge-graph](https://github.com/Pra0809/xai-knowledge-graph) · [Anandesh-Sharma/awesome-agentic-memory](https://github.com/Anandesh-Sharma/awesome-agentic-memory) | ✓ **VERIFIED** (EV-2): xai-knowledge-graph is a real **GraphRAG-vs-vector-RAG** comparison — structured KG query **wins on structural / multi-hop / aggregation reads**, RAG on synthesis (win 6/10). Directional prior that the external index should be a **structured KG, not plain L1-KNN** → input to B2/E3. **SCOPE: n=10, paper-domain, judge-biased — not a transportable number.** awesome-agentic-memory = unvetted lead (external/lifelong memory map). |
| **C4** (BetaEdit port, tooling) | [going-doer/paper2code](https://github.com/going-doer/paper2code) | ICLR-2026 paper→code automation + repro benchmark — could accelerate reimplementing editing-method papers (the parked BetaEdit port). |
| **Methodology / harness exemplars** (cross-cutting; esp. NEW-3 audit discipline) | [whenpoem/aiscientist](https://github.com/whenpoem/aiscientist) · [Zhonghao1995/research-skills](https://github.com/Zhonghao1995/research-skills) · [WhaSukGO/LenaLab](https://github.com/WhaSukGO/LenaLab) · [priyamDalmia/hpc-for-ml-researchers](https://github.com/priyamDalmia/hpc-for-ml-researchers) | Pre-registration + verification harness; tested calibration/uncertainty/repro skills; held-out generator⟂verifier eval gates; reproducible cluster job templates. Reusable for our own experiment discipline. |
| **Discovery feed** | [tmgthb/Autonomous-Agents](https://github.com/tmgthb/Autonomous-Agents) | Daily-updated index of agent/LLM papers — surface new editing/interp/eval work as it appears. |
| **Tooling — autoresearch search loop** (for config-space sub-problems, e.g. C2 band-[8-12]) | [karpathy/autoresearch](https://github.com/karpathy/autoresearch) (canonical loop) · [wjgoarxiv/autoresearch-skill](https://github.com/wjgoarxiv/autoresearch-skill) (installed → `~/.codex/skills/`) · [Orchestra-Research/AI-Research-SKILLs](https://github.com/Orchestra-Research/AI-Research-SKILLs) (98-skill lib) | Autonomous propose→experiment→evaluate→keep/revert loops for **SEARCH** problems (best band/hparams). ⚠️ **OPTIMIZER, NOT falsifier** — Goodhart-prone; outputs are **hypothesis-generation only**. Re-verify any winner with pre-registered criteria + the inertness gate + `advisor()`; **never write a loop winner into `CORPUS/`**. Wired example: `experiments/track_c/autoresearch_band_search/` (honest EXACT-match evaluator + retention/expression guard + frozen held-out). `firecrawl/AI-research-SKILLs` = redundant smaller copy of Orchestra's. |


## I. C2-band fold-in (2026-06-20, `CORPUS/21`, D-C2band-1 ⟨D-C2band-1@c6fb6103⟩)
| # | Hypothesis | Status | Cost | Value |
|---|---|---|---|---|
| C2-band | Editing in low-collinearity band [8-12] reduces SEQUENTIAL cross-entity corruption | **TESTED → REAL-BUT-UNDERPOWERED, NOT PROMOTED** (mechanical PASS +18.73pp cross-JS; real redistribution — within-loc FALL + expr 100% exclude under-editing; underpowered 1 seed, within-entity top-1 cost & mechanism unmeasured) | done | mechanistic (feeds D1) IF de-confounded |
| D7 | **Depth rotates the key basis: relation-clustered (L4-8) → entity-clustered (L8-12).** Predicts same-entity / different-relation key collinearity is inversely-U — HIGHER at L8-12. Explains the cross↑/within↓ trade. builds-on: C2 (CORPUS/20). would-advance: D1 capacity law + C15 tension | OPEN — UNMEASURED (CORPUS/20 measured only same-relation) | low (compute_ks, no edits) | HIGH (decides the C2-band MECHANISM — basis-rotation vs other geometry; feeds D1) |
| C2-band-dc-b | Edited-entity within-attribute TOP-1 measured directly both arms — the VALUE gate: does the within-entity JS perturbation flip real reads, or is the cost distributional-only? | OPEN — gating | low (re-run, log it) | HIGH |
| C2-band-dc-c | Per-band edit-Δ-norm + depth-matched SHAM/null-edit control — removes injection-depth + edit-magnitude confounds from the locality deltas | OPEN | low-med | med-HIGH |
| C2-band-dc-d | ≥3 seeds + ≥2 write orderings on the JS delta — gives the ≥5pp pre-reg threshold a null distribution | OPEN | med | med |

**Sequencing (binding, updated post cross-family review):** the **norm-matched/sham control (dc-c) is now THE primary overturning gate** — gpt-5.5 (FIX-FIRST) flagged that the redistribution claim is not yet shown causal (edit-strength/Δ-norm/depth-metric confounds open). Run dc-c FIRST to confirm the effect is real at all; D7 + dc-b then characterize mechanism + cost. Run D7 (no-edit collinearity curve) for the MECHANISM — if within-entity collinearity is flat/lower at L8-12, the basis-rotation *explanation* is dead (some other depth-dependent geometry), but the redistribution itself still stands (within-loc FALL + expr 100% already exclude an under-editing artifact). dc-b (within-attr top-1) is the VALUE gate. Pre-register + `advisor()` before any re-run.

## J. ConnectedPapers graph review (2026-06-21) — prior-art LEADS (operator-built graphs)
Source: 3 operator-built ConnectedPapers co-citation graphs (seeds: **AlphaEdit** / **MEMIT** / **Connected-Papers-foundations**) exported to `research_and_specs/ConnectedPapers-for-*.bib` (41 nodes each). **DISCIPLINE: co-citation output = IDEATION → LEADS, NEVER evidence, NEVER `CORPUS/`** (§1/§3). arXiv IDs are from the .bib export; only **NeuralDB independently fetched + confirmed** (`external_evidence_notes.md` EV-3). Each lead → falsify by experiment or fold into F1 framing; verify before any port ([[verify-external-artifacts-before-effort]]). Graph B (LightMem/agent-memory) = mostly external-memory **contrast class** + co-author noise (Minecraft/multimodal) → low signal for in-weight; relevant minority = Mem0/MemoryOS (deployment competitor for the "why in-weight not RAG" F1 justification).

| # | Lead (source paper, arXiv) | Maps to | builds-on / would-advance | Status | Cost | Value |
|---|---|---|---|---|---|---|
| D8 | **Norm-growth is the sequential-collapse driver** — NAS *Norm Anchors* (2602.02543, "positive norm-feedback loop", ~exp growth, fix=rescale value-vec to orig norm, +4× horizon) + Gupta *Better Regularization* (2502.01636, Frobenius constraint→10K) + DeltaEdit (2505.07899). → **the §8.7 drift variable may be cumulative (per-relation) ‖ΔW‖, not edit-count/concentration.** | D1 / §8.7 / D-NOISE | builds-on D1 (D-D1-1 ⟨D-D1-1@0db8d819⟩) + the ΔW-norm log already in B1 §2.1 / threshold-instrument §4. would-advance the numeric §8.7 threshold | **OPEN — TESTABLE IN-PLACE** (instrument already logs cumulative ‖ΔW‖_F; check corruption-vs-norm in the sweep output; TEST the field's claim, don't adopt) | ~0 (already logged) | **HIGH (could set the threshold variable)** |
| D9 | **Gated/routed SIDE-STORE structurally avoids cross-entity corruption** — NeuralDB (2507.18028, "linear L&E = KV-DB query"; non-linear gated retrieval fires only on edited facts; 100K facts on Llama-3-8B) + WISE (side-memory+routing) + GRACE/MELO. Closest prior art to the LLM-as-DB spec. | B3 / C1 / spec core | builds-on B3 (drop diffuse L2?) + C1 (GRACE/WISE family). would-advance the F1 in-weight-vs-side-store decision | **OPEN — VERIFY REPO BEFORE PORT** (EV-3: paper confirmed real; no repo link surfaced — GitHub-check before effort) | low (read) → med (port) | **HIGH (bears on the thesis)** |
| D10 | **A1 batch-clean is independently corroborated** — LocFT-BF *Fine-tuning Done Right* (2509.22072): breadth-first (batch/epoch) beats depth-first (sequential) b/c per-edit over-optimization induces cross-edit interference; 100K edits/72B. | A1 (D-A1-1 ⟨D-A1-1@92b78833⟩) | builds-on A1. would-advance F1 (external grounding that batch/Genesis path is the right deployment model) | OPEN — F1 framing (corroboration, not new test) | ~0 | med-HIGH |
| D11 | **Eval-critique cluster = our rigor corroborated + the thesis's standing threat** — *Mirage of Model Editing*/QAEdit (2502.11177, teacher-forcing inflates 96.8%→38.5% real, fails @1000 edits) + *Is Model Editing Built on Sand?* (2510.00625, edits = shortcuts not semantics, collapse under negation). | E-cluster / F1 honesty | builds-on our autoregressive-top-1 discipline + G6.1 scale-corruption. would-advance F1 (must confront "editing is illusory" head-on) | OPEN — F1 must address | low (framing) | **HIGH (F1 credibility)** |
| D12 | **Competing mechanisms for cross-entity corruption** — SADR *Over-Attention* (2502.14838, "Attention Drift": heads over-attend edited entity) + REVIVE *Spectral* (2601.11042, edits disrupt DOMINANT singular dirs) + SUIT (2509.24502) / SPHERE (2510.01172, both on Qwen2.5-7B). Our mechanism (shared relation dir in editable subspace) is ONE of these. | D2 / D7 / C2 | builds-on D2/C2 mechanism. would-advance D1 (which geometry actually drives the corruption) | OPEN — discriminating check mostly no-GPU | low-med | med-HIGH |
| D13 | **Context-reasoning may beat in-weight editing** — SCR *No More Model Editing!* (2503.05212) + *Benchmarking & Rethinking KE* (2505.18690): in-context reasoning > parameter editing under realistic eval. Deployment incarnation = Graph-B external memory (Mem0/MemoryOS). | B3 / E3 (thesis falsifier) | builds-on B3/E3. would-advance the core F1 in-weight-necessity verdict | OPEN — the highest-stakes thesis lead | low (analysis) | **HIGH** |
| D14 | **Localization ≠ editing-site** — Hase *Does Localization Inform Editing?* — causal-trace storage location does NOT predict best edit layer. Challenges the locate-then-edit band premise. | C2 / C15 band-selection | builds-on our band work (L4-8 vs L8-12 vs spec L15-25). would-advance F1 band-choice justification | OPEN — confirm our band choices don't rest on the refuted assumption | low | med-HIGH |
| E4+ | **RippleEdits = the field's name for our cross-entity read corruption** — *Evaluating the Ripple Effects of KE* (Cohen et al.). Strengthens existing **E4**. | E4 (existing) | builds-on G6.1/D1. would-advance F1 (map our per-relation-concentration law onto RippleEdits categories for shared vocabulary + external benchmark) | OPEN — extends E4 | med | med-HIGH |
| D15 | **Cross-architecture recall analysis** — *Do All Autoregressive Transformers Remember Facts the Same Way?* — overlaps our DONE cross-architecture ceiling (Llama/Mistral/Qwen, entity-local-not-attribute-local). | cross-arch ceiling (DONE) | builds-on the cross-arch ceiling result. would-advance F1 (compare BEFORE citing ours as novel — corroborate or partial-scoop) | OPEN — comparison owed before F1 | low (read) | med-HIGH |
| (ref) | **Bedrock citations** — Geva *FFN Layers Are KV Memories* (FFN=KV store = the literal "LLM-as-DB" basis) + *Dissecting Recall* (subject-enrich→attribute-extract, underlies entity-vs-attribute-local). | foundations | — | reference for F1 | — | — |

**Sequencing (J):** D8 is **free + immediate** (test against the instrument sweep already queued). D11/D13 are **F1-framing** (the thesis's two strongest external threats — must be confronted in the readiness write-up, no new compute). D9 (NeuralDB) + D15 (cross-arch) need a **read/verify** before they gate any effort. D12/D14 are cheap no-GPU mechanism/band checks. None promotes without its own falsifying test (DISCIPLINE §3 — co-citation = lead, not evidence).

### J.2 — MEMOIR + foundations-variant graphs (2026-06-21, graphs 4–5)
Two more operator graphs (seeds **MEMOIR** lifelong-editing + a 2nd **foundations** neighborhood). Heavy overlap with the central cluster (AlphaEdit/NeuralDB/WISE/Mirage/Built-on-Sand/SADR/O-Edit/Model-Editing-at-Scale) → confirms the core. The foundations-variant adds the **lifelong adapter/routing family** (LEMoE, Reversible Routing-LoRA, RAG Continuous-Prompt, In-Context Editing, SCEN) — all **reinforce D9** (gated/routed side-store) + **D13** (in-context). **Two GENUINELY NEW high-value leads** (the field has *named our problem and our confound*):

| # | Lead (source paper) | Maps to | Why it matters | Status |
|---|---|---|---|---|
| D16 | **KEO / SetKE** — *Knowledge Editing for Knowledge Elements Overlap* (2025): names **"Knowledge Element Overlap (KEO)"** — triplets sharing a common element cause editing conflicts/degradation — and proposes **Knowledge Set Editing** (edit the overlapping set jointly) + benchmark **EditSet**. | **G6.1/D1** (problem) + **A1** (solution) | **The field's name for our exact cross-entity same-relation corruption** (facts sharing the relation element) — AND their joint set-edit fix is conceptually **our A1 batch result**. Strong external mapping of both our problem and our solution; EditSet is a ready benchmark. | OPEN — compare/read before F1 cites ours as novel |
| D17 | **Editing Overfit / EVOKE** — *Uncovering Overfitting in LLM Editing* (2024): edited models assign **disproportionately high prob to the edit target** ("Editing Overfit"), hurting generalization; benchmark **EVOKE** + metrics; common mitigations ineffective. | **E1** (margin confound) | **External naming + benchmark of our E1 margin confound** (compute_z inflates edited margins, median 0.979). Corroborates E1 and gives a named benchmark to measure it. | OPEN — strengthens E1 |

**Reinforcements (not new):** MEMOIR (sparse activation masks → edits occupy **disjoint** param subsets + activation-pattern routing) and SCEN (per-fact expert + indexing neuron) and InComeS (gist-token KV compression + cross-attention selection) are all **D9 side-store/routing variants** — three more independent votes that the field's answer to our corruption is "route edits to disjoint/gated stores." *Retention-after-fine-tuning* (edited knowledge forgets faster under downstream FT) → minor **F2** durability lead. *RippleCOT* + *Multi-Hop Factual Shortcuts* (~20% of failures = pretraining-co-occurrence shortcuts) → **E4/D11**.

### J.3 — MEMORYLLM graph (2026-06-21, graph 6) — self-updatable parametric memory
Seed **MEMORYLLM**; neighborhood = long-context (Infini-attention, StreamingLLM, InfLLM, RULER, LongBench, LongLoRA…) + self-updatable-memory architectures. Mostly the **"context/external-memory instead of weight-editing" contrast class** → reinforces **D13**. Diminishing returns on this axis (flagged). **One new lead:**

| # | Lead | Maps to | Why it matters | Status |
|---|---|---|---|---|
| D18 | **Self-updatable parametric memory** — MEMORYLLM / M+ (scalable long-term memory) / **Larimar** (episodic memory control) / **MemLLM** (finetune LLM to use an explicit **read-write memory**). | B3 / D9 / **spec read-write contract** | The architectural **midpoint** between diffuse in-weight editing and external RAG: a model with an **explicit, updatable read/write memory interface** — the closest published cousin to the LLM-as-database **read/write contract** itself (not just the write side). Larimar/MemLLM are the ones to read for the spec's interface positioning. | OPEN — spec-positioning read; not a falsifier |

### J.4 — MoE / Parametric-Knowledge-Injection graph (2026-06-21, graph 7)
Seed *Decoupled MoE for Parametric Knowledge Injection*; 0 overlap with the editing graphs (different cluster: Parametric-RAG + frozen-LM-reader + legal-IR co-author noise). One new lead:

| # | Lead | Maps to | Why it matters | Status |
|---|---|---|---|---|
| D19 | **Parametric RAG / per-query knowledge injection** — Parametric RAG, Decoupled-MoE, GenPoE, "Decoupling Knowledge and Context via Cross-Attention": encode retrieved knowledge into **transient per-query adapter/expert parameters** at inference. | B3 / D9 / D13 / spec offline-edit-vs-online-inject | A **third paradigm** between diffuse in-weight editing and context-RAG — bears on the spec's edit-offline-vs-inject-online design choice and the B3 in-weight-necessity question. | OPEN — spec-positioning read |

**Also-relevant (minor):** *Large Scale Knowledge Washing* (deletion at scale) + *Forgetting before Learning* (parametric arithmetic updating) → **NEW-4** (DELETE write-type). *Adaptive Token Biaser* / *Outdated-Issue-Aware Decoding* → decoding-time edit handling (a D13 variant). Long-context methods themselves = out of scope for in-weight editing.

### J.5 — B3N reconcile (2026-06-21): the compaction-at-scale falsifier (advisor reconcile pass)

| # | Lead / Hypothesis | Maps to | Why it matters | Status |
|---|---|---|---|---|
| **D20** | **Compaction-at-scale may REINTRODUCE the corruption it heals.** The §8.10 self-heal assumes "compaction = full MEMIT re-run → returns to clean A1." But A1 validated cleanliness only at **N≤100 / 3B single-batch**, while §8.7 lets drift reach **1,500–8,000 edges** before compaction and **MEMIT batch size is 2,000** (spec line 384) → above that, compaction is **sub-batched = multiple sequential solves** = the exact G6.1/D-D1-2 sequential-accumulation corruption mechanism. | **B3N condition 3** / §8.10 / §10.4 / builds-on A1 (`D-A1-1`) + the G6.1 sequential-corruption finding + D1 (`D-D1-2`) | **The sharpest open F1 falsifier.** If compaction isn't clean at scale, B3N condition 2 ("compact in time") cannot save the in-weight verdict — there is no clean state to return to, and the hybrid needs the side-store after all. Higher-priority than the 7B numeric transfer. | **DONE = DIRECTIONAL MECHANISM, NOT a falsification (D-D20-1, CORPUS/23; tightened by gpt-5.5).** Defensible: accumulating sequential sub-batching can reintroduce held-out corruption even when the equivalent single joint solve is clean — order-insensitive across 3 orderings at **C=10** (−19 to −34pp; expr 96-100%; anchor gates pass; directional, n=3 → not effect-size-bounded); C=25 order-soft → not promotable. **→ B3N condition 3 directionally PRESSURED (not naively clean), NOT evidenced-failing.** **NOT proven the spec's 2,000-SIZE compaction fails** — **(a) K-vs-C confound** (fixed N → C=10 also K=10; may be accumulated-update-COUNT not size → needs a **2D N×C grid**); **(b)** the spec's 2,000-SIZE regime + exact sub-batching semantics untested (may indict our cache_c algorithm). Under this harness only. NOT a promoted node. Dual-reviewed (advisor ×2 + gpt-5.5). `docs/D20_COMPACTION_SUBBATCH_PREREG.md`. |

**Cumulative graph-review verdict (6 graphs):** the central editing cluster is **well-mapped and saturating**; new graphs now mostly re-surface known nodes. Net yield = leads **D8–D18** + E1/E4 strengthened. The dominant cross-graph signal is unchanged and strong: **the field routes edits to gated/disjoint/explicit side-stores (D9/D18) or avoids weight edits via context (D13); diffuse in-weight editing has a named failure mode (KEO/D16) and a named confound (Editing-Overfit/D17).** This is the F1 in-weight-necessity question (B3) — the highest-value next analysis. Further editing-neighborhood graphs = strong diminishing returns ([[evidence-over-scaffolding]]).


**D-D1-2 closure (2026-06-21):** D8 norm-growth lead folds into the §8.7 instrument result — **D-D1-2** (2026-06-21): §8.7 numeric-threshold instrument → **operational guardrail `k≤2`** (max unanchored per-relation concentration; anchor before k=3). Dual-reviewed (Opus advisor + gpt-5.5 cross-family). k=3-4/k=10-12 = scoped order-dominated observations, NOT portable thresholds; per-relation count = fail-closed SENTINEL not the causal var (edit-set/key-collinearity geometry is). 3B-only (size transfer OPEN), pure-capital anti-conservative, incremental-path-only (deploy=batch/Genesis A1-clean). Instrument: 3B within-process SD=0; ~50pp noise is 7B/across-process; binding 3B uncertainty = edit-ORDER. Artifacts: results/d1_threshold_lowk_3b_s3{,_lowextra}.json, results/d1_instrument_variance_diagnostic_3b_*.json; reviews logs/codex_review_threshold_*OUT.log.

## K. Chris Hay (chrishayuk) corpus deep-read (2026-06-23) — prior-art LEADS
_Tier-1 deep-read of the LARQL author's corpus (`larql`, `the-mechanism`, `tiny-model`, `chuk-kv-anatomist`, `virtual-experts`, `verifiers`, `structured-outputs`, …). He has independently built a near-complete parallel implementation of our thesis. **LEADS only — code-level facts, not verified on our (Qwen) stack; several repos unlicensed.** Full detail: `docs/CHRISHAYUK_CORPUS_GAP_MAP.md`. The four ⚠LOAD-BEARING items are flagged in runbook §0.3._

| # | Lead / Hypothesis | Maps to | Why it matters | Status |
|---|---|---|---|---|
| **K1** ⚠ | **CP2 read-contract partly UNSUPPORTED by LARQL.** LQL `SELECT/WALK/INFER` over flat `(s,r,o)` edges (`WHERE = != > < >= <= LIKE IN`, reverse lookup, `NEAREST TO … AT LAYER`, FR3 synonym-resolution) but **NO aggregation, NO negation/NOT-EXISTS, NO JOIN, NO multi-hop in SELECT, NO "violates"/constraint mechanism**. Native read = 2-stage (clean RELATION index L10 × fuzzy ENTITY top-k L24-26 → rank/verify), not deterministic single-row; optimal read layer field-dependent. | **CP2** | Our CP2 "5 query families + violates-rejection + reverse/aggregation/negation" → aggregation/negation/violates are **net-new on top of LARQL, not served**. Reshapes CP2 scope + the readback-determinism assumption. | **OPEN — LOAD-BEARING** |
| **K2** ⚠ | **LARQL already implements the compaction-bounded HYBRID we decided (D-B3N-1) + a `REBALANCE` fixed-point solver.** Tiered `MODE KNN` (side-store L0, 25K edges) → `MODE COMPOSE` (in-weight L1, ~5-10/layer cap) → L2 MEMIT; `COMPACT MINOR/MAJOR` promote; `REBALANCE` rescales compose magnitudes to break the per-insert cap. | **B3 / D-B3N-1** / compaction | We theorized the hybrid + a rebalance; **he has the working mechanism**. Corroborates D-B3N-1; gives a concrete tier policy to verify on Qwen. | **OPEN — LOAD-BEARING** |
| **K3** ⚠ | **LARQL serve = MEMIT (vanilla ridge), NOT AlphaEdit.** `memit_solve` = ridge, no covariance/null-space; MEMIT bake off-by-default on Gemma. `the-mechanism/native.py` = training-free projection-free single-neuron write (6/6 retention). | write contract / B3N | Our recipe leans on **AlphaEdit null-space for locality** — LARQL's serve layer doesn't provide it (hence COMPOSE needs REBALANCE). Gap if the write contract assumes AlphaEdit at serve. | **OPEN — LOAD-BEARING** |
| **K4** ✅ | **LARQL keeps FFN `down` at Q6_K (not Q4_K) by default** (bakes edit into f32, re-quantizes whole; weak-gate trick). | **B3/G6.2** (CORPUS/17) | **✅ VERIFIED 2026-06-23** (`b3_edited_q4km.gguf` via gguf-py): `Q4_K_M` makes ffn_down a MIX (18×Q6_K/18×Q4_K); **4 of our 5 edited band-[4,5,6,7,8] `down_proj` layers were Q4_K (4-bit), only L6 Q6_K** → our B3 test was **STRICTER** than LARQL's uniform-Q6_K-down serve path, and edits survived 100%. Result is **conservative/robust**; LARQL merely more protective (optional margin we don't depend on). NOT a hidden weakness. Gotcha: `Q4_K_M` ≠ uniform 4-bit — verify per-tensor. | **RESOLVED — verified favorably** |
| K5 | LARQL **GGUF export still 🔴 Planned** (safetensors only); no CUDA (Metal-only); no continuous batching. | Track E deploy | Our LARQL-native GGUF/CPU route assumes an unmerged path (our llama.cpp path is independent). | OPEN — serve risk |
| K6 | **GQA edit-transfer untested everywhere.** `mha_gqa_benchmark` = unreliable LLM-prose judge, not weight-editing. GQA `repeat_interleave` → an edited K/V direction hits all heads in a group, uncharacterized vs MHA. | **OQ-W1 / 7B (B1)** | Our 7B (Qwen-GQA) transfer stays genuinely open; the repo named for it doesn't address the weight-editing form. | OPEN (shared blind spot) |
| K7 | **Multi-token unaddressed by LARQL's own author.** `the-mechanism` all single-token (drops multi-token facts). | **G7** | Confirms multi-token is real + unsolved upstream — not closeable by reading. | OPEN (shared blind spot) |
| K8 | **Per-relation capacity ceiling ~"a dozen facts/relation" from geometry** (`unique_part()` runs out of near-orthogonal entity directions); LARQL COMPOSE ~5-10/layer = same wall. | **D1 / D20** | Geometric (not method) capacity bound + scaling prediction (wider d_model = higher cap) — corroborates the concentration law. | PARTIAL (convergent) |
| K9 | **Borrowable instruments:** `kv_inject_test` (KL<0.001 "fact=one direction") + `is_one_dimensional` rank-of-fact (⚠ cache-side vs our weight-side); deterministic verifier-registry + graduated/decomposed reward + anti-shortcut/provenance invariant + schema-constrained typed readback → a 3-way CP2 read-contract decomposition. | CP2 + eval rigor | Ready instruments for an independent edit-locality read + a falsifiable decomposed read/eval contract. | OPEN — adoptable |
| K10 | **Routed side-store reliability lever** (`virtual-experts`): route the **normalized canonical action**, not raw NL → deterministic routing; 2 router substrates (activation-direction vs symbolic-action). | B3 (side-store) | Concrete protocol for the "reliable routing" half of B3; a routing failure may be an upstream *normalization* failure. | OPEN — lead |

## L. Git-repos-audit batch review (2026-06-23) — general-tooling axis, LOW yield
_Triage of the 45-repo browser-tab-group audit (`research_and_specs/git-repos-audit-6-22-26.md`), reviewed by **content not stars** at description-level (proportionate to yield; no deep-reads warranted). **HEADLINE / EXHAUSTION CALL:** this batch is the **general-tooling + "awesome-list" + personal-project axis** — categorically different from the editing/prior-art clusters of §H/§J/§K. It yields **ZERO load-bearing leads** and **zero new hypotheses**. The four genuine touches below are either tooling-to-verify-before-rely or **REINFORCEMENTS of already-saturated clusters** (D9/D18 side-store, D2 mechanism) — no new signal. **Recommendation: do not commission further batches of this kind** (awesome-lists / Claude-Code-workflow / curated-index repos); the discovery-resource axis is exhausted for this program ([[evidence-over-scaffolding]]). Fence: external/unvetted LEADS, never `CORPUS/` (§3). No double-logging vs the 130-catalog/§H (collision-checked: transformers/promptfoo/LlamaFactory absent there). ⚠ **Audit-integrity notes:** header says "45 repositories" but the TOC + body list **46** (MLX-LoRA-Studio is #46, uncounted); all spot-checked metadata (gstack 113k, headroom 47.1k, continual-harness, mengram, promptfoo, apache/age, flux) **verified accurate** against the live GitHub API 2026-06-23 — the two implausibly-high star counts are real, audit numbers are trustworthy ([[verify-external-artifacts-before-effort]])._

| # | Repo | Maps to | Relevance | Status |
|---|---|---|---|---|
| L1 | [apache/age](https://github.com/apache/age) — graph DB as a PostgreSQL extension (openCypher in Postgres; 4.6k★, Apache project) | **CP2 / B2 / D9** | The only entry of structural interest: a real, production graph-query engine. **Dual mapping** — (a) *positioning* comparator for the spec's LQL/"queryable graph DB" read contract; (b) a **concrete substrate candidate** for the gated/structured side-store the field converges on (D9, EV-2 "structured KG not L1-KNN"). Do NOT inflate past comparison/positioning — it is a relational+graph DB, not a knowledge-editing system. | OPEN — positioning lead (verify relevance before any effort) |
| L2 | [promptfoo/promptfoo](https://github.com/promptfoo/promptfoo) — LLM prompt/agent/RAG eval + red-team framework (22.5k★, used by OpenAI/Anthropic) | **E-cluster / CP2** | Eval-harness lead **only if** it can express our autoregressive-**top-1** / read-back / `violates`-rejection assertions at the **token-distribution** level. Likely **redundant** with our bespoke EXACT-match harness (declarative-config assertions ≠ next-token-distribution probes); verify-before-adopt, do not assume it replaces our instrument. | OPEN — verify-before-rely |
| L3 | [alibaizhanov/mengram](https://github.com/alibaizhanov/mengram) (179★) · [hiyouga/LlamaFactory](https://github.com/hiyouga/LlamaFactory) (72.4k★) · [jphall663/awesome-machine-learning-interpretability](https://github.com/jphall663/awesome-machine-learning-interpretability) (4k★) | **D9/D18** (side-store) · **C1/B3** (LoRA contrast) · **D2** (mechanism index) | **REINFORCEMENTS, NOT new leads** (cf. §J.2 "Reinforcements"). mengram = another semantic/episodic/procedural **external side-store** (⚠ single-author + "Free API" external dependency — contrast class, not adoptable). LlamaFactory = LoRA/QLoRA fine-tuning = the side-store/adapter contrast method, not our MEMIT path. awesome-ml-interpretability = a discovery index over the D2 mechanism space we already have specific refs for. All three add **votes to clusters §J already declared saturated** → zero new signal. | NOT-NEW (saturated cluster) |
| L4 | Claude-Code / agent **operational tooling** (not science): [headroom](https://github.com/headroomlabs-ai/headroom) 47.1k★ (token compression — context-cost only) · [yamadashy/repomix](https://github.com/yamadashy/repomix) · [nicobailon/visual-explainer](https://github.com/nicobailon/visual-explainer) · [anthropics/claude-cookbooks](https://github.com/anthropics/claude-cookbooks) · [Skill_Seekers](https://github.com/yusufkaraaslan/Skill_Seekers) · [zcaceres/claude-rm-rf](https://github.com/zcaceres/claude-rm-rf) · [garrytan/gstack](https://github.com/garrytan/gstack) 113k★ · codedna · retune | **operational / harness** | Workflow utilities that could marginally improve *how we work* (context compression, result-rendering, repo-packing, rm-rf safety already covered by our CLAUDE.md). **Not program science**; no hypothesis maps here. Noted once, not row-by-row. | reference (operational) |

**Noise bucket (≈30 repos — named once, not triaged individually):** the broad **awesome-lists / curated indexes / learning resources** (awesome-jupyter(lab), awesome-ai-tools, awesome-mlops, awesome-db-tools, datascience, anomaly-detection-resources, awesome-project-ideas, awesome-data-engineering, awesome-ml-on-source-code, awesome-database-learning, 500-AI-projects, awesome-generative-ai-guide, awesome-production-ml, awesome-nlp, Awesome-pytorch-list, Qix, data-engineer-handbook, system-design-primer) and the **personal/student-project cluster** (the `ashley-ha` repos: ULMFiT, detect_anomaly_pm, transformers-summarization, DatesetExtraction, research2Code; plus cursive-py 2★, llm-evals-workshop 15★, flux, ai-data-science-team, LLM-Engineers-Handbook, MLX-LoRA-Studio, continual-harness/PokeAgent) carry **no knowledge-editing or read/query-contract content** → discovery-resource value at most, irrelevant to the write-engine/CP2/B3 program. `huggingface/transformers` = already our pinned core dependency (4.51.0), not a lead. `awesome-database-learning`/`awesome-db-tools` only loosely touch the DB-vocabulary framing the spec already borrows. DatesetExtraction (Q&A-from-LLM extraction) is the lone *method-adjacent* personal repo (the D20 single-token stimulus-pool build) but is a trivial student script — no value over our own `g6_screen.py`.

**Sequencing (L):** nothing here gates or accelerates the current §0.3 priorities (CP2 read-contract → D20 component-1 scale → 7B transfer → F1). L1 (apache/age) is the only entry worth a **bounded** look — and only *when* CP2/B3 side-store substrate selection is actively in scope, as positioning, not as a port. This pass's primary product is the **exhaustion call**: the general-tooling/awesome-list discovery axis is mined out; future repo-review effort should target **editing/memory/eval prior-art** (the §J/§K axis), not curated-index batches.

## M. Editing/memory/eval prior-art repo SWEEP (2026-06-23) — HIGH yield, code-level LEADS
_Comprehensive GitHub sweep of the editing/memory/eval **implementation** axis (the L-recommended §J/§K axis), keyed to §0.3 (CP2 → D20 → 7B/GQA → F1). 6 parallel search agents, one per sub-axis; **every repo verified live via the GitHub REST API 2026-06-23.** Full tables + per-axis detail: **`docs/PRIOR_ART_REPO_SWEEP_2026-06-23.md`**. **Fence:** verified-to-EXIST, NOT verified on our Qwen/AlphaEdit/Q4_K_M stack — LEADS only, never `CORPUS/` (§1/§3). ⚠ Authenticated code-search was unavailable to agents (web+name search only) → a brand-new low-star query-DSL repo could be missed. Unlike §L this axis is **rich in runnable code** — the six items below CHANGE an open-question framing (vs. the many merely-confirmatory repos in the doc)._

| # | Lead | Maps to | Why it matters | Status |
|---|---|---|---|---|
| **M1** | **CP2 — no query-*DSL* over weights surfaced among the obvious read-side neighbors (search-bounded).** PatchScopes, [nnsight](https://github.com/ndif-team/nnsight), SelfIE, [TransformerLens](https://github.com/TransformerLensOrg/TransformerLens), LAMA = **NL-probe or Python-API, NOT a declarative query DSL** (no SELECT/WALK/aggregation/negation/violates). NeuralDB states the concept, ships no DSL/code. | **CP2 / K1** | **Supports (not proves)** CP2 novelty — "no query-DSL surfaced via name/web search," NOT "verified none exists." ⚠ Authenticated code-search was unavailable; this is the claim we most *want* true → hold search-bounded. Substrates: [nnsight](https://github.com/ndif-team/nnsight) + [mega002/ff-layers](https://github.com/mega002/ff-layers). | OPEN — F1 novelty framing (search-bounded) |
| **M2** ⚠ | **D20 — runnable norm-anchoring mitigation baselines exist; but the headline repo is NOT A1 corroboration.** [ENCORE](https://github.com/scalable-model-editing/encore) (Frobenius constraint, 10K) + [NAS](https://github.com/SasyaTitech/NAS) (value-rescale, ~20K) = closed-form-editor drop-ins diagnosing collapse as **norm growth** (aligns D8). **⚠ Calibration (advisor):** [LocFT-BF](https://github.com/ICT-STAR/LocFT) (100K/72B) is **gradient fine-tuning, not closed-form MEMIT**, and its breadth-vs-depth contrast **does not exercise the sub-batched-MEMIT-at-~2000 compaction mechanism D20 actually tests** → **cross-family ANALOG only; NOT A1 corroboration and NOT a D20 pre-emption.** | **D20 / D8** (A1 link demoted to analog) | We gain **mitigations to test against** our sub-batched compaction (ENCORE/NAS) + [MEMIT-Merge](https://github.com/NUSTM/MEMIT-Merge) (same-entity batch-collision fix → maps our same-entity-locality corruption). We do **not** gain external A1 corroboration. | OPEN — test mitigations |
| **M3** ⚠ | **GQA edit-transfer gap confirmed in code; narrows K6, NOT OQ-W1.** No repo characterizes edit-transfer under GQA; all mainstream editors edit **MLP down/up-proj** (GQA K/V head-sharing **architecturally orthogonal**). **Our inference (unverified):** our `down_proj` recipe *likely* dodges GQA too → narrows **K6** (GQA sub-concern). **⚠ Scope (advisor): does NOT narrow OQ-W1 proper** = the recipe's *7B numeric-threshold transfer via determinism*, an MLP-side question GQA-orthogonality says nothing about. Edited-K/V case open everywhere. | **K6** narrowed; **OQ-W1 / B1** untouched | If we test the K/V case we'd be first — instrument = [pyvene](https://github.com/stanfordnlp/pyvene). 7B numeric transfer stays the real open item. | OPEN — K6 narrowed (inference) |
| **M4** | **7B/our-model-family editing exists at scale.** [UltraEdit](https://github.com/XiaojieGu/UltraEdit) edits **Qwen2.5-7B-Instruct, 7B on 24GB, 2M edits** (highest stack-match); [LocFT](https://github.com/ICT-STAR/LocFT) edits the **Qwen2.5 7B→72B** ladder. [composable-interventions](https://github.com/hartvigsen-group/composable-interventions) = MEMIT×AWQ-8bit on Llama3-8B(GQA) (quant-aware). | **OQ-W1 / B3/G6.2** | Concrete 7B-transfer comparators on our exact model family + the closest quant-aware-editing reference. | OPEN — adoptable comparators |
| **M5** | **Side-store family benchable from ONE repo; NeuralDB is the code gap.** [EasyEdit](https://github.com/zjunlp/EasyEdit) (major update 2026-06-22; our vendored assets stale) **bundles WISE+GRACE+MEMOIR+MELO** → compare gated side-stores vs in-weight on the SAME read contract without porting 4 codebases. Self-updatable parametric memory (the middle path, D18): [MemoryLLM](https://github.com/wangyu-ustc/MemoryLLM)+M+, [Larimar](https://github.com/IBM/larimar), [MemLLM](https://github.com/amodaresi/MemLLM). | **B3 / D9 / D18 / K2** | The B3 in-weight-vs-side-store decision can be **empirically benched** from EasyEdit. ⚠ **NeuralDB has NO public code** (confirmed 2×; the archived `facebookresearch/NeuralDB` is unrelated) → reimplement to test (corrects §J D9). | OPEN — highest-leverage bounded bench |
| **M6** | **F1 eval-honesty critiques are all runnable harnesses to borrow + report against.** [QAEdit/WILD](https://github.com/WanliYoung/Revisit-Editing-Evaluation) (teacher-forced→autoregressive, D11) · [RippleEdits](https://github.com/edenbiran/RippleEdits) (our cross-entity corruption, E4) · [EVOKE](https://github.com/Acruxos/EVOKE) (our margin confound, E1/D17) · [MQuAKE-Remastered](https://github.com/henryzhongsc/MQuAKE-Remastered). **G7 multi-token is NOT novel** — [AnyEdit](https://github.com/jianghoucheng/AnyEdit) (plug-and-play over MEMIT/AlphaEdit, 458-tok) is a ready port target; UnKE/μKE follow. "Built on Sand" negation critique = no repo, port the small PP/PN/NN/NP protocol. | **E-cluster / F1 / G7 / K7** | F1 can report against shared peer-reviewed benchmarks instead of bespoke-only; G7 has a concrete adopt path. | OPEN — F1 eval + G7 port |

**Sequencing (M):** ORDER unchanged (CP2 → D20 → 7B → F1) — this sweep **arms** each cell, doesn't reorder it. Highest-leverage *bounded* actions if/when taken (all operator-gated): (1) pull current [EasyEdit](https://github.com/zjunlp/EasyEdit) for a WISE/GRACE/MEMOIR-vs-in-weight bench on the same read contract (B3/D9, M5); (2) [QAEdit/WILD](https://github.com/WanliYoung/Revisit-Editing-Evaluation) for the autoregressive eval-honesty bar (M6). Memory [[larql-the-mechanism-prior-art]] "our work fills its gap" already flagged for revision (§K) — M1 (CP2 query-DSL novelty) is the part that **search-boundedly holds** (no competing DSL surfaced; not exhaustively verified): the query-DSL layer appears to be ours, while the edit/memory/eval cells are crowded prior art. **⚠ M1 is REFINED by §N/N1** — a formal query-language-over-a-network (Grohe FO(SUM)≈SQL) DOES exist at the *numeric weight* altitude; CP2's novelty holds only at the *decoded-factual* altitude. None promotes without its own pre-registered falsifier on our stack ([[prototype-tautology-trap]], [[verify-external-artifacts-before-effort]]).

## N. Editing/memory/eval prior-art arXiv SWEEP (2026-06-23) — paper-side counterpart to §M
_Comprehensive arXiv sweep of the editing/memory/eval **paper** axis, keyed to §0.3 (CP2 → D20 → 7B/GQA → F1). 6+1 parallel agents; **all 57 arXiv IDs batch-verified by the MAIN agent via two export-API `id_list=` calls** (titles all matched; zero missing) — subagents were 429-throttled onto abs-page/WebSearch fallbacks, so main-agent re-verification was required, not optional ([[verify-external-artifacts-before-effort]]). Full tables + per-axis detail: **`docs/PRIOR_ART_ARXIV_SWEEP_2026-06-23.md`**. **No tool install needed** (official arXiv API + WebSearch; the audited arXiv-MCPs were deliberately not installed — unvetted single-author). **Fence:** verified-to-EXIST, NOT read in full, NOT on our stack — LEADS only, never `CORPUS/` (§1/§3). Papers complement the §M repos (claims/critiques/mechanisms vs runnable code). Verbs calibrated — a cross-family/cross-metric paper is NOT corroboration of our results ([[editing-memory-eval-prior-art-landscape]])._

| # | Lead | Maps to | Why it matters | Status |
|---|---|---|---|---|
| **N1** ⚠ | **CP2 novelty REFRAMED: a formal query-language-over-a-network EXISTS, but not over decoded facts.** Grohe "Query languages for neural networks" ([2408.10362](https://arxiv.org/abs/2408.10362), ICDT'25) + [2601.09381](https://arxiv.org/abs/2601.09381) define **FO(SUM) = "an abstraction of SQL"** over a network-as-weighted-graph — **but at the NUMERIC weight altitude, explicitly NOT (s,r,o) facts.** Closest factual cousin = "Crawling the Internal KB" ([2301.12810](https://arxiv.org/abs/2301.12810), prompt-extraction, not a query language). | **CP2 / M1 / K1** | **Tempers §M/M1**: at the formal-query-language level prior art exists (must cite Grohe); CP2's contract is novel only at the **decoded-factual** altitude (SELECT facts/negation/violates/multi-hop over in-weight s-r-o). Position LQL as Grohe's factual specialization. | OPEN — F1 positioning |
| **N2** ⚠ | **D20's batch-clean PREMISE is itself untested + one paper complicates it.** Sub-batched closed-form MEMIT compaction at ~2000 = unaddressed. Nearest = "Is Bigger Edit Batch Size Always Better?" ([2405.00664](https://arxiv.org/abs/2405.00664), MEMIT/ROME/EMMET→4096) which **(a)** measures general degradation not cross-entity read corruption, **(b)** no compaction, **(c) ⚠ finds smaller seq batches degrade LESS than one big joint batch** → points AGAINST D20's "joint solve = clean reference" premise. Closest mechanistic neighbor = RLSEdit ([2601.15686](https://arxiv.org/abs/2601.15686), closed-form recursive lifelong). | **D20 / D-B3N-1 cond.3** | D20's framing assumption must be **stated as falsifiable, not assumed**; the [2405.00664](https://arxiv.org/abs/2405.00664) complication folds into D20/F1. Rich NEW collapse-cause (norm-growth [2502.19416](https://arxiv.org/abs/2502.19416)) + mitigation papers to test against. | OPEN — premise to stress-test |
| **N3 / N4** ⚠ | **GQA edit-transfer unaddressed at paper level too (N3, confirms M3); sharper OQ-W1 risk = N4.** [2509.08778](https://arxiv.org/abs/2509.08778) (known): **Qwen stores facts in EARLY ATTENTION, not mid-MLP** → cautions our `down_proj` band [4-8] recipe may not transfer to Qwen at scale, independent of GQA. No in-study size-sweep of recipe+threshold→7B. | **K6 / OQ-W1 / B1 / C2-band** | The attention-vs-MLP storage finding is a concrete transfer RISK (sharper than GQA); ACE ([2510.07896](https://arxiv.org/abs/2510.07896)) edits Qwen3-8B; Golden-Layers ([2602.20207](https://arxiv.org/abs/2602.20207)) = layer-choice-by-architecture. | OPEN — transfer risk flagged |
| **N5** ⚠ | **Eval-critique cluster NOT saturated — 3 NEW existential threats.** [2606.00570](https://arxiv.org/abs/2606.00570) (autoregressive + dimensional-collapse → editors LOSE to retrieval) · [2604.05995](https://arxiv.org/abs/2604.05995) (edits = surface compliance, belief unchanged) · [2510.17941](https://arxiv.org/abs/2510.17941) (belief-depth). New benches: TRACK [2601.15495](https://arxiv.org/abs/2601.15495), logical-rule [2606.10554](https://arxiv.org/abs/2606.10554), ground-truth-free locality [2601.17343](https://arxiv.org/abs/2601.17343). ⚠ [2602.01977](https://arxiv.org/abs/2602.01977) WITHDRAWN. | **E-cluster / F1 / D11 / D13** | F1 must confront the N5 trio (theoretical-limits + belief-depth + surface-compliance) — NOT in our current citations. | OPEN — mandatory F1 reading |
| **N6** | **B3 NEW lit MIXED → reinforces D-B3N-1 hybrid + a NEW in-weight-FAVORING counterweight.** User-as-Engram ([2606.19172](https://arxiv.org/abs/2606.19172)): local parametric edits **overtake retrieval past ~100 facts**, ~33,000× smaller, no per-query context cost. ⚠ **Sign-flip:** HYPE ([2505.18343](https://arxiv.org/abs/2505.18343)) + MeG ([2512.14395](https://arxiv.org/abs/2512.14395)) titled "external memory" but are **in-weight** — NOT side-store evidence. Side-store convergence (RECIPE/ERASE/DR-IKE/LMLM) holds for the incremental regime. | **B3 / D9 / D-B3N-1** | Sharpens the B3 discriminator to **per-query-context-cost vs edit-count crossover**; not a blanket "side-store wins." User-as-Engram = citable in-weight counterweight. | OPEN — F1 framing |
| **N7 / N8** ⚠ | **G7 crowded port target (N7); cross-entity corruption MECHANISM under-addressed (N8).** N7: FABLE ([2604.12559](https://arxiv.org/abs/2604.12559), beats UnKE/AnyEdit, code) = port target. **N8:** the strong recent mechanism papers (MEMIT-Merge [2502.07322](https://arxiv.org/abs/2502.07322), RoSE [2603.15518](https://arxiv.org/abs/2603.15518), [2502.06868](https://arxiv.org/abs/2502.06868)) are all **same-SUBJECT** (identical/collinear keys) — **NOT our same-RELATION/different-entity** phenomenon. No paper models same-relation-direction corruption of held-out entities. | **G7 / D2 / D7 / D12 / G6.1** | G7 = port+re-tune not from-scratch. **N8 = our contribution space**: frame our same-relation corruption as a specialization of Knowledge-in-Superposition ([2408.07413](https://arxiv.org/abs/2408.07413), interference term for unrelated-fact corruption), spectral ([2505.12636](https://arxiv.org/abs/2505.12636)) secondary. | OPEN — contribution space |

**Sequencing (N):** ORDER unchanged. The three highest-stakes *calibrations* (don't let an unread abstract move confidence — the LocFT lesson): **N1** (Grohe = real formal prior art, tempers M1), **N2** (D20's own premise is untested + complicated), **N4** (Qwen attention-vs-MLP storage = concrete transfer risk). F1 newly owes the **N5 existential trio**. Confirmed white cells (contribution space): factual query-contract (N1), same-relation corruption mechanism (N8), GQA transfer (N3), in-study 7B recipe+threshold size-sweep (N4). Bounded reads if/when taken (operator-gated, none started): [2405.00664](https://arxiv.org/abs/2405.00664)+[2601.15686](https://arxiv.org/abs/2601.15686) (D20), [2606.00570](https://arxiv.org/abs/2606.00570) (F1), [2408.07413](https://arxiv.org/abs/2408.07413) (mechanism), [2408.10362](https://arxiv.org/abs/2408.10362) (CP2 positioning). None promotes without a pre-registered falsifier on our stack.


## W. Workflow / process gaps — InfraNodus audit (2026-06-23) — LEADS
| # | Gap | Maps-to | Why it matters | Status |
|---|---|---|---|---|
| W1 | **Governance prototype layer has NO promotion-path edge into the workflow.** 20/20 governance-lifecycle terms (issue/revoke/expire/quorum/lease/two-phase/rollback/ceremony/warden/retention/break-glass/burst/concurrency/transaction/immutability/tamper) = **0 hits** in `DISCIPLINE.md`+`WORKFLOWS_OVERVIEW.md`; `closeout_check` fingerprints result-refs only. Experiments graduate (register→PROGRESS→CORPUS); governance prototypes have no graduation gate. The commit-integrity boundary (hooks/fingerprints) stops at narrative/result edits and never reaches governance transaction state. | G1/G2/G3 governance · governance-stress e2e cell · F1 readiness | When a governance prototype is ready there is no documented path into operational discipline. source: InfraNodus difference+merged → grep-verified. Detail: `docs/WORKFLOW_GAP_AUDIT_2026-06-23.md` | OPEN — lead |
| W2 | **No spec-level threat-model / security-review gate.** Workflow "security" = secret-scan only; `CORPUS/05` contracts + `CORPUS/11` G2 + `security-review` skill not wired into the experiment/closeout cycle. | G2 · threat model · F1 | Adversarial *science* review exists; adversarial *security/threat* review does not. | OPEN — lead |
| W3 | **Deployment/serving mentioned but not gated.** 23 mentions as a constraint, no validation stage like edits have. | E1 deployment loop | weak lead; ties to untested E1. | OPEN — weak lead |

**Fence (W):** InfraNodus-derived → **LEADS, never CORPUS**. Binding evidence = the lexical zero-check (primary-source grep), independent of the lens. None promotes without a pre-registered falsifier. These are *process*-gaps; promoting any into the canonical `DISCIPLINE`/`WORKFLOWS_OVERVIEW` docs is **operator-gated**.

| **R15** | **Can an in-weight MEMIT edit install a prohibition that fires under the §21.2 adversarial Constraint probe?** The one read requirement with no delegation route (a flag/refusal must be generated). MEMIT installs (s,r,o) associations, not behavioral policies — open whether it yields adversarial constraint-firing. | **CP2 read-contract matrix R15** / §21.2 / §8.9 / builds-on the read-contract slice | The sharpest WEIGHTS-owned UNTESTED read-contract falsifier; no governance/index escape. | **D-R15-1** (R15 L2 Constraint-probe firing; `CORPUS/24`, 2026-06-24): the sharpest **WEIGHTS-owned** read-contract falsifier (§21.2, **no delegation route**). v1 subject-property hazard edits express perfectly + fire under cooperative paraphrase (Tier-0 24/24, Tier-1 24/24) but **adversarial firing collapses to ~½** (hand-adj ~11–14/24 flag; ~7/24 silent compliance-leaks; 2/24 explicit refusals). Controls clean (global-shift 0/4; neutral property-specificity color/price 1/24; `Holding` 15/24 = hazard-adjacent, a v1 control-design lesson, pre-reg HALT reinterpreted post-hoc). **Not-ready-with-conditions** for the Constraint sub-type; **bounded** (easiest single-entity case; relational archetype expected worse). **Spec-gap flag (Finding 2):** §21.2 disjunctive pass admits warn-and-comply (Wexolite + ~3 borderline; suggestive, entangled with hazard-vs-prohibition operationalization). band[4–8]/3B/single-batch/1-seed/fictional-hazard. Pre-reg `docs/R15_CONSTRAINT_PROBE_PREREG.md`; runner `experiments/track_c/r15_constraint_probe.py`; result `results/r15_constraint_probe.json`. NOT a promoted node. |

| **R9** | **Does corrective in-weight delete leave residue (resurface on untouched paraphrases)?** Re-tagged 2026-06-24: §11.2 makes weights non-authoritative for every class + no delete-time must-not-fire clause → CHARACTERIZATION not falsification. | **CP2 read-contract matrix R9** / §8.5 / §7.8 / §11.2 | Grounds the overlay-authoritative architecture empirically + delete-time-L2 spec-gap. | **D-R9-1** (R9 in-weight deletion residue; `CORPUS/25`, 2026-06-24): **CHARACTERIZATION, NOT falsification** (label pre-committed — spec read: §11.2/D42 makes Git/`.vindex` authoritative for every class, **NO class is weights-authoritative**; no delete-time must-not-fire clause → resurfacing CONFIRMS the overlay architecture, R2-class). Corrective canonical-only delete (toward pre-write top-1, NOT snapshot-revert) **suppresses a freshly-written localized edit even on untouched held-out paraphrases — RESIDUE 0/7, code rank 0→10³–10⁴** (write generalized 19/24; delete-took 12/12). **But this is the EASIEST deletion case** (inverting a localized self-made edit): (a) FLOOR result — native/distributed-knowledge redaction UNTESTED + expected hard; (b) mechanism = **overwrite-toward-generic** (top-5 filler), not clean removal; (c) **2/12 undeleted bystanders lost paraphrase-retrievability = the G6.1 corruption signature**. Composes with R15: localized FFN edits are easy to REMOVE but hard to make adversarially ROBUST. **Delete-time-L2 spec-gap stands. STRUCTURAL: no class is weights-authoritative (§11.2).** band[4–8]/3B/1-seed/n_informative=7. Pre-reg `docs/R9_DELETION_RESIDUE_PREREG.md`; runner `experiments/track_c/r9_deletion_residue.py`; result `results/r9_deletion_residue.json`. NOT promoted. |

### C1-(a) compaction K-vs-C (D-C1KVC-1, 2026-06-25)
**D-C1KVC-1** (C1-(a) compaction K-vs-C; `CORPUS/26`, 2026-06-25): compaction corruption = **non-additive SCALE×SUB-BATCHING interaction** — concentration-alone (N100 single solve 98.2%) & sub-batching-alone (N50/5ch 98.0%) BOTH clean; only the interaction corrupts (N100/10ch 73.2%, −25pp, p≤0.001). Single-factor-ONLY models refuted by EQUAL-LEVEL contrasts; diff-in-diff (N-eff −3.8pp@C50 vs −24.8pp@C10). Unifies D1+D20; control-dissociation=G6.1 (only cross-entity bystanders corrupt). ⚠ does NOT break literal size-vs-count (count=N/C reciprocal) nor pin functional form; **pressures B3N condition 3, not falsified**. Advisor+Perplexity-Sonar cross-family CONVERGED (codex auth-expired). band[4-8]/3B/N≤100/country/8-ord/1-held-out. Pre-reg `docs/C1_KVC_PREREG.md`; runner `experiments/track_d/c1_kvc_grid.py`; results `results/c1_kvc_grid_result.json` + `results/c1_kvc_stats.json`. NOT promoted; true-scale stimulus-gated (tier b).

### R2 reverse-lookup (D-R2-1, 2026-06-25)
**D-R2-1** (R2 reverse-lookup; `CORPUS/27`, 2026-06-25): **CHARACTERIZATION** (pre-committed; D16 auto-reverse + §11.2 → cannot falsify). A forward in-weight MEMIT edit (C→X, subject-keyed) creates **ZERO reverse-readable edge** — forward-took 24/24, native-reverse positive control 8/10 (template valid), reverse base 0/24 → post 0/24, **max |ΔP(country)|=0.0003 across all 24** (nothing moved). Edits are **write-only/forward-only** (reversal curse, complete). → D4 'no write-only edges' unsatisfiable weight-native; reverse-lookup index/`.vindex`-delegated (D16), §11.2 confirmed. **Bounds read contract for B3N: weights carry FORWARD native-knowing (100%) but ZERO reverse → reverse/bidirectional leg entirely side-store-delegated.** Composes with R9/R6 (storage/retrieval read legs index-owned; only behavioral firing R5/R15 weights-owned). band[4-8]/3B/N=24/capital↔country/1-seed. Advisor done-gate PASS; cross-family WAIVED (confirmatory, codex auth-expired). NOT promoted. Pre-reg `docs/R2_REVERSE_LOOKUP_PREREG.md`; runner `experiments/track_c/r2_reverse_lookup.py`; result `results/r2_reverse_lookup.json`.
### R13 paraphrase-generalization (D-R13-1, 2026-06-25)
**D-R13-1** (R13 paraphrase-generalization / editing-overfit; `CORPUS/28`, 2026-06-25): **CHARACTERIZATION** — relabeled (NOT the spec storage-vs-behavior split; both probes behavioral, true L1-SELECT index-delegated per CP2). Counterfactual capital edits fire **100% on the trained prompt but mean ~22% across 3 natural paraphrases** (P1 8%/P2 17%/P3 42%; all-3-fail 54%; any 46%) → trained-prompt firing **over-reports usable knowledge**. BOUND (consistent-with, NOT isolated; domain/protocol confounds): editing AGAINST dense native priors generalizes far worse than fictional (R9 79% vs R13 22%); mechanism = narrow trained-template attractor FAILS to override native attractor (revert-to-native diagnostic UNLOGGED). CONSEQUENCE (narrowed): counterfactual overwrites of strong priors fragile to paraphrase at small N → qualifies B3N 'weights carry forward native-knowing' (true on trained phrasing; degrades vs priors); NOT generalized to native-knowing broadly (neutral/novel + recipes UNTESTED). Legitimate §8.9 point: L2 probe MUST use held-out paraphrases. band[4-8]/3B/N=24/capital/1-seed. Advisor + Perplexity-Sonar cross-family CONVERGED. NOT promoted. Pre-reg `docs/R13_STORAGE_BEHAVIOR_SPLIT_PREREG.md`; runner `experiments/track_c/r13_storage_behavior_split.py`; result `results/r13_storage_behavior_split.json`.
### R5 paraphrase-robustness (D-R5-1, 2026-06-25)
**D-R5-1** (R5 native-knowing paraphrase-robustness; `CORPUS/29`, 2026-06-25): **CHARACTERIZATION** — in-weight editing CAN produce USABLE paraphrase-robust knowledge, COMPETITOR-SPECIFICALLY. **NOVEL facts robust (16/16 all-3-hit, 100% mean); counterfactual-over-prior fragile (0/16 all-hit, 25% mean), 75% of failures revert to TRUE fact** (R13 mechanism confirmed + isolated within-protocol; competitor Fisher p≈0). Paraphrase-DIVERSE recipe partially rescues (25→65% mean, 13/0 sign p≈0.0002; full-robust only 3/16) — DIVERSITY not intensity (intensity-control 31%; isolated steps underpowered N=16, directional). **F1: INSERT path (novel+thin project facts) materially IMPROVED/robust; UPDATE-vs-pretraining fragile-but-partially-rescuable; update-over-prior-EDIT a SEPARATE UNMEASURED cell** (may localize=easier OR entangle=harder; not assumed between). B3N two-sided. Recipe amendment=CANDIDATE pending powered run. Advisor+Perplexity cross-family CONVERGED. band[4-8]/3B/N=16/capital/1-seed. NOT promoted. Pre-reg `docs/R5_PARAPHRASE_ROBUSTNESS_PREREG.md`; runner `experiments/track_c/r5_paraphrase_robustness.py`; results `results/r5_paraphrase_robustness.json`+`r5_stats.json`.
### C1 true-scale substrate diagnostic (D-C1TS-1, 2026-06-25)
**D-C1TS-1** (C1 true-scale substrate-feasibility DIAGNOSTIC; `docs/C1_TRUESCALE_SUBSTRATE_DIAGNOSTIC.md`, 2026-06-25): **DIAGNOSTIC — NOT a corruption datapoint, NOT promoted.** Operator-greenlit true-scale C1 attempt (B3N cond-3). Screen found 2,631 native-known `city→country` single-token facts (looked unblocked), but pilot + cheap diagnostics found the substrate **non-viable on two editability walls**: (1) **multi-token city subjects** (2,516/2,631) → key-collinear → ill-conditioned AlphaEdit solve → **ΔW blow-up (8900 vs 207), model emits garbage 'all-!'** (the N=2,000 pilot collapse is **weight-destruction, NOT compaction corruption** — single-token-subject N=50/100 stay clean: held-out 100%/86.7%, ΔW 207/294); (2) **strong-prior counterfactual under-expression**: even clean single-token subjects express only **27–42%** (R5/R13 counterfactual-over-prior fragility) < the ≥85% gate. → an edit-corruption substrate needs single-token-value + clean(≤2-tok)-subject + EXPRESSIBLE-counterfactual + cardinality≥2,000 + native SIMULTANEOUSLY; clean-subject single-token relations cap ~78–118. **True-2,000-scale C1 with real native knowledge is NOT runnable with this recipe/substrate → the 78-entity country pool (C1-(a), N≤100) stays the clean ceiling; C1 is now characterized AND bounded (substrate-ceiling'd).** Cost anchor: N=2,000 single-solve = 62 min, 8.4 GB VRAM. **Pilot-first + cheap-diagnostic discipline (advisor-steered) prevented a 4–10-day wasted grid.** Reinforces D-B3N-1 (unstable counterfactual-at-scale = the incremental path, already side-store-routed). Folded into `docs/F1_DETERMINATION.md` C1. Artifacts: `results/c1_scale_city_country_screen.json`, `results/c1_truescale_substrate_diagnostic.json`.

### C5 compaction-verify audit (D-C5-1, 2026-06-25)
**D-C5-1** (C5 compaction-verify audit; `docs/C5_COMPACTION_VERIFY_AUDIT.md`, 2026-06-25): **ANALYSIS (spec-read + CPU sampling-power) — corrects a pushed over-claim; NOT an experiment.** The C5 mount-verify governance fork. **CORRECTION:** post-compaction verification is **spec-MANDATED**, not the gap the R1-bit/R10 annotations claimed — §11.14/D66/C-OC3: a `CompactionProbeReport` to the TC **before Phase-2 commit**, **100% CORE / CORE=1.0-or-abort**, atomic 2PC; a CORE-diverged recompile never goes live (retracted across all artifacts). **Findings:** (A) C-OC3 by design tolerates non-CORE `behavior_fail` (≤5% SUPPORTING / ≤20% INCIDENTAL — a named §8.9/D19/D30 mode) that **nothing surfaces at read** (R1-bit's bit is tier-blind → the R13 L1/L2 split reappears post-compaction, un-flagged; D43 is transactional-isolation not fidelity, weights lossy per D42). (B) the non-CORE sampling (20%/floor-5, 10%/floor-3) has a **quantified detection-power deficit** near thresholds (exact hypergeometric; S=100/true-10% = 36% false-negative; floor-dominated; reliable only at large strata / large breach); **CORE fully protected**. **Prediction (NOT concluded → C1-true-scale / B3N cond-3):** CORE=1.0 vs sub-batched-compaction at the 8000-edge HARD-drift livelock (multiply untested; engine single-solve-vs-sub-batch open; committed→CORE + N100→8000 double-extrapolation). Recs (operator-gated): surface the C-OC3 tolerance at read + a tier/freshness-marked bit + lift small-stratum floors. F1 net readiness UNCHANGED; C5 stays prototyped-not-empirical. Artifacts: `results/c5_compaction_probe_power.json`.

### C10 multi-token VALUE expression — FALSIFIER FIRED (D-C10-1, 2026-06-25)
**D-C10-1** (`CORPUS/35`): **FALSIFIER FIRED** (scoped, NOT a universal law, NOT promotable). First on-target leg (target=`local Intel CPU + batch writes`). 3-arm novel-insert at the N≤100 batch path (binding metric = held-out-paraphrase full-sequence): single-token **97.2%** & prior-coherent multi-token **97.2%** robust, arbitrary **incoherent** multi-token **36.1%** (first-token 70.8%; **conditional P(full|first)=0.51 vs coherent 1.00** — de-confounds "weaker first-token edit"). The edit **FITS** the trained prompt (incoherent canon 95.8%) but does **NOT GENERALIZE** to held-out paraphrases → the multi-token instance of R13/R5 trained-prompt-parrot. Coherent positive control + clean fuller LAW#5 gate + base 0/24 close confounds; Run-1 counterfactual floored both arms (unrepresentative). **→ C10 = OPEN MUST-FIX for the fixed target (first condition on the critical path AND failing); named fix = AnyEdit per-token editing.** 3 advisor gates each caught a real error; Perplexity cross-family reviewed. Scope 3B/band[4-8]/AlphaEdit/N≤100/1-seed/HF-fp16; "cities"=controlled proxy not literal code. NOT promoted. Pre-reg `docs/C10_MULTI_TOKEN_VALUE_PREREG.md`.

### C6 ledger-immutability red-team (D-C6L-1, 2026-06-25)
**D-C6L-1** (C6 ledger-immutability red-team; `CORPUS/34`, 2026-06-25): **PROPERTY DEMONSTRATION + SPEC-LEVEL SECURITY FINDING — NOT an empirical red-team, NOT a spec falsification, NOT promotable.** First C6 move (only non-coupled, falsifiable, readiness-moving solo leg post-frontier-inflection). Demo vs the real G2 `verify_chain`: K1 rewrite-then-recompute (keyless; suppresses the W3 `INTEGRITY_VIOLATION_EMERGENCY` entry) → INTACT/UNDETECTED; K2 truncation → UNDETECTED; K4 naive-edit CONTROL → DETECTED (reproduces G2 T-LEDGER-TAMPER); K5 fix = CT-style Signed Tree Head (offline-signed `(seq,tip)`, custody-compatible CAK §20.2) → detects K1+K2. **Finding (K6), two claims:** (A, threat-model-INDEPENDENT, solid) spec mandates NO operational-window cryptographic tamper-evidence — keyless chain §16.1, external anchors only at root §13.2 + close §16.7/§27 → elevates CORPUS/11's prototype caveat to a spec-level property; (B, threat-model-SCOPED) spec never reconciles §16.5 (Orchestrator granted ledger fs-write) with §16.2 (Orchestrator compromise in-scope; §5 physical-only) → a logically-compromised Orchestrator can rewrite/suppress the W3 entry meant to flag it = an UNRECONCILED THREAT-MODEL SEAM (not a flat contradiction). **⚠ Advisor caught a first-pass "self-undermining gap" over-claim → corrected by naming the covering clause (§16.5) + why it doesn't cover** ([[spec-coherence-audit-is-circular]] #4). Vectors cross-checked vs Perplexity/CT; A3-fork/timestamp/2nd-preimage surveyed-excluded (single-machine v1.2). Moves C6 not-red-teamed → red-teamed (ONE mechanism); C6 stays open (key custody, Gate/token-forgery, boot). Recs (operator-gated): STH head-anchor + reconcile §16.5↔§16.2. NOT promoted. Pre-reg `docs/C6_LEDGER_IMMUTABILITY_REDTEAM_PREREG.md`; runner `experiments/governance/c6_ledger_immutability_redteam.py`; result `results/c6_ledger_immutability_redteam.json`.

### R11 medium/severity ON READ (D-R11-1, 2026-06-25)
**D-R11-1** (R11 content-scoped authoritative-medium & severity ON READ; `CORPUS/33`, 2026-06-25): **SPEC-COHERENCE AUDIT — NOT a falsifier** (matrix category ③, medium-delegated per §11.2/D42), CPU-only, NOT promotable. First BUILD-path leg after the FRONTIER INFLECTION (C2 read-contract). Q: does the spec's read surface determine (a) authoritative-medium/content-class and (b) divergence severity AT READ? **Verdict: COHERENT via derivation + prevention.** (a) "wrong medium wins on read" foreclosed by §11.3/D43 strong-consistency PREVENTION (reads block during mount; no stale-read fallback) + DIVERGED_STATE→system-wide trip §11.8; (b) content-class = f(entity_type) (§11.2) and severity = the SAME §11.7/D47 class-function → both **DERIVABLE-IF-TYPE-RETURNED** since §7.2/C4 mandates every entity is typed. **⚠ Advisor caught a first-pass over-claim** (the runner silently baked in "reads don't return entity_type" → a spurious 3-gap "spec-gap discovered"). Corrected: the read-RETURN shape is itself unspecified (no formal query-language section; §8.9 SELECT = existence read-back only) → R11's questions **restate the ONE already-recorded root gap** ('no formal query-language section') — NOT new F1 conditions → **DO NOT double-count**. Honesty: runner output = documentation echo of the spec read, not an independent check. Concrete spec rec: pin the query-result schema to return `entity_type` (or a derived class/severity field); optionally extend §26.3's read-quality-flag beyond L4-external. F1 impact: matrix R11 ❌→✅ characterized; no new conditions-register row. NOT promoted. Pre-reg `docs/R11_MEDIUM_ON_READ_PREREG.md`; runner `experiments/track_e/r11_medium_on_read.py`; result `results/r11_medium_on_read.json`.

### R1-bit commit-bit SELECT (D-R1-2, 2026-06-25)
**D-R1-2** (R1-bit commit-bit SELECT; `CORPUS/32`, 2026-06-25): **CP-class CONSTRUCTIVE DELIVERY — DELIVERED-FOR-SCOPE (commit-time)**, NOT a falsifier. Fixes R1's bleed-unsound reconciliation (D-R1-1): replaces the in-weight logprob storage-signature with a hash-chained G1 State Ledger whose per-(entity,relation,target) **commit-status bit is set only on a successful 2PC commit** — SELECT reads the bit, never the weights → **bleed-immune by construction**. D1 LANDED read-back 8/8 · D2 LEAK NULL 6/6 (model fires 5/6) · D3 GATE-REJECTED NULL 4/4 + **DROPPED NULL 2/2 via 2PC-abort (`VINDEX_FAILED`)** · D4 **bleed-immunity: Velloria** — the logprob proxy would return a phantom TRIPLE (sig 2.06≥2.0) but the **commit-bit correctly returns NULL** (no COMMITTED entry) · D5 ledger hash-chain intact, no PREPARED-bypass. **Scope = commit-TIME consistency ONLY**; post-commit divergence (compaction/quant/corruption drift, §11.3/D43/R10) **DEFERRED to C1 (operator-scope-gated), NOT solved** → R1 status = 'L1 read-back delivered for commit-time; post-commit divergence open' (NOT 'R1 delivered'). Free scope evidence: committed-triple expression 94–100% across all D20/C1 sub-batched-compaction cells (held-out is what corrupts). Re-assembles proven G1/CP1 parts; labeled CP-class like CP1/G3 (not falsification). band[4-8]/3B/N=8/1-seed/capital/fictional/CPU-governance. NOT promoted. Pre-reg `docs/R1_COMMIT_BIT_SELECT_PREREG.md`; runner `experiments/track_e/r1_commit_bit_select.py`; result `results/r1_commit_bit_select.json`.

### R1 SELECT read-back (D-R1-1, 2026-06-25)
**D-R1-1** (R1 SELECT triple-readback; `CORPUS/31`, 2026-06-25): **CHARACTERIZATION / NOT-DELIVERED** (frozen pre-reg `hard_fail=True`; mechanical label `FAIL_TAUTOLOGY` corrected — anti-firing held, so it is NOT intent/firing-echo). First build of the operator-chosen #1 frontier (INDEX/QUERY LAYER, load-bearing per §11.2/R2/R6/R9). Ledger-backed SELECT reconciled vs the deployed store: **abstains correctly on never-stored facts** — LEAK 6/6 NULL while the model *fires* the value 5/6 (≠greedy top-1), GATE-REJECTED 4/4 NULL, LANDED read-back 8/8. **But the NOVEL divergence test FAILED: DROPPED (ledger-claimed, store-not-persisted) flagged 1/2** — a phantom read `SELECT→(Velloria,capital,Tokyo)` (never written, doesn't fire) because cross-entity bleed from the 8 batch edits lifted common token 'Tokyo' to **+2.06 nats**, 0.06 over the frozen STORE_THRESH=2.0 (LANDED sigs 7.9–10.75 vs DROPPED 0.18/2.06). The **in-weight logprob storage-signature is UNSOUND — bleed-noise-limited not mistuned** (~50pp run-to-run swing [[sequential-edit-run-nondeterminism]] forecloses threshold-tuning). → R1 read-back **delegated-but-NOT-delivered**; **confirms B0/§11.2** (commit-status can't be inferred from weights → the ledger/`.vindex` medium must carry it). **Post-commit note (CORRECTED 2026-06-25, D-C5-1):** in-weight proxy bleed-unsound; a 2PC commit-bit (G1) is bleed-immune but covers **commit-time only** (no post-compaction freshness). The spec's post-compaction verify is **MANDATED, not unspecified** (§11.14/C-OC3, CORE=1.0 abort) — see `docs/C5_COMPACTION_VERIFY_AUDIT.md`. R13 split half-shown (fired-not-stored only; stored-not-fired 0 obs). GATE-REJECTED 4/4 only re-tests the proven G3 validator, not a new read result. band[4-8]/3B/N=8/1-seed/capital/fictional. Advisor done-gate done (corrected the mislabel + the 'peripheral proxy' reframe). NOT promoted. Pre-reg `docs/R1_SELECT_READBACK_PREREG.md`; runner `experiments/track_e/r1_select_readback.py`; result `results/r1_select_readback.json`.

### R5b overwrite-prior-edit (D-R5b-1, 2026-06-25)
**D-R5b-1** (R5b overwrite-prior-edit; `CORPUS/30`, 2026-06-25): **CHARACTERIZATION** — discriminating axis is **PRETRAINED-prior presence, NOT prior-EDIT presence** (advisor-corrected). Re-editing a NO-pretrained-prior (fictional) fact stays paraphrase-robust: OVERWRITE-EDIT 93.8% (15/16 all-hit) ≈ NOVEL 97.9% (p=1.0) ≫ PRIOR 14.6% (0/16, p<1e-6); prior edit v1 does NOT resurface (0/3). 2×2: no-prior×{1,2}edits robust; pretrained-prior×1edit fragile; **pretrained-prior×2edit (real-subject re-edit) UNMEASURED, predicted ≈PRIOR**. F1 CONDITION (not assumption): in-weight native-knowing robust IFF subjects lack an entrenched pretrained competitor (code/structural→yes; common-knowledge domain_concepts→no). Advisor done-gate PASS; cross-family WAIVED (decisive ordinal). band[4-8]/3B/N=16/1-seed. NOT promoted. Pre-reg `docs/R5B_OVERWRITE_PRIOR_EDIT_PREREG.md`; runner `experiments/track_c/r5b_overwrite_prior_edit.py`; results `results/r5b_overwrite_prior_edit.json`+`r5b_stats.json`.
