**Transparency caveat first:** the `$(sed -n 1,260p docs/C10K_ANYEDIT_OFFICIAL_UPSTREAM_PREREG.md)` in the item-under-review **did not expand** — I received the shell command literal, not the prereg body. I also have no file-read tool in this environment (Drive/Prospecting/InfraNodus/advisor only), so I cannot fetch the 260 lines myself. The review below is against the **design as described in your context block + the upstream facts you listed + program memory**. Where a point depends on the literal text, I mark it **CONFIRM-IN-TEXT** — some of these gates may already be present, in which case treat them as "verify," not "add."

---

**VERDICT: FIX-FIRST**

The *direction* is right — going to official upstream is the correct response to "local transplant failure ≠ upstream falsification," and it is not bounded-hybrid drift. But Stage 0 is exactly where environment/source faithfulness is decided, and there are concrete must-fix gates before that packet locks in. Not OVERTURNED (direction sound); not PROCEED (faithfulness landmines are load-bearing and currently unaddressed in what I can see).

**Single most important next action:** Add a mandatory **Stage 0 known-baseline reproduction gate (LAW#3)** *and* resolve the **transformers-version vs Qwen2.5 incompatibility** — before any A1/A2 conversion or run. Concretely: reproduce a *stock* upstream UnKE number on upstream's own dataset/config to within range first. Until that passes, every A1/A2 result is uninterpretable — you cannot distinguish "AnyEdit fails C10" from "our adapter/env is wrong," which is the *exact* ambiguity that made C10h/C10J uninformative. Running upstream without this gate just rebuilds the transplant problem one layer up.

**Issues, priority order:**

1. **Faithfulness landmine — deps are self-contradictory for Qwen2.5 (highest).** README pins `transformers==4.23.1` (late-2022). Qwen2.5 architecture support did not exist until transformers ~4.43+. **You cannot load Qwen2.5-7B on 4.23.1.** Therefore upstream's own `hparams/AlphaEdit_ARE/Qwen2.5-7B-Instruct.json` must have been produced on a *newer* transformers than the README states. Stage 0 must pin **the transformers version upstream actually used for the Qwen2.5 config** (check their git history/CI/issues), not the GPT-J-era README pin. If you guess, it's a transplant again. **CONFIRM-IN-TEXT** whether the prereg already nails this.

2. **Hardware vs faithfulness collision.** Upstream says A100 80G; our pod is a 4090 24GB. 7B ARE (window_size=50, layers [4-8], lm_head editing) at full faithfulness likely won't fit unmodified — and our known 7B-on-24GB tricks (`eigh`-not-`svd`, `expandable_segments`, del-Pi-before-solve; memory `wide-intermediate-7b-editing-vram`) are **modifications** → no longer source-faithful. Resolve explicitly in Stage 0: either confirm 7B ARE fits **unmodified** in 24GB, or rent an A100 (forward infra, pre-approved). Do not silently apply our VRAM patches and still call it "official upstream."

3. **F1 north-star: 7B-upstream cannot *close* C10 — pre-commit the outcome→consequence map (north-star critical).** Our entire C10 substrate is Qwen2.5-**3B** (deployment = 3B/Q4_K/CPU; memory `no-model-cleared-full-bars-c10-included`, `native-knowledge-screen-is-not-editability`). Upstream ships **7B** ARE. So bake the asymmetry into the prereg *now*, before results:
   - **7B-upstream PASS = mechanism-viability existence proof only.** It does NOT close C10 — it still owes a *faithful* 3B port + Q4/CPU survival, which reintroduces the transplant step you're currently treating as un-validated. Guard against a 7B pass being over-sold as "C10 solved."
   - **7B-upstream FAIL on its OWN config/dataset = strong falsification of AnyEdit's multi-token-value mechanism** — high F1 value, and the cleaner outcome.
   This is the crux of your Q3: a 7B run can *meaningfully license* a later 3B A7 addendum only as "mechanism exists, now port faithfully" — never as direct C10 closure. State that explicitly or the addendum will drift.

4. **A1/A2 licensing metric — run BOTH, don't substitute (metric-match).** Binding C10 metric is held-out-paraphrase **FULL-SEQUENCE `para_full`** + conditional P(full|first); canonical/teacher-forced numbers are artifacts (memory `default-to-the-less-flattering-reading`, `in-weight-value-expression-experiment-design`). UnKE's `question`/`para_question` is the right vehicle. Risk: upstream `evaluate_uns` reports its *own* metric (rewrite/paraphrase/neighborhood, often token-F1/gen-quality) which does **not** match the C10 claim. Right move: keep upstream's metric for the **faithfulness/easy-control** read, AND compute **our `para_full`** on the *same generated outputs*. If the prereg binds A1/A2 on upstream's metric alone, that's a FIX. **CONFIRM-IN-TEXT.**

5. **Data-adapter is a backdoor transplant.** A1/A2 must be converted to UnKE `final_data_v3.json` (`question/para_question/answer/sub_question`). Prompt construction, `sub_question` handling, answer formatting are all places faithfulness silently breaks. Stage 0 should pin the adapter exactly and validate it by round-tripping one *stock* UnKE item through it first (folds into gate #1).

6. **Bounded-hybrid drift — verify the escape clause.** Direction is clean (no hybrid proposed). But **CONFIRM-IN-TEXT** that the prereg's failure branch does not say "if upstream also fails, accept bounded hybrid as closure" — that would re-drift into the position the operator explicitly rejects. The correct failure branch for upstream-FAIL is "C10 in-weight mechanism falsified at the strongest available method → F1 reports in-weight cannot serve project-coined multi-word semantic values," not hybrid acceptance.

**Cheapest falsifier of the whole plan (the overturning test):** the Stage 0 stock-UnKE reproduction (gate #1). If you cannot reproduce a single stock upstream UnKE number in your "upstream-compatible" env, the env is not faithful and no A1/A2 result counts — cheap, mandatory, and it directly tests the thing (faithfulness) the whole prereg rests on.

I did **not** call the meta-`advisor()` here: the binding limitation is the unrendered prereg source, which that call can't recover (it sees the same transcript). Please paste the actual 260 lines (or confirm the gates above are present) and I'll convert each **CONFIRM-IN-TEXT** into a hard verify.
